---
phase: 13-pkgdown-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - _pkgdown.yml
  - DESCRIPTION
autonomous: true
requirements: [PKG-02]
tags: [pkgdown, site-config, reference-taxonomy, article-grouping]

must_haves:
  truths:
    - "User visiting /articles/ finds 3 narrative groups (Getting started, Workflow, Data sources in practice) in the canonical Phase 12 D-04 reading order"
    - "User visiting /reference/ finds 6 function groups (Data import, Matrix building, Compute & models, Pipeline helpers, Paper replication, Output & export) each with a one-sentence `desc:` and functions ordered by pipeline flow"
    - "pkgdown::check_pkgdown() exits 0 — every exported symbol appears exactly once, every vignette appears exactly once, URL check passes"
    - "Navbar, template (bootstrap: 5), home block, and url remain exactly as they are (D-09)"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "pkgdown site config with D-06..D-13 taxonomy applied"
      contains: "Getting started"
    - path: "DESCRIPTION"
      provides: "Package metadata; URL list aligned with _pkgdown.yml url: if Wave-0 check flags mismatch"
      contains: "URL:"
  key_links:
    - from: "_pkgdown.yml (articles)"
      to: "vignettes/*.Rmd"
      via: "contents: list of basenames"
      pattern: "getting-started|package-design|data-preparation|modeling-and-outputs|paper-replication|figaro-workflow|pipeline-helpers"
    - from: "_pkgdown.yml (reference)"
      to: "NAMESPACE exports"
      via: "contents: list of function names"
      pattern: "import_suts|compute_sube|plot_sube"
    - from: "DESCRIPTION URL:"
      to: "_pkgdown.yml url:"
      via: "pkgdown::check_pkgdown() URL consistency check"
      pattern: "davidzenz.github.io/sube"
---

<objective>
Realign `_pkgdown.yml` article grouping, ordering, and reference-section taxonomy to match the documentation narrative from Phases 11–12; run a Wave-0 `pkgdown::check_pkgdown()` to verify URL consistency and patch DESCRIPTION only if check errors.

Purpose: Satisfy PKG-02 — the pkgdown site surfaces articles in the canonical reading order (D-06), groups them into 3 narrative sections (D-07), splits the reference index into 6 pipeline-ordered groups with one-sentence descriptions (D-10..D-12), and exposes only the 18 NAMESPACE exports (D-13).

Output:
- `_pkgdown.yml` — `reference:` and `articles:` blocks rewritten (url/template/home/navbar preserved verbatim)
- `DESCRIPTION` — `URL:` field patched ONLY if Task 1's `check_pkgdown()` flags the mismatch (L-06); otherwise untouched
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pkgdown-deployment/13-CONTEXT.md
@.planning/phases/13-pkgdown-deployment/13-RESEARCH.md
@.planning/phases/13-pkgdown-deployment/13-PATTERNS.md
@NAMESPACE
@DESCRIPTION
@_pkgdown.yml

<interfaces>
<!-- Source-of-truth data the executor writes against. Do NOT rederive — use these verbatim. -->

### 18 NAMESPACE exports (from `/home/zenz/R/sube/NAMESPACE`, verified)

```
export(batch_sube)
export(build_matrices)
export(compute_sube)
export(estimate_elasticities)
export(extract_leontief_matrices)
export(extract_domestic_block)
export(filter_paper_outliers)
export(filter_sube)
export(import_suts)
export(read_figaro)
export(run_sube_pipeline)
export(plot_paper_comparison)
export(plot_paper_interval_ranges)
export(plot_paper_regression)
export(plot_sube)
export(prepare_sube_comparison)
export(sube_example_data)
export(write_sube)
```

### 7 vignette basenames (from `/home/zenz/R/sube/vignettes/*.Rmd`, verified)

`getting-started`, `package-design`, `data-preparation`, `modeling-and-outputs`, `paper-replication`, `figaro-workflow`, `pipeline-helpers`

### D-10 reference group taxonomy (D-12 pipeline order applied)

| # | Group `title:` | Ordered `contents:` | Pipeline rationale |
|---|---|---|---|
| 1 | Data import | `import_suts`, `read_figaro`, `sube_example_data` | Workbook import first; FIGARO CSV parallel alternative; shipped example data is a fallback/demo |
| 2 | Matrix building | `extract_domestic_block`, `build_matrices`, `extract_leontief_matrices` | Cut domestic block; build S/U/tax/margin; derive Leontief |
| 3 | Compute & models | `compute_sube`, `estimate_elasticities` | Compute multipliers; elasticity regressions operate on results |
| 4 | Pipeline helpers | `run_sube_pipeline`, `batch_sube` | Single-run wrapper; batch/multi-year wrapper |
| 5 | Paper replication | `filter_paper_outliers`, `prepare_sube_comparison`, `plot_paper_comparison`, `plot_paper_regression`, `plot_paper_interval_ranges` | Filter → shape → three plot styles |
| 6 | Output & export | `filter_sube`, `plot_sube`, `write_sube` | Filter → inspect → write to disk |

**Total: 18 functions — matches NAMESPACE export count exactly. No duplicates, no internals.**

### D-11 `desc:` candidates (from 13-RESEARCH.md §Reference Group Descriptions)

| Group | `desc:` sentence (≤15 words; planner discretion on exact wording) |
|---|---|
| Data import | Ingest supply-use tables from WIOD workbooks, FIGARO CSVs, or shipped example datasets. |
| Matrix building | Extract the domestic block and assemble the supply, use, and Leontief matrices. |
| Compute & models | Compute SUBE multipliers and estimate panel or cross-sectional elasticity regressions. |
| Pipeline helpers | One-call wrappers from raw input paths to SUBE results, for single runs or batches. |
| Paper replication | Filter outliers, prepare comparison tibbles, and plot the paper's three diagnostic views. |
| Output & export | Filter, visualize, and write SUBE results to CSV, Excel, Stata, or PDF. |

### D-06 + D-07 article grouping (3 groups, D-06 reading order within each)

| # | Group `title:` | `navbar:` | Ordered `contents:` |
|---|---|---|---|
| 1 | Getting started | `Get started` (D-08) | `getting-started`, `package-design` |
| 2 | Workflow | (none) | `data-preparation`, `modeling-and-outputs`, `pipeline-helpers` |
| 3 | Data sources in practice | (none) | `paper-replication`, `figaro-workflow` |

**Total: 7 vignettes — matches `ls vignettes/*.Rmd` count exactly. No duplicates, no missing.**

### Blocks to preserve VERBATIM in `_pkgdown.yml` (D-09)

```yaml
url: https://davidzenz.github.io/sube/
template:
  bootstrap: 5

home:
  title: sube
  description: Supply-use based econometric tools for Leontief benchmarks,
    SUBE models, and paper-style comparison workflows in R.
```

```yaml
navbar:
  structure:
    left:  [reference, articles, paper, news]
    right: [search, github]
  components:
    paper:
      text: Paper
      href: https://link.springer.com/article/10.1186/s40008-024-00331-4
    news:
      text: News
      href: news/index.html
```

### Current state of `_pkgdown.yml` (what's being rewritten — reference only)

Full 72-line file available at `/home/zenz/R/sube/_pkgdown.yml`. Target modifications: lines 10–36 (reference block — complete rewrite) and lines 38–60 (articles block — complete rewrite). Lines 1–8 and 62–72 MUST be preserved exactly.

### Current DESCRIPTION URL line (line 15, for L-06 check)

```
URL: https://github.com/davidzenz/sube
```

`_pkgdown.yml` line 1: `url: https://davidzenz.github.io/sube/`

These differ. `pkgdown::check_pkgdown()` may or may not flag this (research Assumption A3, MEDIUM confidence). Task 1 runs the check and decides whether to edit DESCRIPTION.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 URL consistency check — run pkgdown::check_pkgdown() on current config, decide if DESCRIPTION URL needs extension</name>

  <read_first>
    - `/home/zenz/R/sube/DESCRIPTION` (line 15 — current `URL:` value)
    - `/home/zenz/R/sube/_pkgdown.yml` (line 1 — current `url:` value)
    - `/home/zenz/R/sube/.planning/phases/13-pkgdown-deployment/13-RESEARCH.md` §Landmines L-06 (URL mismatch semantics)
    - `/home/zenz/R/sube/.planning/phases/13-pkgdown-deployment/13-PATTERNS.md` §Source-of-Truth References §DESCRIPTION URL (line 15) (explicit fix if check errors)
  </read_first>

  <files>DESCRIPTION</files>

  <action>
Run `pkgdown::check_pkgdown()` on the CURRENT (pre-rewrite) `_pkgdown.yml` and use the exit status to decide whether to edit `DESCRIPTION:15`:

```bash
Rscript -e 'pkgdown::check_pkgdown()' 2>&1 | tee /tmp/phase13-check-wave0.log
echo "EXIT=$?"
```

Branch on the result:

**Case A — check passes (exit 0, no URL-related error):**
- DESCRIPTION is fine as-is. Do NOT edit it.
- Log outcome: write a single line to `.planning/phases/13-pkgdown-deployment/13-02-WAVE0.log` reading `URL check: PASS — DESCRIPTION unchanged`.

**Case B — check fails with a URL-related error:**
The error text will mention one or more of: "URL", "pkgdown url", "DESCRIPTION URL", or reference the pages URL. In that case, edit `DESCRIPTION:15` from:

```
URL: https://github.com/davidzenz/sube
```

to:

```
URL: https://github.com/davidzenz/sube, https://davidzenz.github.io/sube/
```

(Comma-separated list — this is the fix specified in 13-PATTERNS.md §Source-of-Truth References §DESCRIPTION URL.)

Re-run `Rscript -e 'pkgdown::check_pkgdown()'`. The URL error MUST now be gone (exit 0 or only non-URL errors — the non-URL errors about missing exports/vignettes are expected at this stage because the reference/articles blocks haven't been rewritten yet; those are Task 2's concern).

Log outcome: write to `.planning/phases/13-pkgdown-deployment/13-02-WAVE0.log` two lines: `URL check: FAIL (pre-fix)` and `URL check: PASS (post-fix, DESCRIPTION extended to include https://davidzenz.github.io/sube/)`.

**Case C — check fails with only non-URL errors (e.g., missing exports, missing vignettes):**
Those errors will be fixed by Task 2's rewrite. DESCRIPTION stays untouched.
- Log outcome: `URL check: PASS (non-URL errors present — addressed by Task 2) — DESCRIPTION unchanged`.

**Decision rule:** Edit DESCRIPTION ONLY if the check output explicitly mentions a URL mismatch. Do NOT edit DESCRIPTION for any other reason (scope constraint from `<phase_scope>`: no DESCRIPTION content changes except this one conditional URL extension per L-06).
  </action>

  <verify>
    <automated>test -f .planning/phases/13-pkgdown-deployment/13-02-WAVE0.log && { if grep -q "DESCRIPTION unchanged" .planning/phases/13-pkgdown-deployment/13-02-WAVE0.log; then grep -q "^URL: https://github.com/davidzenz/sube$" DESCRIPTION; else grep -q "^URL: https://github.com/davidzenz/sube, https://davidzenz.github.io/sube/$" DESCRIPTION; fi; }</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/13-pkgdown-deployment/13-02-WAVE0.log` — outcome log exists
    - `grep -qE "^URL check: (PASS|FAIL)" .planning/phases/13-pkgdown-deployment/13-02-WAVE0.log` — outcome line present
    - If log says "DESCRIPTION unchanged": `grep -q "^URL: https://github.com/davidzenz/sube$" DESCRIPTION` — DESCRIPTION line 15 is exactly the original
    - If log says "DESCRIPTION extended": `grep -q "^URL: https://github.com/davidzenz/sube, https://davidzenz.github.io/sube/$" DESCRIPTION` — comma-separated two-URL form
    - In either branch: `Rscript -e 'cat(as.character(any(grepl("davidzenz.github.io/sube", readLines("DESCRIPTION")))))' 2>/dev/null` returns `TRUE` — pages URL must be reachable from DESCRIPTION if the check required it, and is at minimum not removed if it was already present
    - No other DESCRIPTION fields modified: `git diff DESCRIPTION` touches only line 15 (if any change) — verifiable by `git diff --unified=0 DESCRIPTION | grep -cE '^[-+][^-+]' || true` returns 0 or 2 (no change = 0; one line changed = 2: one `-` and one `+`)
  </acceptance_criteria>

  <done>Wave-0 URL check ran; `13-02-WAVE0.log` documents the outcome; DESCRIPTION is either unchanged (Case A/C) or extended to a comma-separated two-URL form (Case B); no other DESCRIPTION fields touched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _pkgdown.yml reference: and articles: blocks per D-06..D-13; preserve url/template/home/navbar verbatim</name>

  <read_first>
    - `/home/zenz/R/sube/_pkgdown.yml` (full current 72-line file — identify lines 1–8 and 62–72 to preserve, lines 10–36 and 38–60 to rewrite)
    - `/home/zenz/R/sube/NAMESPACE` (18 exports — verify every function name in the rewritten `reference:` block exists as an export)
    - `/home/zenz/R/sube/vignettes/` (7 Rmd basenames — verify every basename in the rewritten `articles:` block exists as a vignette)
    - `/home/zenz/R/sube/.planning/phases/13-pkgdown-deployment/13-CONTEXT.md` §Decisions D-06..D-13 (locked article grouping + reference taxonomy)
    - `/home/zenz/R/sube/.planning/phases/13-pkgdown-deployment/13-RESEARCH.md` §Reference Group Function Order (D-12 pipeline order table), §Reference Group Descriptions (D-11 candidate sentences), §_pkgdown.yml Schema Notes
    - `/home/zenz/R/sube/.planning/phases/13-pkgdown-deployment/13-PATTERNS.md` §Target Files §3 (shape of replacement for reference + articles blocks, blocks to keep verbatim)
  </read_first>

  <files>_pkgdown.yml</files>

  <action>
Rewrite `_pkgdown.yml` in place so that lines 1–8 (url, template, home) and the entire `navbar:` block (currently lines 62–72) are preserved EXACTLY, while the `reference:` and `articles:` blocks are replaced with the D-10/D-11/D-12 (reference) and D-06/D-07/D-08 (articles) shapes.

Final file contents (verbatim — this is the exact content to write):

```yaml
url: https://davidzenz.github.io/sube/
template:
  bootstrap: 5

home:
  title: sube
  description: Supply-use based econometric tools for Leontief benchmarks,
    SUBE models, and paper-style comparison workflows in R.

reference:
  - title: Data import
    desc: Ingest supply-use tables from WIOD workbooks, FIGARO CSVs, or shipped example datasets.
    contents:
      - import_suts
      - read_figaro
      - sube_example_data
  - title: Matrix building
    desc: Extract the domestic block and assemble the supply, use, and Leontief matrices.
    contents:
      - extract_domestic_block
      - build_matrices
      - extract_leontief_matrices
  - title: Compute & models
    desc: Compute SUBE multipliers and estimate panel or cross-sectional elasticity regressions.
    contents:
      - compute_sube
      - estimate_elasticities
  - title: Pipeline helpers
    desc: One-call wrappers from raw input paths to SUBE results, for single runs or batches.
    contents:
      - run_sube_pipeline
      - batch_sube
  - title: Paper replication
    desc: Filter outliers, prepare comparison tibbles, and plot the paper's three diagnostic views.
    contents:
      - filter_paper_outliers
      - prepare_sube_comparison
      - plot_paper_comparison
      - plot_paper_regression
      - plot_paper_interval_ranges
  - title: Output & export
    desc: Filter, visualize, and write SUBE results to CSV, Excel, Stata, or PDF.
    contents:
      - filter_sube
      - plot_sube
      - write_sube

articles:
  - title: Getting started
    navbar: Get started
    contents:
      - getting-started
      - package-design
  - title: Workflow
    contents:
      - data-preparation
      - modeling-and-outputs
      - pipeline-helpers
  - title: Data sources in practice
    contents:
      - paper-replication
      - figaro-workflow

navbar:
  structure:
    left:  [reference, articles, paper, news]
    right: [search, github]
  components:
    paper:
      text: Paper
      href: https://link.springer.com/article/10.1186/s40008-024-00331-4
    news:
      text: News
      href: news/index.html
```

Decisions baked in (do NOT alter):
- `url:`, `template:`, `home:`, and `navbar:` blocks are BYTE-IDENTICAL to the current file — D-09 locks navbar; scope constraint preserves url/template/home.
- **Reference — 6 groups, D-10 / D-11 / D-12:**
  - Exactly 6 `- title:` entries, in the D-10 order: Data import → Matrix building → Compute & models → Pipeline helpers → Paper replication → Output & export.
  - Each group has a `desc:` sentence (D-11 candidates from research; wording within Claude's discretion but MUST be one sentence ≤15 words).
  - Function order within each group is pipeline-flow per D-12 (see table in interfaces).
  - Total function count across all groups = 18 (matches NAMESPACE exports); each export appears EXACTLY ONCE (D-13 — no internals, no duplicates).
- **Articles — 3 groups, D-06 / D-07 / D-08:**
  - Exactly 3 `- title:` entries, in the D-07 order: Getting started → Workflow → Data sources in practice.
  - `navbar: Get started` appears ONLY on the Getting started group (D-08) — no `navbar:` on the other two groups.
  - Basenames within each group are in D-06 reading order (see table in interfaces).
  - Total basename count across all groups = 7 (matches `vignettes/*.Rmd`); each basename appears EXACTLY ONCE.
- **NO article-group `desc:`** added (research Open Question #2; CONTEXT D-11 mandates desc only for reference groups — planner's decision is to NOT parallel the treatment on article groups, keeping the articles block minimal).
- **NO custom `components.get-started:`** entry added — L-02 warns this would double-list in the navbar.

After writing, run `Rscript -e 'pkgdown::check_pkgdown()'`. It MUST exit 0 with no errors about missing exports, duplicate exports, missing vignettes, duplicate vignettes, or URL mismatch (the URL case was settled in Task 1). If it errors:
- "export XYZ not indexed": executor missed a NAMESPACE entry in `contents:` — compare against NAMESPACE grep and fix.
- "duplicate export XYZ": executor put the same function in two groups — compare against D-10 table and fix.
- "vignette XYZ not indexed": executor missed a vignette basename — compare against `vignettes/*.Rmd` and fix.
- "URL mismatch": regression from Task 1 — recheck DESCRIPTION and `_pkgdown.yml url:`.
  </action>

  <verify>
    <automated>Rscript -e 'pkgdown::check_pkgdown()' && grep -q "^url: https://davidzenz.github.io/sube/$" _pkgdown.yml && grep -q "^template:$" _pkgdown.yml && grep -q "^  bootstrap: 5$" _pkgdown.yml && grep -qE "^    left:  \[reference, articles, paper, news\]" _pkgdown.yml && grep -qE "^    right: \[search, github\]" _pkgdown.yml && test "$(grep -cE '^  - title:' _pkgdown.yml)" = "9" && test "$(awk '/^reference:/{f=1;next}/^articles:/{f=0}f && /^  - title:/' _pkgdown.yml | wc -l)" = "6" && test "$(awk '/^articles:/{f=1;next}/^navbar:/{f=0}f && /^  - title:/' _pkgdown.yml | wc -l)" = "3" && grep -q "    navbar: Get started" _pkgdown.yml && test "$(grep -c '^    navbar: Get started' _pkgdown.yml)" = "1" && for fn in import_suts read_figaro sube_example_data extract_domestic_block build_matrices extract_leontief_matrices compute_sube estimate_elasticities run_sube_pipeline batch_sube filter_paper_outliers prepare_sube_comparison plot_paper_comparison plot_paper_regression plot_paper_interval_ranges filter_sube plot_sube write_sube; do grep -q "      - $fn$" _pkgdown.yml || { echo "MISSING: $fn"; exit 1; }; done && for vg in getting-started package-design data-preparation modeling-and-outputs paper-replication figaro-workflow pipeline-helpers; do grep -q "      - $vg$" _pkgdown.yml || { echo "MISSING vignette: $vg"; exit 1; }; done</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript -e 'pkgdown::check_pkgdown()'` exits 0 — the canonical static check passes (PKG-02 primary acceptance)
    - `grep -q "^url: https://davidzenz.github.io/sube/$" _pkgdown.yml` — url preserved byte-identical
    - `grep -q "^template:$" _pkgdown.yml && grep -q "^  bootstrap: 5$" _pkgdown.yml` — template block preserved
    - `grep -q "^home:$" _pkgdown.yml && grep -q "^  title: sube$" _pkgdown.yml` — home block preserved
    - `grep -qE "^    left:  \[reference, articles, paper, news\]" _pkgdown.yml` — navbar structure preserved (D-09)
    - `grep -qE "^    right: \[search, github\]" _pkgdown.yml` — navbar right preserved (D-09)
    - `grep -q "href: https://link.springer.com/article/10.1186/s40008-024-00331-4" _pkgdown.yml` — paper navbar component preserved
    - `grep -q "href: news/index.html" _pkgdown.yml` — news navbar component preserved
    - `awk '/^reference:/{f=1;next}/^articles:/{f=0}f && /^  - title:/' _pkgdown.yml | wc -l` returns `6` — exactly 6 reference groups (D-10)
    - `awk '/^articles:/{f=1;next}/^navbar:/{f=0}f && /^  - title:/' _pkgdown.yml | wc -l` returns `3` — exactly 3 article groups (D-07)
    - `grep -c "^  - title:" _pkgdown.yml` returns `9` — total = 6 reference + 3 article groups (no more, no fewer)
    - `grep -c "^    desc: " _pkgdown.yml` returns `6` — every reference group has a desc (D-11); no article group has a desc (planner decision)
    - `grep -c "^    navbar: Get started$" _pkgdown.yml` returns `1` — navbar label appears exactly once, on Getting started group (D-08); L-02 double-listing avoided
    - `grep -qE "^  - title: Data import$" _pkgdown.yml` and `grep -qE "^  - title: Matrix building$" _pkgdown.yml` and `grep -qE "^  - title: Compute & models$" _pkgdown.yml` and `grep -qE "^  - title: Pipeline helpers$" _pkgdown.yml` and `grep -qE "^  - title: Paper replication$" _pkgdown.yml` and `grep -qE "^  - title: Output & export$" _pkgdown.yml` — all 6 D-10 reference group titles present
    - `grep -qE "^  - title: Getting started$" _pkgdown.yml` and `grep -qE "^  - title: Workflow$" _pkgdown.yml` and `grep -qE "^  - title: Data sources in practice$" _pkgdown.yml` — all 3 D-07 article group titles present
    - Every export from NAMESPACE appears as a bullet in `contents:` (shell loop in `<verify>` checks all 18): `import_suts`, `read_figaro`, `sube_example_data`, `extract_domestic_block`, `build_matrices`, `extract_leontief_matrices`, `compute_sube`, `estimate_elasticities`, `run_sube_pipeline`, `batch_sube`, `filter_paper_outliers`, `prepare_sube_comparison`, `plot_paper_comparison`, `plot_paper_regression`, `plot_paper_interval_ranges`, `filter_sube`, `plot_sube`, `write_sube`
    - Every vignette basename appears as a bullet in `articles: contents:` (shell loop checks all 7): `getting-started`, `package-design`, `data-preparation`, `modeling-and-outputs`, `paper-replication`, `figaro-workflow`, `pipeline-helpers`
    - No function appears twice: `awk '/^    contents:/{f=1;next}/^  - title:|^articles:|^navbar:/{f=0}f && /^      - /' _pkgdown.yml | sort | uniq -d | wc -l` returns `0` (no duplicates across any `contents:` block)
    - No internals leak in: `grep -E "^      - " _pkgdown.yml | grep -vE "^      - (import_suts|read_figaro|sube_example_data|extract_domestic_block|build_matrices|extract_leontief_matrices|compute_sube|estimate_elasticities|run_sube_pipeline|batch_sube|filter_paper_outliers|prepare_sube_comparison|plot_paper_comparison|plot_paper_regression|plot_paper_interval_ranges|filter_sube|plot_sube|write_sube|getting-started|package-design|data-preparation|modeling-and-outputs|paper-replication|figaro-workflow|pipeline-helpers)$"` returns empty (no bullet is anything other than an approved export or vignette basename)
  </acceptance_criteria>

  <done>`_pkgdown.yml` has 6 reference groups with pipeline-ordered contents and one-sentence descs; 3 article groups in D-07 order with `navbar: Get started` only on Getting started; `pkgdown::check_pkgdown()` exits 0; url/template/home/navbar blocks byte-identical to the pre-rewrite state; all grep and count acceptance criteria pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_pkgdown.yml` → pkgdown build → rendered HTML | Site config is trusted (author-controlled); rendered output is served to anonymous web visitors |
| Rendered HTML → external link targets (Springer paper URL, GitHub repo) | Third-party URLs embedded in navbar — same-site CSP trust flows outward |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Information Disclosure | `reference:` exposing internal symbols | mitigate | D-13 locks reference to the 18 NAMESPACE exports only; acceptance criterion checks no bullet is anything other than an approved export. `check_pkgdown()` enforces no missing exports. |
| T-13-08 | Tampering | `_pkgdown.yml` URL field drift from DESCRIPTION | mitigate | Task 1 runs `check_pkgdown()` as a Wave-0 gate; if mismatch is flagged, DESCRIPTION is patched to a comma-separated two-URL form (L-06). Acceptance criteria verify the final URL state. |
| T-13-09 | Tampering | Navbar external link targets | accept | Paper link (Springer) and news link are hardcoded; no user input flows into hrefs. Links are author-controlled and preserved verbatim from the pre-rewrite state. |
| T-13-10 | Denial of Service | pkgdown build failing on a malformed YAML | mitigate | `pkgdown::check_pkgdown()` in Task 2 verify is the gate; if it errors (typically on duplicate or missing topic/vignette), the task does not complete. PR smoke-build in Plan 01 catches the same regression on downstream PRs. |
</threat_model>

<verification>
**Static checks (run by /gsd-verify-work):**

- Wave-0 log exists: `test -f .planning/phases/13-pkgdown-deployment/13-02-WAVE0.log`
- `Rscript -e 'pkgdown::check_pkgdown()'` exits 0 — the comprehensive static check (URL consistency, export coverage, vignette coverage) passes
- All Task 2 grep and count acceptance criteria pass
- `Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); cat(length(y$reference), length(y$articles))'` returns `6 3` — programmatic count via `yaml::read_yaml` (alternative to awk, works regardless of indent variations)
- `Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); cat(sum(sapply(y$reference, function(g) length(g$contents))))'` returns `18` — total exported symbols indexed
- `Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); cat(sum(sapply(y$articles, function(g) length(g$contents))))'` returns `7` — total vignettes indexed

**Cross-artifact consistency checks:**

- Every NAMESPACE export is in `_pkgdown.yml` reference `contents`: `diff <(grep -oP '(?<=^export\()[^)]+' NAMESPACE | sort) <(Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); cat(sort(unlist(lapply(y$reference, function(g) g$contents))), sep="\n")' | sort)` returns empty
- Every vignette basename is in `_pkgdown.yml` articles `contents`: `diff <(ls vignettes/*.Rmd | xargs -n1 basename | sed 's/\.Rmd$//' | sort) <(Rscript -e 'y <- yaml::read_yaml("_pkgdown.yml"); cat(sort(unlist(lapply(y$articles, function(g) g$contents))), sep="\n")' | sort)` returns empty

**Manual verification deferred to Plan 03:**

- D-15: Post-merge visual check of https://davidzenz.github.io/sube/articles/ — confirms the 3 article groups render in the correct order, and the Getting started group appears in the navbar dropdown labeled "Get started"
- D-15: Post-merge visual check of https://davidzenz.github.io/sube/reference/ — confirms the 6 reference groups render with their desc sentences and pipeline-ordered function lists
</verification>

<success_criteria>

- `_pkgdown.yml` has 6 reference groups (D-10), each with a one-sentence `desc:` (D-11), contents ordered by pipeline flow (D-12), covering exactly the 18 NAMESPACE exports with no duplicates and no internals (D-13)
- `_pkgdown.yml` has 3 article groups (D-07) in D-06 reading order, with `navbar: Get started` only on the Getting started group (D-08), covering all 7 vignettes exactly once
- `url:`, `template:`, `home:`, and `navbar:` blocks are byte-identical to the pre-rewrite state (D-09 + scope constraint)
- `pkgdown::check_pkgdown()` exits 0 after both tasks complete
- DESCRIPTION is either unchanged (if the URL check passed as-is) or extended to the comma-separated two-URL form (if L-06 fired); no other DESCRIPTION fields touched
- All Task 1 and Task 2 acceptance criteria (greps, counts, diffs) pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-pkgdown-deployment/13-02-SUMMARY.md` documenting:
- Which branch of the Wave-0 URL check fired (Case A / B / C) and the resulting DESCRIPTION state
- Reference and articles blocks rewritten; url/template/home/navbar byte-identical
- `pkgdown::check_pkgdown()` exit status (must be 0)
- Cross-artifact consistency verified (all 18 exports indexed, all 7 vignettes indexed, no duplicates)
- Manual D-15 verification steps flagged for Plan 03's VERIFICATION.md
</output>
