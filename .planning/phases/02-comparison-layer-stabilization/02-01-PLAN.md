---
phase: 02-comparison-layer-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/paper_tools.R
  - R/utils.R
  - tests/testthat/test-workflow.R
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - Leontief extraction exposes stable list, long, and wide formats from a `sube_results` object.
    - Comparison preparation aligns Leontief and model outputs into a predictable tidy table across measure, variable, and aggregation choices.
  artifacts:
    - R/paper_tools.R
    - tests/testthat/test-workflow.R
  key_links:
    - Workflow tests assert exact comparison-layer column contracts and format variants.
    - Shared validation helpers in R/utils.R back public comparison entry points where needed.
---

<objective>
Stabilize the extraction and comparison-table contracts that sit between Phase 1 compute outputs and all downstream comparison/reporting helpers.

Purpose: Make the data structures coming out of the comparison layer explicit and dependable so plotting, export, and user code can rely on them.
Output: Hardened extraction/comparison helpers plus targeted tests that pin their public shapes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-comparison-layer-stabilization/02-CONTEXT.md
@.planning/phases/02-comparison-layer-stabilization/02-RESEARCH.md
@R/paper_tools.R
@R/utils.R
@tests/testthat/test-workflow.R
@vignettes/modeling-and-outputs.Rmd
</context>

<threat_model>
- T-2-01: Ambiguous extraction or comparison-table shapes could produce plausible but misaligned research outputs and plots.
- Mitigation: Normalize output structures explicitly, keep selection logic deterministic, and add tests that assert the exact column sets and nested-object shapes users depend on.
- Block on: medium severity or higher issues that allow silent comparison misalignment.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Pin Leontief extraction formats</name>
  <files>R/paper_tools.R, tests/testthat/test-workflow.R</files>
  <read_first>R/paper_tools.R, tests/testthat/test-workflow.R, man/paper_tools.Rd</read_first>
  <action>Audit `extract_leontief_matrices()` for the public `list`, `long`, and `wide` formats. Ensure each format preserves `COUNTRY`, `YEAR`, matrix identity, and stable row or dimension labeling in a way downstream code can consume without inspecting internals. If needed, tighten validation or normalization around the accepted `matrix` and `format` values, but keep the exported signature unchanged. Extend `tests/testthat/test-workflow.R` so the workflow asserts not only the long format but also list and wide outputs, including representative column or component expectations for each format.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-workflow.R` asserts `extract_leontief_matrices(..., format = "list")`
    - `tests/testthat/test-workflow.R` asserts `extract_leontief_matrices(..., format = "wide")`
    - At least one test checks the metadata columns or names that distinguish extraction formats
  </acceptance_criteria>
  <done>Leontief extraction formats are explicit, stable, and covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize comparison-table preparation</name>
  <files>R/paper_tools.R, R/utils.R, tests/testthat/test-workflow.R</files>
  <read_first>R/paper_tools.R, R/utils.R, tests/testthat/test-workflow.R, README.md, vignettes/getting-started.Rmd</read_first>
  <action>Review `prepare_sube_comparison()` and its helper paths for selection and shaping drift. Ensure measure matching, variable normalization, year aggregation, type labeling, and paper-filter application produce a stable tidy table with predictable columns. If edge handling is currently implicit, make it explicit in code or validation. Add or refine workflow tests so comparison output is asserted under at least one aggregated and one non-aggregated path, and so the expected type set and key columns remain pinned.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-workflow.R` covers `aggregate_years = TRUE` and at least one `aggregate_years = FALSE` comparison path
    - Comparison tests assert the presence of `COUNTRY`, `CPAagg`, `variable`, `measure`, `type`, and `value`
    - At least one assertion checks for the expected comparison type labels across Leontief and model outputs
  </acceptance_criteria>
  <done>The comparison table contract is explicit enough for plotting and export helpers to rely on directly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'` succeeds
- [ ] Tests cover all advertised extraction formats and at least two comparison-shaping branches
- [ ] No exported function names or comparison-layer object classes changed unexpectedly
</verification>

<success_criteria>
- All tasks completed
- Leontief extraction formats are documented by tests and remain stable
- Comparison-table preparation yields predictable tidy output across the supported selection and aggregation paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-comparison-layer-stabilization/02-01-SUMMARY.md`
</output>
