---
phase: 02-comparison-layer-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/paper_tools.R
  - R/filter_plot_export.R
  - tests/testthat/test-workflow.R
autonomous: true
requirements:
  - COMP-03
  - COMP-04
user_setup: []
must_haves:
  truths:
    - Paper-style and generic plotting helpers return predictable `ggplot` or named-list structures for supported inputs.
    - Export helpers produce stable single-file or directory outputs across CSV, RDS, and DTA formats.
  artifacts:
    - R/paper_tools.R
    - R/filter_plot_export.R
    - tests/testthat/test-workflow.R
  key_links:
    - Plot and export tests cover both happy-path behavior and at least one constrained selection branch.
    - Export path semantics match the package examples and do not require user glue code.
---

<objective>
Harden the plotting and export workflows that turn comparison-ready tables and model outputs into usable research artifacts.

Purpose: Ensure the package can produce comparison plots and persisted outputs predictably, without users needing paper-specific post-processing scripts.
Output: Stabilized plotting/export helpers plus tests that pin returned object and path behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-comparison-layer-stabilization/02-CONTEXT.md
@.planning/phases/02-comparison-layer-stabilization/02-RESEARCH.md
@R/paper_tools.R
@R/filter_plot_export.R
@tests/testthat/test-workflow.R
@vignettes/modeling-and-outputs.Rmd
</context>

<threat_model>
- T-2-02: Weak plot/export contracts could let users believe outputs were generated correctly when list structure, file naming, or format behavior actually drifted.
- Mitigation: Assert returned plot/list classes, handle narrow selections deliberately, and verify export paths and files for each supported output mode.
- Block on: medium severity or higher issues that produce missing, misnamed, or silently partial output artifacts.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Pin plotting helper return structures</name>
  <files>R/paper_tools.R, R/filter_plot_export.R, tests/testthat/test-workflow.R</files>
  <read_first>R/paper_tools.R, R/filter_plot_export.R, tests/testthat/test-workflow.R, vignettes/modeling-and-outputs.Rmd</read_first>
  <action>Audit `plot_sube()`, `plot_paper_comparison()`, `plot_paper_regression()`, and `plot_paper_interval_ranges()` for return-shape consistency. Ensure supported inputs always yield a `ggplot` or a named list of `ggplot` objects in the structure implied by the current examples, and that empty or narrow subsets are handled intentionally rather than by accident. Extend workflow tests with assertions that pin representative nested-list names, non-null plot objects, and at least one selection branch beyond the current single-variable smoke path.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-workflow.R` asserts a `ggplot` result for `plot_sube()`
    - `tests/testthat/test-workflow.R` asserts the named-list structure returned by at least one paper-style plotting helper
    - At least one plotting test exercises a branch other than the current `by_country` single-variable smoke case
  </acceptance_criteria>
  <done>Plot helpers return stable structures that match their public examples.</done>
</task>

<task type="auto">
  <name>Task 2: Verify export semantics across supported formats</name>
  <files>R/filter_plot_export.R, tests/testthat/test-workflow.R</files>
  <read_first>R/filter_plot_export.R, tests/testthat/test-workflow.R, man/filter_plot_write.Rd</read_first>
  <action>Review `write_sube()` for the supported CSV, RDS, and DTA modes and for both single-table and named-list inputs. Keep the exported interface unchanged, but ensure path normalization, extension handling, directory creation, and per-table filenames are predictable. Add workflow tests that cover at least one single-file export and one named-list export path, checking file existence and directory structure without depending on external state.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `tests/testthat/test-workflow.R` contains a named-list export assertion in addition to a single-table export assertion
    - At least one test covers non-CSV output among `rds` or `dta`
    - Export tests verify the normalized path or generated filenames rather than only checking that no error was raised
  </acceptance_criteria>
  <done>Export behavior is pinned across supported formats and object shapes.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'` succeeds
- [ ] Plot tests assert returned object structure, not just non-error execution
- [ ] Export tests verify both single-file and directory-output semantics
</verification>

<success_criteria>
- All tasks completed
- Plotting helpers return stable objects for supported comparison workflows
- Export helpers produce predictable files or directories across the supported formats
</success_criteria>

<output>
After completion, create `.planning/phases/02-comparison-layer-stabilization/02-02-SUMMARY.md`
</output>
