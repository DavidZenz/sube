---
phase: 02-comparison-layer-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - tests/testthat/test-workflow.R
  - vignettes/modeling-and-outputs.Rmd
  - README.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
user_setup: []
must_haves:
  truths:
    - The public comparison workflow is reproducible from shipped example data and documented without extra glue code.
    - Tests and examples agree on the comparison-layer contract after stabilization work lands.
  artifacts:
    - tests/testthat/test-workflow.R
    - vignettes/modeling-and-outputs.Rmd
    - README.md
  key_links:
    - Automated tests pin the same comparison story the docs describe.
    - Public examples show the intended path from `compute_sube()` and `estimate_elasticities()` into plots and exports.
---

<objective>
Close the loop on Phase 2 by aligning tests and user-facing examples with the stabilized comparison-layer contract.

Purpose: Prove the comparison workflow can be reproduced from a clean checkout using package objects alone.
Output: Updated workflow tests and examples that demonstrate extraction, comparison, plotting, and export together.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-comparison-layer-stabilization/02-CONTEXT.md
@.planning/phases/02-comparison-layer-stabilization/02-RESEARCH.md
@tests/testthat/test-workflow.R
@vignettes/modeling-and-outputs.Rmd
@README.md
@vignettes/getting-started.Rmd
</context>

<threat_model>
- T-2-03: If tests and examples diverge, users may follow a documented comparison workflow that no longer matches the actual package contract.
- Mitigation: Update examples and tests together, using shipped sample data and asserting the same public return shapes and export semantics.
- Block on: medium severity or higher mismatches between tests and published examples.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Extend reproducible comparison workflow verification</name>
  <files>tests/testthat/test-workflow.R</files>
  <read_first>tests/testthat/test-workflow.R, R/paper_tools.R, R/filter_plot_export.R</read_first>
  <action>Refine `tests/testthat/test-workflow.R` so the comparison layer is exercised as a coherent workflow rather than isolated smoke calls. Build on the sample-data path from `compute_sube()` and `estimate_elasticities()` to cover extraction, comparison preparation, at least one paper-style plotting helper, and at least one export branch in the same conceptual flow. Keep the test file focused and fast enough for the phase validation cadence.</action>
  <verify>R -q -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - The workflow test file covers extraction, comparison preparation, plotting, and export in a linked sample-data path
    - The full `tests/testthat` suite remains green
    - New assertions focus on public behavior rather than internal implementation details
  </acceptance_criteria>
  <done>The comparison workflow is verified end-to-end from package objects and shipped data.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh modeling/output examples to match the stabilized contract</name>
  <files>vignettes/modeling-and-outputs.Rmd, README.md</files>
  <read_first>vignettes/modeling-and-outputs.Rmd, README.md, tests/testthat/test-workflow.R</read_first>
  <action>Update the modeling/output vignette and any compact README comparison example only where needed to reflect the final stabilized comparison contract. Keep the phase bounded: focus on examples for extraction formats, comparison preparation, representative plot return values, and export path semantics. Avoid broad narrative rewrites that belong to Phase 3.</action>
  <verify>R -q -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - `vignettes/modeling-and-outputs.Rmd` shows the intended comparison workflow from sample package objects
    - If README snippets are touched, they stay compact and consistent with the vignette
    - Example code does not require paper-only scripts or external data downloads
  </acceptance_criteria>
  <done>The documented comparison workflow matches the tested package contract.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_dir("tests/testthat")'` succeeds
- [ ] Tests and examples describe the same comparison-layer flow and return shapes
- [ ] Documentation changes stay scoped to Phase 2 comparison usage rather than a general docs rewrite
</verification>

<success_criteria>
- All tasks completed
- The comparison layer is reproducible from shipped data through tests and examples
- Public examples no longer rely on implicit knowledge about plot/list/export behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-comparison-layer-stabilization/02-03-SUMMARY.md`
</output>
