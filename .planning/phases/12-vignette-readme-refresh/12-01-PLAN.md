---
phase: 12-vignette-readme-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vignettes/getting-started.Rmd
  - vignettes/package-design.Rmd
  - vignettes/data-preparation.Rmd
  - vignettes/modeling-and-outputs.Rmd
  - vignettes/paper-replication.Rmd
  - vignettes/figaro-workflow.Rmd
  - vignettes/pipeline-helpers.Rmd
  - README.md
autonomous: true
requirements: [VIG-01, VIG-02, VIG-03, DOC-01]

must_haves:
  truths:
    - "User reading any vignette sees WIOD and FIGARO described as example data sources, not as the only data source"
    - "User reading data-preparation.Rmd finds Phase 11 spec sections connected to the original workflow sections via transition sentences"
    - "User reading vignettes in sequence finds cross-references guiding them through the canonical reading order"
    - "User reading README finds a source-agnostic intro and a pointer to the data-preparation vignette for custom data"
  artifacts:
    - path: "vignettes/getting-started.Rmd"
      provides: "Source-agnostic framing sentence and forward cross-reference"
      contains: "canonical"
    - path: "vignettes/package-design.Rmd"
      provides: "Source-agnostic framing sentence"
      contains: "canonical"
    - path: "vignettes/data-preparation.Rmd"
      provides: "Transition sentences connecting spec sections to workflow sections"
      contains: "canonical format described above"
    - path: "vignettes/modeling-and-outputs.Rmd"
      provides: "Source-agnostic framing sentence"
      contains: "canonical"
    - path: "vignettes/paper-replication.Rmd"
      provides: "Source-agnostic framing note about pipeline generality"
      contains: "source-agnostic"
    - path: "vignettes/figaro-workflow.Rmd"
      provides: "Note about downstream pipeline generality"
      contains: "any SUT source"
    - path: "vignettes/pipeline-helpers.Rmd"
      provides: "Source-agnostic framing sentence"
      contains: "canonical"
    - path: "README.md"
      provides: "Source-agnostic intro, expanded import bullet, BYOD pointer, full vignette list"
      contains: "any SUT data in the canonical format"
  key_links:
    - from: "vignettes/getting-started.Rmd"
      to: "vignettes/data-preparation.Rmd"
      via: "cross-reference link"
      pattern: "data-preparation\\.html"
    - from: "README.md"
      to: "vignettes/data-preparation.Rmd"
      via: "BYOD pointer"
      pattern: "data-preparation"
---

<objective>
Add source-agnostic framing to all 7 vignettes and README, integrate Phase 11's format spec into the data-preparation narrative with transitions, and establish cross-references for coherent reading order.

Purpose: Ensure no vignette or README text implies WIOD or FIGARO is "the" data source, and that readers can navigate vignettes in a logical sequence.
Output: All 8 documentation files updated with source-agnostic prose, transitions, and cross-references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-vignette-readme-refresh/12-CONTEXT.md
@.planning/phases/12-vignette-readme-refresh/12-RESEARCH.md
@.planning/phases/11-data-format-specification/11-01-SUMMARY.md

<interfaces>
<!-- Cross-reference link format (established pattern from figaro-workflow.Rmd): -->
[Pipeline Helpers vignette](pipeline-helpers.html)
[run_sube_pipeline()](../reference/run_sube_pipeline.html)

<!-- Canonical reading order (D-04): -->
1. getting-started
2. package-design
3. data-preparation
4. modeling-and-outputs
5. paper-replication
6. figaro-workflow
7. pipeline-helpers

<!-- Gated vignettes (eval=FALSE, DO NOT change): paper-replication.Rmd, figaro-workflow.Rmd -->
<!-- Non-gated vignettes (eval=TRUE, use sube_example_data()): all others -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source-agnostic framing and cross-references to 6 vignettes</name>
  <files>vignettes/getting-started.Rmd, vignettes/package-design.Rmd, vignettes/modeling-and-outputs.Rmd, vignettes/paper-replication.Rmd, vignettes/figaro-workflow.Rmd, vignettes/pipeline-helpers.Rmd</files>
  <read_first>vignettes/getting-started.Rmd, vignettes/package-design.Rmd, vignettes/modeling-and-outputs.Rmd, vignettes/paper-replication.Rmd, vignettes/figaro-workflow.Rmd, vignettes/pipeline-helpers.Rmd</read_first>
  <action>
Add a source-agnostic framing sentence and cross-references to each of 6 vignettes. All edits are prose-only -- do not modify any R code blocks, setup chunks, or eval settings.

**Per D-01: Non-gated vignettes (getting-started, package-design, modeling-and-outputs, pipeline-helpers)**

Insert one framing sentence in the opening paragraph (before the first `##` heading). Use wording along the lines of: "The examples below use shipped sample data, but the package works with any supply-use data in the canonical long format -- see the [data-preparation vignette](data-preparation.html) for format details."

Specific insertion points:
- `getting-started.Rmd`: After the companion paper citation block (around line 21), before `## Load sample inputs`. Per D-05, this intro layer can also note that practitioners may skip ahead to data-preparation.
- `package-design.Rmd`: After the opening paragraph (around line 13-14), before `## Why this package exists`.
- `modeling-and-outputs.Rmd`: In the intro paragraph (around lines 14-20), before `## Compute the sample workflow`.
- `pipeline-helpers.Rmd`: In Section 1 "When to reach for the convenience helpers" (around lines 20-33). Per D-01, note that helpers work with any SUT source, not just WIOD and FIGARO.

**Per D-03: paper-replication.Rmd (gated, eval=FALSE)**

Insert one framing note in Section 1 "What this vignette replicates" (around lines 22-31). Use wording: "This vignette is specific to WIOD data. The pipeline from `build_matrices()` onward is identical for any SUT source in canonical format." Do NOT touch the setup chunk or change eval settings.

**Per D-02: figaro-workflow.Rmd (gated, eval=FALSE)**

Insert one note in Section 1 "What FIGARO is and what this vignette covers" (around lines 21-37). Use wording: "This vignette covers FIGARO-specific import. Once data is in canonical `sube_suts` format, the downstream pipeline -- `build_matrices()`, `compute_sube()`, and `estimate_elasticities()` -- works the same for any SUT source." Do NOT touch the setup chunk or change eval settings.

**Per D-04/VIG-03: Cross-references for narrative flow**

Add cross-reference links where they aid reading flow. Use the established `[title](slug.html)` format:
- In `getting-started.Rmd`: Forward link to `[data-preparation vignette](data-preparation.html)` in the framing sentence (already included above).
- In `modeling-and-outputs.Rmd`: After framing sentence, optionally mention that data preparation is covered in the [data-preparation vignette](data-preparation.html).
- In `pipeline-helpers.Rmd`: The existing text likely references run_sube_pipeline already; ensure there is a forward note that the manual workflow steps are covered in the [modeling-and-outputs vignette](modeling-and-outputs.html).
- In `paper-replication.Rmd`: Existing cross-ref to run_sube_pipeline is fine; no additional needed.
- In `figaro-workflow.Rmd`: Existing cross-ref to pipeline-helpers is fine; no additional needed.

**Per D-07: No file renaming.** Current titles are descriptive enough.
  </action>
  <verify>
    <automated>cd /home/zenz/R/sube && grep -l "canonical" vignettes/getting-started.Rmd vignettes/package-design.Rmd vignettes/modeling-and-outputs.Rmd vignettes/pipeline-helpers.Rmd && grep -l "any SUT source" vignettes/paper-replication.Rmd vignettes/figaro-workflow.Rmd && grep -c "eval = FALSE" vignettes/paper-replication.Rmd vignettes/figaro-workflow.Rmd</automated>
  </verify>
  <acceptance_criteria>
    - getting-started.Rmd contains the string "canonical" in the intro paragraph before `## Load sample inputs`
    - package-design.Rmd contains the string "canonical" in text before `## Why this package exists`
    - modeling-and-outputs.Rmd contains the string "canonical" in text before `## Compute the sample workflow`
    - pipeline-helpers.Rmd contains the string "canonical" in the opening section
    - paper-replication.Rmd contains the string "any SUT source" in Section 1
    - figaro-workflow.Rmd contains the string "any SUT source" in Section 1
    - paper-replication.Rmd still contains `eval = FALSE` in setup chunk (unchanged)
    - figaro-workflow.Rmd still contains `eval = FALSE` in setup chunk (unchanged)
    - getting-started.Rmd contains the cross-reference link `data-preparation.html`
    - No R code blocks were modified in any of the 6 files
  </acceptance_criteria>
  <done>All 6 vignettes have source-agnostic framing sentences and cross-references; gated vignettes retain eval=FALSE; no code blocks changed</done>
</task>

<task type="auto">
  <name>Task 2: Polish data-preparation.Rmd integration and verify eval status</name>
  <files>vignettes/data-preparation.Rmd</files>
  <read_first>vignettes/data-preparation.Rmd, .planning/phases/11-data-format-specification/11-01-SUMMARY.md</read_first>
  <action>
Three edits to data-preparation.Rmd, all prose-only. Do not rewrite Phase 11's spec content (D-12).

**1. Source-agnostic framing sentence (D-01/D-12)**

In the opening paragraph (lines 14-25, before `## Canonical SUT Format`), add one sentence noting that the format described here works with any supply-use data source, not just WIOD or FIGARO. Insert after the sentence about four input families (around line 22). Example: "Whether your data comes from WIOD, FIGARO, or another national statistical office, the same canonical format applies."

**2. Transition sentences connecting spec sections to workflow sections (D-13)**

Add a 1-sentence opener to each of the four original sections that echoes back to the canonical spec above:

- Before the first paragraph of `## Supply-use data`: Add something like "The `sut_data` example below shows the 7-column canonical format described above in practice."
- Before the first paragraph of `## Mapping tables`: Add something like "Mapping tables bridge the canonical SUT columns to the matrix dimensions needed by `build_matrices()`."
- Before the first paragraph of `## Input metrics`: Add something like "The `inputs` example provides the satellite vectors described in the Satellite Vector Inputs section above."
- Before the first paragraph of `## Modeling table`: Add something like "The modeling table combines matrix outputs with satellite-derived multipliers for econometric estimation."

Also check the existing transition at the end of `## Bring Your Own Data` (around line 107: "The remaining sections walk through the shipped example objects in detail..."). If it adequately bridges to `## Supply-use data`, leave it. If it could be strengthened, add one sentence.

**3. Eval audit (D-14)**

Scan every code chunk in the file. Any chunk that uses `sube_example_data()` must have `eval=TRUE` (or no eval option, since the default is TRUE given no global override). If any such chunk has `eval=FALSE`, remove the `eval=FALSE` option. Based on the research audit, all chunks should already be eval=TRUE, but verify explicitly.
  </action>
  <verify>
    <automated>cd /home/zenz/R/sube && grep -c "canonical" vignettes/data-preparation.Rmd && grep "eval.*FALSE" vignettes/data-preparation.Rmd || echo "No eval=FALSE found (correct)" && R -q -e 'knitr::knit("vignettes/data-preparation.Rmd", output = tempfile())' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - data-preparation.Rmd opening paragraph (before first `##`) contains a source-agnostic sentence with the word "canonical"
    - `## Supply-use data` section has a transition sentence containing "canonical format" within the first 3 lines after the heading
    - `## Mapping tables` section has a transition sentence referencing the spec within the first 3 lines after the heading
    - `## Input metrics` section has a transition sentence referencing "satellite" or "Satellite Vector" within the first 3 lines after the heading
    - `## Modeling table` section has a transition sentence within the first 3 lines after the heading
    - No code chunk using `sube_example_data()` has `eval=FALSE` or `eval = FALSE`
    - Vignette renders without error via knitr::knit
  </acceptance_criteria>
  <done>data-preparation.Rmd has source-agnostic framing, transition sentences connecting all spec sections to workflow sections, and all sube_example_data() chunks confirmed eval=TRUE</done>
</task>

<task type="auto">
  <name>Task 3: Refresh README with source-agnostic framing and updated vignette list</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
Four targeted prose edits to README.md. No code changes (D-11 -- keep existing code example as-is).

**1. Source-agnostic sentence in intro (D-08)**

After the companion paper citation block (after line 17, before `## Installation`), add one sentence: "The package works with any supply-use data in the canonical long format, including WIOD, FIGARO, and custom national accounts."

**2. Expand first bullet in "What the package does" (D-09)**

Current line 31: `- imports and standardizes supply-use inputs`

Replace with: `- imports and standardizes supply-use inputs (WIOD workbooks, FIGARO CSVs, or custom supply-use tables in the canonical format)`

**3. BYOD pointer (D-10)**

After the last bullet in "What the package does" (after line 36), add one sentence: "For reshaping custom supply-use data into the expected format, see `vignette("data-preparation", package = "sube")`."

**4. Update Documentation section to list all 7 vignettes (discretion -- housekeeping)**

Replace the current Documentation section (lines 103-113) with an updated list of all 7 vignettes in the canonical reading order (D-04):

```
## Documentation

- `vignette("getting-started", package = "sube")` for the end-to-end sample
  workflow
- `vignette("package-design", package = "sube")` for how the package workflow
  relates to the companion paper
- `vignette("data-preparation", package = "sube")` for input contracts and the
  canonical data format
- `vignette("modeling-and-outputs", package = "sube")` for modeling and plot
  outputs
- `vignette("paper-replication", package = "sube")` for reproducing the
  companion paper results (requires WIOD data)
- `vignette("figaro-workflow", package = "sube")` for working with Eurostat
  FIGARO tables (requires FIGARO data)
- `vignette("pipeline-helpers", package = "sube")` for convenience wrappers
  that run the full pipeline in one call

The full reference and articles are designed to be published with `pkgdown`,
with the website acting as the main public documentation surface.
```
  </action>
  <verify>
    <automated>cd /home/zenz/R/sube && grep "any supply-use data in the canonical" README.md && grep "WIOD workbooks, FIGARO CSVs" README.md && grep "data-preparation" README.md && grep -c 'vignette("' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the string "any supply-use data in the canonical" in the intro section (before `## Installation`)
    - README.md first bullet in "What the package does" contains "WIOD workbooks, FIGARO CSVs"
    - README.md contains a BYOD pointer sentence with `vignette("data-preparation"`
    - README.md Documentation section lists 7 vignettes (grep for `vignette("` returns 7 matches in that section)
    - README.md Documentation section lists vignettes in canonical order: getting-started, package-design, data-preparation, modeling-and-outputs, paper-replication, figaro-workflow, pipeline-helpers
    - Existing code example block is unchanged
  </acceptance_criteria>
  <done>README has source-agnostic intro sentence, expanded import bullet naming WIOD/FIGARO/custom, BYOD pointer to data-preparation vignette, and complete 7-vignette Documentation section in canonical order</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries applicable. This phase edits only local Rmd and Markdown documentation files with no authentication, data handling, external calls, or user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | vignette eval settings | accept | Prose-only edits; acceptance criteria verify eval=FALSE unchanged in gated vignettes |
</threat_model>

<verification>
After all 3 tasks complete:

1. `R CMD build .` succeeds (vignettes render without error)
2. `R CMD check sube_*.tar.gz --no-manual` passes with no new errors or warnings
3. All 7 vignettes contain a source-agnostic framing sentence
4. data-preparation.Rmd has transition sentences connecting spec sections to workflow sections
5. README.md has source-agnostic intro, expanded bullet, BYOD pointer, and full vignette list
</verification>

<success_criteria>
- All 7 vignettes have source-agnostic framing (VIG-01 complete)
- data-preparation.Rmd has transitions and eval=TRUE confirmed (VIG-02 complete)
- Cross-references establish coherent reading flow (VIG-03 complete)
- README is refreshed with source-agnostic framing and full vignette list (DOC-01 complete)
- R CMD build succeeds; no gated vignette eval settings were changed
</success_criteria>

<output>
After completion, create `.planning/phases/12-vignette-readme-refresh/12-01-SUMMARY.md`
</output>
