---
phase: 01-core-workflow-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/compute.R
  - R/utils.R
  - tests/testthat/test-workflow.R
autonomous: true
requirements:
  - WF-03
user_setup: []
must_haves:
  truths:
    - `compute_sube()` distinguishes contract failures from recoverable numeric diagnostics in a stable, testable way.
    - Diagnostic statuses remain observable in `result$diagnostics$status` for singular numeric branches.
  artifacts:
    - R/compute.R
    - R/utils.R
    - tests/testthat/test-workflow.R
  key_links:
    - Workflow tests exercise both successful compute paths and at least one explicit failure or diagnostic path.
    - Utility helpers support deterministic compute failure handling.
---

<objective>
Harden the compute and diagnostics contract for the core SUBE workflow.

Purpose: Downstream comparison and documentation work depends on compute failures being understandable and reproducible, not implicit.
Output: Updated compute/utility logic and workflow tests that pin diagnostic versus error behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-workflow-contracts/01-CONTEXT.md
@.planning/phases/01-core-workflow-contracts/01-RESEARCH.md
@R/compute.R
@R/utils.R
@tests/testthat/test-workflow.R
</context>

<threat_model>
- T-1-02: Ambiguous numeric-failure handling can let users misread invalid or singular computations as trustworthy research results.
- Mitigation: Preserve explicit stops for broken input contracts, preserve explicit diagnostic status codes for recoverable singular branches, and assert both in tests.
- Block on: medium severity issues that can blur the line between invalid input and degraded computation.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Audit compute input alignment and metric validation</name>
  <files>R/compute.R, tests/testthat/test-workflow.R</files>
  <read_first>R/compute.R, tests/testthat/test-workflow.R, R/matrices.R, inst/extdata/sample/inputs.csv</read_first>
  <action>Review `compute_sube()` for every hard-stop contract: required input columns, presence of an industry identifier, missing metrics, and industry alignment against matrix industries. Keep the current public call signature intact, but make the stop messages and branch conditions explicit enough for tests. Extend `tests/testthat/test-workflow.R` with focused failure-path expectations for at least one missing-metric or misaligned-input scenario using tiny in-test data derived from shipped example data.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `R/compute.R` contains `stop("\`inputs\` must include an industry identifier column.", call. = FALSE)`
    - `R/compute.R` contains `stop(sprintf("Missing input metrics: %s", paste(missing_metrics, collapse = ", ")), call. = FALSE)`
    - `tests/testthat/test-workflow.R` contains at least one `expect_error(` case for compute input validation
  </acceptance_criteria>
  <done>Compute input-contract failures are explicit and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Pin diagnostic-status behavior for singular compute branches</name>
  <files>R/compute.R, R/utils.R, tests/testthat/test-workflow.R</files>
  <read_first>R/compute.R, R/utils.R, tests/testthat/test-workflow.R</read_first>
  <action>Audit the singular branches driven by `.safe_solve()` in `compute_sube()`. Preserve or refine the statuses `singular_supply`, `singular_go`, `singular_leontief`, and `ok` so they remain stable, observable outputs in `result$diagnostics$status`. Add tests that create or simulate at least one singular branch without external data, and assert the exact status string in the diagnostics table rather than only checking that a result exists.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `R/compute.R` contains `status = "singular_supply"`
    - `R/compute.R` contains `status = "singular_go"`
    - `R/compute.R` contains `status = "singular_leontief"`
    - `tests/testthat/test-workflow.R` contains a diagnostics assertion using one of those exact status strings
  </acceptance_criteria>
  <done>Recoverable singular compute states are exposed as stable diagnostics and tested explicitly.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'` succeeds
- [ ] Workflow tests assert at least one hard-stop compute contract and one recoverable diagnostics branch
- [ ] `compute_sube()` still returns `sube_results` with `status == "ok"` for shipped example data
</verification>

<success_criteria>
- All tasks completed
- Compute failure modes are divided cleanly into explicit contract errors and diagnostic status outputs
- Sample-data compute workflow still passes with `status == "ok"`
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-workflow-contracts/01-02-SUMMARY.md`
</output>
