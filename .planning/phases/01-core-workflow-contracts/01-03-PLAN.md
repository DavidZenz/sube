---
phase: 01-core-workflow-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - vignettes/getting-started.Rmd
  - tests/testthat/test-workflow.R
  - tests/testthat.R
autonomous: true
requirements:
  - WF-01
  - WF-02
user_setup: []
must_haves:
  truths:
    - A clean checkout can verify the documented sample workflow entirely from shipped example data.
    - Tests and the getting-started vignette describe the same import -> matrix -> compute path.
  artifacts:
    - vignettes/getting-started.Rmd
    - tests/testthat/test-workflow.R
    - tests/testthat.R
  key_links:
    - The vignette workflow matches the tested sample-data path.
    - The test runner remains able to execute the strengthened workflow coverage.
---

<objective>
Align reproducibility verification around the shipped example-data workflow.

Purpose: Phase 1 is only complete if users can follow the documented sample path and maintainers can verify it from the repo without relying on unstated setup.
Output: Updated vignette/test alignment and a stronger automated baseline for the documented workflow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-workflow-contracts/01-CONTEXT.md
@.planning/phases/01-core-workflow-contracts/01-RESEARCH.md
@.planning/phases/01-core-workflow-contracts/01-01-SUMMARY.md
@.planning/phases/01-core-workflow-contracts/01-02-SUMMARY.md
@vignettes/getting-started.Rmd
@tests/testthat/test-workflow.R
@tests/testthat.R
</context>

<threat_model>
- T-1-03: Documentation and automated verification can drift, causing users to trust a sample workflow that no longer matches the package contract.
- Mitigation: Make the vignette and workflow tests assert the same example-data sequence and keep the test runner path intact.
- Block on: medium severity drift where docs describe behavior not covered by automated verification.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Align the getting-started vignette with the tested workflow contract</name>
  <files>vignettes/getting-started.Rmd, tests/testthat/test-workflow.R</files>
  <read_first>vignettes/getting-started.Rmd, tests/testthat/test-workflow.R, README.md, R/import.R, R/matrices.R, R/compute.R</read_first>
  <action>Review the sample-data narrative in `vignettes/getting-started.Rmd` against the strengthened tests from Plans 01 and 02. Update the vignette so the documented sequence explicitly uses shipped example data, `extract_domestic_block()`, `build_matrices()`, and `compute_sube()` in the same order and with the same object expectations covered by tests. If the vignette currently omits diagnostics or object-shape expectations that are now contractually important, add concise explanatory text without broadening into Phase 3 documentation work.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `vignettes/getting-started.Rmd` contains `sut <- sube_example_data("sut_data")`
    - `vignettes/getting-started.Rmd` contains `bundle <- build_matrices(`
    - `vignettes/getting-started.Rmd` contains `result <- compute_sube(`
    - `tests/testthat/test-workflow.R` still contains a sample workflow assertion for `sube_results`
  </acceptance_criteria>
  <done>The vignette and tests describe the same sample-data workflow contract.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the executable verification baseline for Phase 1</name>
  <files>tests/testthat/test-workflow.R, tests/testthat.R</files>
  <read_first>tests/testthat/test-workflow.R, tests/testthat.R, DESCRIPTION, .planning/phases/01-core-workflow-contracts/01-VALIDATION.md</read_first>
  <action>Keep `tests/testthat.R` as the stable entry point and expand `tests/testthat/test-workflow.R` so it serves as the canonical Phase 1 smoke/integration suite. Ensure the file covers the shipped example-data path from load/import through compute, plus the targeted contract and diagnostics assertions added in Plans 01 and 02. Do not introduce new test frameworks or non-package verification scripts in this phase.</action>
  <verify>R -q -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - `tests/testthat.R` contains `test_check("sube")`
    - `tests/testthat/test-workflow.R` contains tests for import, matrix, and compute workflow behavior
    - `tests/testthat/test-workflow.R` contains at least five `test_that(` blocks after the update
  </acceptance_criteria>
  <done>The repository has a clear automated baseline for the documented example-data workflow.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_dir("tests/testthat")'` succeeds
- [ ] The getting-started vignette still reflects the sample-data workflow implemented by the package
- [ ] Workflow tests cover shipped example data plus hardened contract/diagnostic branches from Plans 01 and 02
</verification>

<success_criteria>
- All tasks completed
- Maintainers can verify the documented core workflow from the repo using shipped examples and `testthat`
- The sample-data vignette and tests no longer drift on core import -> matrix -> compute expectations
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-workflow-contracts/01-03-SUMMARY.md`
</output>
