---
phase: 01-core-workflow-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/import.R
  - R/matrices.R
  - R/utils.R
  - tests/testthat/test-workflow.R
autonomous: true
requirements:
  - WF-02
user_setup: []
must_haves:
  truths:
    - Import paths that are missing or structurally invalid fail with explicit, testable errors.
    - Matrix construction accepts the documented SUT inputs and produces stable `sube_matrices` objects from shipped example data.
  artifacts:
    - R/import.R
    - R/matrices.R
    - tests/testthat/test-workflow.R
  key_links:
    - Workflow tests assert the documented contract of import and matrix functions.
    - Shared validation helpers in R/utils.R back the public import and matrix entry points.
---

<objective>
Harden the import and matrix-construction contracts used by the sample workflow.

Purpose: Make the earliest part of the package pipeline explicit and reliable so downstream compute and comparison work does not depend on ambiguous input behavior.
Output: Updated import/matrix validations plus workflow tests that pin the expected contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-workflow-contracts/01-CONTEXT.md
@.planning/phases/01-core-workflow-contracts/01-RESEARCH.md
@R/import.R
@R/matrices.R
@R/utils.R
@tests/testthat/test-workflow.R
</context>

<threat_model>
- T-1-01: Weak input validation could allow malformed SUT or mapping data to propagate into later computations, producing plausible-but-wrong research outputs.
- Mitigation: Fail fast on missing structural columns, unsupported file layouts, and mapping shapes; add tests that assert the exact error branches.
- Block on: medium severity or higher issues that allow silent bad-data propagation.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Audit and tighten import contract boundaries</name>
  <files>R/import.R, R/utils.R, tests/testthat/test-workflow.R</files>
  <read_first>R/import.R, R/utils.R, tests/testthat/test-workflow.R, README.md, vignettes/getting-started.Rmd</read_first>
  <action>Review `import_suts()`, `extract_domestic_block()`, and `sube_example_data()` against the documented workflow. Add or refine validation so the package explicitly rejects nonexistent paths, directories with no supported files, CSV inputs missing any of `REP, PAR, CPA, VAR, VALUE, YEAR, TYPE`, and workbook inputs missing required identifier columns or expected sheets. Keep exported function names and returned classes unchanged. Extend `tests/testthat/test-workflow.R` with focused expectations for the chosen error branches, using `expect_error(..., fixed = TRUE)` with exact error fragments already emitted or deliberately standardized in the implementation.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `R/import.R` contains `stop("\`path\` does not exist.", call. = FALSE)`
    - `R/import.R` contains `stop("No supported input files were found.", call. = FALSE)`
    - `tests/testthat/test-workflow.R` contains at least one `expect_error(` case for malformed import input
  </acceptance_criteria>
  <done>Import entry points fail explicitly for malformed inputs and tests assert those behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Pin matrix-construction input and output expectations</name>
  <files>R/matrices.R, R/utils.R, tests/testthat/test-workflow.R</files>
  <read_first>R/matrices.R, R/utils.R, tests/testthat/test-workflow.R, inst/extdata/sample/cpa_map.csv, inst/extdata/sample/ind_map.csv</read_first>
  <action>Audit `build_matrices()` and `.coerce_map()` for ambiguous mapping behavior. Ensure the implementation clearly enforces the required SUT columns, requires two-column mapping tables, normalizes final-demand handling through `final_demand_var <- toupper(final_demand_var)`, and returns stable `aggregated`, `final_demand`, and named `matrices` components. Add or update workflow tests so shipped example data proves the object class is `sube_matrices`, the component names are present, and malformed mapping inputs fail with an exact, testable message instead of partial coercion.</action>
  <verify>R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'</verify>
  <acceptance_criteria>
    - `R/matrices.R` contains `.sube_required_columns(sut_data, c("REP", "PAR", "CPA", "VAR", "VALUE", "YEAR", "TYPE"))`
    - `R/utils.R` contains `stop("Mapping tables must have at least two columns.", call. = FALSE)`
    - `tests/testthat/test-workflow.R` contains `expect_s3_class(bundle, "sube_matrices")`
    - `tests/testthat/test-workflow.R` contains a matrix-contract assertion beyond class checks
  </acceptance_criteria>
  <done>Matrix construction has explicit validation and the workflow test file asserts stable object structure.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_file("tests/testthat/test-workflow.R")'` succeeds
- [ ] Import and matrix tests cover both happy-path shipped examples and at least one malformed-input branch
- [ ] No exported function names or object classes changed unexpectedly
</verification>

<success_criteria>
- All tasks completed
- Workflow entry points reject malformed import or mapping inputs explicitly
- Sample-data matrix construction still succeeds and returns a stable `sube_matrices` object
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-workflow-contracts/01-01-SUMMARY.md`
</output>
