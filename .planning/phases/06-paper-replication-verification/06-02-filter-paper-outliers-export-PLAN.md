---
phase: 06-paper-replication-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/paper_tools.R
  - NAMESPACE
  - man/filter_paper_outliers.Rd
autonomous: true
requirements: [REP-01]
threat_model:
  note: "Rename + @export of an internal pure data.table filter. No new inputs, HTTP surface, auth, or deserialization. ASVS V2/V3/V4/V5/V6 not applicable."
  boundaries: []
  threats: []

must_haves:
  truths:
    - "`filter_paper_outliers` appears in NAMESPACE"
    - "`man/filter_paper_outliers.Rd` exists"
    - "`?filter_paper_outliers` (help index) resolves after devtools::load_all"
    - "existing prepare_sube_comparison + plot_paper_interval_ranges tests still pass (both call sites were updated)"
    - "variables= and apply_bounds= arguments honoured — callers can filter on subsets and skip layer 5"
  artifacts:
    - path: "R/paper_tools.R"
      provides: "Renamed exported filter_paper_outliers() with roxygen block; two updated call sites"
      contains: "filter_paper_outliers <- function(data"
    - path: "NAMESPACE"
      provides: "export(filter_paper_outliers) line"
      contains: "export(filter_paper_outliers)"
    - path: "man/filter_paper_outliers.Rd"
      provides: "CRAN-style man page for the new export"
      min_lines: 30
  key_links:
    - from: "R/paper_tools.R::prepare_sube_comparison (line ~187)"
      to: "filter_paper_outliers"
      via: "direct function call"
      pattern: "filter_paper_outliers\\(comparison"
    - from: "R/paper_tools.R::plot_paper_interval_ranges (line ~325)"
      to: "filter_paper_outliers"
      via: "direct function call"
      pattern: "filter_paper_outliers\\(data"
---

<objective>
Export the paper's six-layer outlier filter as `filter_paper_outliers()`
so researchers can apply the exact historical exclusion rules from the
2018 paper. Refactor the body to honour `variables =` and `apply_bounds =`
args per CONTEXT.md D-08, add a full roxygen block citing
`08_outlier_treatment.R:89-181` per D-09, hand-edit NAMESPACE per Phase 5
D-23, and write the `.Rd` by hand so NAMESPACE stays deterministic.

Purpose: Surfaces the paper-specific outlier treatment as a documented
public API — directly supporting REP-01 (the exported helper is used by
the replication vignette in PLAN 03).

Output:
- R/paper_tools.R — `.apply_paper_filters()` renamed + exported + refactored + documented
- NAMESPACE — one new `export(...)` line
- man/filter_paper_outliers.Rd — new man page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-paper-replication-verification/06-CONTEXT.md
@.planning/phases/06-paper-replication-verification/06-RESEARCH.md
@.planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md

@R/paper_tools.R
@NAMESPACE
@archive/legacy-scripts/08_outlier_treatment.R

<interfaces>
<!-- Current state of .apply_paper_filters at R/paper_tools.R:109-146 -->

Existing signature (internal):
```r
.apply_paper_filters <- function(data)
```

Two in-code callers (grep "apply_paper_filters" R/paper_tools.R):
- Line 17 (roxygen @param on prepare_sube_comparison — argument name, not a call)
- Line 109 (the function definition)
- Line 156 (`apply_paper_filters = TRUE` argument default on prepare_sube_comparison)
- Line 186-187 (call site inside prepare_sube_comparison: `if (apply_paper_filters) comparison <- .apply_paper_filters(comparison)`)
- Line 325 (call site inside plot_paper_interval_ranges: `data <- .apply_paper_filters(data[, ...])`)

New public signature (D-08):
```r
filter_paper_outliers(data,
                      variables = c("GO", "VA", "EMP", "CO2"),
                      apply_bounds = TRUE)
```

NAMESPACE alphabetical placement: currently lines 4-5 have
`export(extract_leontief_matrices)` and `export(extract_domestic_block)`.
`filter_paper_outliers` sorts between `extract_domestic_block` and
`filter_sube`. Insert at line 6 (after line 5 `export(extract_domestic_block)`,
before current `export(filter_sube)`).

Man page style — NAMESPACE is hand-edited per Phase 5 D-23, but
man/*.Rd files are a mix: older ones (compute_sube.Rd, import_suts.Rd)
are hand-edited, newer ones (read_figaro.Rd) carry the roxygen2 header
`% Generated by roxygen2: do not edit by hand`. For Phase 6 we write the
.Rd by hand with the roxygen2-generated style header so it looks
uniform with read_figaro.Rd and survives any future roxygen run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename + refactor + roxygen-document filter_paper_outliers() in R/paper_tools.R</name>
  <files>R/paper_tools.R</files>
  <read_first>
    - R/paper_tools.R (entire file — need exact current state of .apply_paper_filters at lines 109-146, prepare_sube_comparison call site at lines 150-200, plot_paper_interval_ranges call site at lines 300-330, plus the @param docs at line 17)
    - archive/legacy-scripts/08_outlier_treatment.R (lines 89-181 — verify 6-layer citation accuracy in @details)
    - .planning/phases/06-paper-replication-verification/06-RESEARCH.md (section "Code Examples -> Roxygen block for the renamed filter")
  </read_first>
  <action>
Perform four edits in R/paper_tools.R in this order:

**Edit 1: Insert roxygen block + renamed + refactored function at line 109, replacing the existing `.apply_paper_filters <- function(data) { ... }` block (lines 109-146).**

Replace the whole block with:

```r
#' Apply the paper's outlier treatment
#'
#' Applies the six exclusion layers from the 2018 paper's legacy script
#' \code{archive/legacy-scripts/08_outlier_treatment.R} (lines 89-181) to a
#' SUBE comparison table or results summary. These are historical,
#' paper-specific filters - not general-purpose data-quality rules.
#'
#' The six layers:
#' \enumerate{
#'   \item Drop whole countries: \code{CAN}, \code{CYP}.
#'   \item Drop country-year range: \code{BEL} 2000-2008; drop \code{CO2} rows with \code{YEAR > 2009}.
#'   \item Drop 38 country-product pairs across 14 countries (e.g. \code{LUX} P04/P05/P06/...).
#'   \item Drop \code{CO2} rows for \code{CHE}, \code{HRV}, \code{NOR}.
#'   \item Multiplier plausibility bounds (only when \code{apply_bounds = TRUE}):
#'     \code{GO} in [1, 4], \code{VA} in [0, 1], \code{EMP}/\code{CO2} >= 0.
#'   \item Drop rows where elasticity is negative.
#' }
#'
#' @param data A tidy comparison table (the shape returned by
#'   \code{\link{prepare_sube_comparison}}) or a SUBE results summary with
#'   \code{COUNTRY, YEAR, CPAagg, variable, measure, value} columns.
#' @param variables Character vector subset of \code{c("GO", "VA", "EMP", "CO2")}
#'   indicating which metrics to retain. Rows whose \code{variable} is not in
#'   this set are dropped before the layered filters apply. Defaults to all four.
#' @param apply_bounds Logical; whether to apply layer 5 (multiplier
#'   plausibility bounds). Defaults to \code{TRUE}. Set \code{FALSE} to keep
#'   multiplier outliers in the output.
#' @return A filtered \code{data.table} with the same columns as \code{data}.
#' @export
#' @examples
#' \dontrun{
#'   comp <- prepare_sube_comparison(leontief, models)
#'   filt <- filter_paper_outliers(comp)
#'   filt_no_bounds <- filter_paper_outliers(comp, apply_bounds = FALSE)
#' }
filter_paper_outliers <- function(data,
                                  variables = c("GO", "VA", "EMP", "CO2"),
                                  apply_bounds = TRUE) {
  variables <- toupper(variables)
  out <- data.table::copy(data)
  if ("variable" %in% names(out)) {
    out <- out[variable %in% variables]
  }
  out <- out[!COUNTRY %in% c("CAN", "CYP")]
  if ("YEAR" %in% names(out)) {
    out <- out[!(COUNTRY == "BEL" & YEAR %in% 2000:2008)]
    if ("variable" %in% names(out)) {
      out <- out[!(variable == "CO2" & YEAR > 2009)]
    }
  }

  exclusion_map <- list(
    BEL = "P14",
    BRA = "P11",
    DNK = c("P04", "P09"),
    FIN = "P20",
    GRC = c("P09", "P10"),
    HRV = c("P05", "P12", "P18"),
    IRL = "P06",
    LUX = c("P04", "P05", "P06", "P09", "P12", "P15", "P17"),
    KOR = "P11",
    MEX = "P19",
    MLT = c("P08", "P10", "P15", "P20"),
    POL = "P15",
    RUS = c("P20", "P22"),
    SVN = c("P05", "P19")
  )
  for (country in names(exclusion_map)) {
    out <- out[!(COUNTRY == country & CPAagg %in% exclusion_map[[country]])]
  }

  if ("variable" %in% names(out)) {
    out <- out[!(variable == "CO2" & COUNTRY %in% c("CHE", "HRV", "NOR"))]
  }

  if (isTRUE(apply_bounds) && "value" %in% names(out) && "variable" %in% names(out) && "measure" %in% names(out)) {
    out <- out[!(measure == "multiplier" & variable == "GO" & (value < 1 | value > 4))]
    out <- out[!(measure == "multiplier" & variable == "VA" & (value < 0 | value > 1))]
    out <- out[!(measure == "multiplier" & variable %in% c("EMP", "CO2") & value < 0)]
  }
  if ("value" %in% names(out) && "measure" %in% names(out)) {
    out <- out[!(measure == "elasticity" & value < 0)]
  }
  out[]
}
```

Behaviour notes:
- `variables` now actually filters rows by variable membership (new behaviour per D-08); previously ignored.
- `apply_bounds = FALSE` skips layer 5 (multiplier bounds) but still applies the layer-6 negative-elasticity drop to preserve the historical default shape of prepare_sube_comparison output.
- `toupper(variables)` matches the existing `toupper(variables)` style in prepare_sube_comparison (line 160).
- Keep the `out[]` terminal idiom (data.table auto-print trigger) — existing code uses it.

**Edit 2: Update caller at line 186-187 in `prepare_sube_comparison`.**

Replace:
```r
  if (apply_paper_filters) {
    comparison <- .apply_paper_filters(comparison)
  }
```
with:
```r
  if (apply_paper_filters) {
    comparison <- filter_paper_outliers(comparison, variables = variables)
  }
```
(Pass the caller's `variables` argument through so the existing API is preserved; the old internal helper ignored `variables`, but the new public function honours it — threading it here keeps prepare_sube_comparison behaviour unchanged for default calls and correct for non-default calls.)

**Edit 3: Update caller at line ~325 in `plot_paper_interval_ranges`.**

Replace:
```r
  data <- .apply_paper_filters(data[, .(COUNTRY, CPAagg, CPAgroup, type, variable = y, Dp)])
```
with:
```r
  data <- filter_paper_outliers(data[, .(COUNTRY, CPAagg, CPAgroup, type, variable = y, Dp)])
```

**Edit 4: Leave the `@param apply_paper_filters` roxygen line at line ~17 UNCHANGED.** That argument name on `prepare_sube_comparison` is stable public API; only the internal call it delegates to changes.

Final sanity: after all edits run `grep -n "apply_paper_filters\|filter_paper_outliers" R/paper_tools.R`. Expected matches:
- Line ~17: `@param apply_paper_filters Whether to apply the legacy paper exclusion rules.` (roxygen param name — UNCHANGED)
- Line ~156: `apply_paper_filters = TRUE)` (prepare_sube_comparison arg — UNCHANGED)
- Line ~186: `if (apply_paper_filters) {` (prepare_sube_comparison branch — UNCHANGED)
- Line ~187: `filter_paper_outliers(comparison, variables = variables)` (UPDATED)
- Line ~325: `filter_paper_outliers(data[, ...])` (UPDATED)
- The `filter_paper_outliers <- function(` definition (NEW, replacing the old `.apply_paper_filters <- function(data)`)

There must be ZERO remaining `.apply_paper_filters(` calls (with dot prefix) anywhere in the file.
  </action>
  <verify>
    <automated>Rscript -e 'devtools::load_all(); stopifnot(exists("filter_paper_outliers", mode = "function")); stopifnot(!exists(".apply_paper_filters", where = asNamespace("sube"), inherits = FALSE)); devtools::test(filter = "paper|workflow")' </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filter_paper_outliers <- function(data" R/paper_tools.R` returns 1
    - `grep -c "\\.apply_paper_filters(" R/paper_tools.R` returns 0 (zero remaining dot-prefixed calls)
    - `grep -c "\\.apply_paper_filters <- function" R/paper_tools.R` returns 0 (old definition gone)
    - `grep -c "@export" R/paper_tools.R` increases by exactly 1 versus pre-edit baseline
    - `grep -n "08_outlier_treatment.R" R/paper_tools.R` shows at least one match inside the new roxygen block
    - `grep -n "apply_bounds" R/paper_tools.R` shows at least 3 matches (param line, body condition, possibly example)
    - `Rscript -e 'devtools::load_all(); filter_paper_outliers(data.table::data.table(COUNTRY="DEU", YEAR=2005, variable="GO", measure="multiplier", CPAagg="P01", value=2))' returns a data.table with 1 row
    - `Rscript -e 'devtools::load_all(); nrow(filter_paper_outliers(data.table::data.table(COUNTRY="CAN", YEAR=2005, variable="GO", measure="multiplier", CPAagg="P01", value=2)))' returns 0 (layer 1: CAN dropped)
    - `Rscript -e 'devtools::test(filter = "paper|workflow")'` exit 0 (existing prepare_sube_comparison + plot_paper tests still pass)
  </acceptance_criteria>
  <done>Rename complete, roxygen block present with 6-layer enumeration and `08_outlier_treatment.R` citation, both call sites updated, old `.apply_paper_filters` gone, existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-edit NAMESPACE + write man/filter_paper_outliers.Rd</name>
  <files>NAMESPACE, man/filter_paper_outliers.Rd</files>
  <read_first>
    - NAMESPACE (lines 1-20 — current export ordering; find correct alphabetical slot)
    - man/read_figaro.Rd (header + structure of a roxygen2-generated .Rd to mirror style)
    - R/paper_tools.R (updated roxygen block from Task 1 — .Rd content must match)
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md (D-23 — NAMESPACE hand-edited)
  </read_first>
  <action>
**Edit 1: NAMESPACE (hand-edit per Phase 5 D-23 — do NOT run roxygen2::roxygenise()).**

Current NAMESPACE lines 1-8:
```
export(build_matrices)
export(compute_sube)
export(estimate_elasticities)
export(extract_leontief_matrices)
export(extract_domestic_block)
export(filter_sube)
export(import_suts)
export(read_figaro)
```

Insert exactly one new line between `export(extract_domestic_block)` and `export(filter_sube)`:
```
export(filter_paper_outliers)
```

Resulting lines 1-9:
```
export(build_matrices)
export(compute_sube)
export(estimate_elasticities)
export(extract_leontief_matrices)
export(extract_domestic_block)
export(filter_paper_outliers)
export(filter_sube)
export(import_suts)
export(read_figaro)
```

Do NOT reorder other lines. Do NOT run `devtools::document()` or `roxygen2::roxygenise()` — that would overwrite NAMESPACE non-deterministically per Phase 5 D-23.

**Edit 2: Create man/filter_paper_outliers.Rd by hand.**

Write the file verbatim (roxygen2-style header for forward compatibility if someone ever runs it):

```
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper_tools.R
\name{filter_paper_outliers}
\alias{filter_paper_outliers}
\title{Apply the paper's outlier treatment}
\usage{
filter_paper_outliers(
  data,
  variables = c("GO", "VA", "EMP", "CO2"),
  apply_bounds = TRUE
)
}
\arguments{
\item{data}{A tidy comparison table (the shape returned by
\code{\link{prepare_sube_comparison}}) or a SUBE results summary with
\code{COUNTRY, YEAR, CPAagg, variable, measure, value} columns.}

\item{variables}{Character vector subset of \code{c("GO", "VA", "EMP", "CO2")}
indicating which metrics to retain. Rows whose \code{variable} is not in
this set are dropped before the layered filters apply. Defaults to all four.}

\item{apply_bounds}{Logical; whether to apply layer 5 (multiplier
plausibility bounds). Defaults to \code{TRUE}. Set \code{FALSE} to keep
multiplier outliers in the output.}
}
\value{
A filtered \code{data.table} with the same columns as \code{data}.
}
\description{
Applies the six exclusion layers from the 2018 paper's legacy script
\code{archive/legacy-scripts/08_outlier_treatment.R} (lines 89-181) to a
SUBE comparison table or results summary. These are historical,
paper-specific filters - not general-purpose data-quality rules.
}
\details{
The six layers:
\enumerate{
  \item Drop whole countries: \code{CAN}, \code{CYP}.
  \item Drop country-year range: \code{BEL} 2000-2008; drop \code{CO2} rows with \code{YEAR > 2009}.
  \item Drop 38 country-product pairs across 14 countries (e.g. \code{LUX} P04/P05/P06/...).
  \item Drop \code{CO2} rows for \code{CHE}, \code{HRV}, \code{NOR}.
  \item Multiplier plausibility bounds (only when \code{apply_bounds = TRUE}):
    \code{GO} in [1, 4], \code{VA} in [0, 1], \code{EMP}/\code{CO2} >= 0.
  \item Drop rows where elasticity is negative.
}
}
\examples{
\dontrun{
  comp <- prepare_sube_comparison(leontief, models)
  filt <- filter_paper_outliers(comp)
  filt_no_bounds <- filter_paper_outliers(comp, apply_bounds = FALSE)
}
}
```

Write this to `man/filter_paper_outliers.Rd` exactly as above (preserve backslashes, no Markdown).
  </action>
  <verify>
    <automated>Rscript -e 'tools::checkRd("man/filter_paper_outliers.Rd"); ns <- readLines("NAMESPACE"); stopifnot("export(filter_paper_outliers)" %in% ns); stopifnot(length(ns[ns == "export(filter_paper_outliers)"]) == 1); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export(filter_paper_outliers)$" NAMESPACE` returns exactly 1
    - NAMESPACE line 6 (1-indexed) equals `export(filter_paper_outliers)` (alphabetical slot between extract_domestic_block and filter_sube)
    - NAMESPACE has no other line changes (same total export count = old + 1, same line content elsewhere)
    - `man/filter_paper_outliers.Rd` exists
    - `tools::checkRd("man/filter_paper_outliers.Rd")` returns character(0) (no Rd errors)
    - `.Rd` contains `\title{Apply the paper's outlier treatment}`
    - `.Rd` contains `\alias{filter_paper_outliers}`
    - `.Rd` contains `08_outlier_treatment.R`
    - `.Rd` contains `apply_bounds` in both `\usage{}` and `\arguments{}`
    - `Rscript -e 'devtools::load_all(); ?filter_paper_outliers' does not error (help system resolves)
    - `Rscript -e 'R.utils::withOptions(list(warn = 2), tools::buildVignettes(dir = "."))' OR simply `R CMD check` rdcheck passes on the new .Rd
  </acceptance_criteria>
  <done>NAMESPACE has the new export in correct alphabetical slot; man page exists and checks clean; help resolves via devtools::load_all().</done>
</task>

</tasks>

<verification>
- `Rscript -e 'devtools::check_man()'` emits no errors or warnings about filter_paper_outliers
- `Rscript -e 'devtools::test()'` — full suite still green
- `grep "filter_paper_outliers" NAMESPACE` returns exactly one line
- `ls man/filter_paper_outliers.Rd` — file present
</verification>

<success_criteria>
- `filter_paper_outliers` is exported and documented
- Both in-tree callers updated (prepare_sube_comparison, plot_paper_interval_ranges)
- Phase 5 D-23 respected (NAMESPACE hand-edit, no devtools::document run)
- No regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-paper-replication-verification/06-02-SUMMARY.md`
</output>
</content>
</invoke>