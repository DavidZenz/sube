---
phase: 05-figaro-sut-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv
  - inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv
  - tests/testthat/test-figaro.R
autonomous: true
requirements:
  - FIG-04
requirements_addressed:
  - FIG-04
tags:
  - r-package
  - testthat
  - fixtures
  - figaro
must_haves:
  truths:
    - "Synthetic FIGARO supply fixture exists and matches the live 7-column header exactly"
    - "Synthetic FIGARO use fixture exists with intermediate-use rows, five final-demand rows, one primary-input row, and at least one FIGW1 row"
    - "tests/testthat/test-figaro.R exists with test_that() blocks covering every row in 05-VALIDATION.md § Phase Requirements → Test Map"
    - "Running devtools::test(filter = 'figaro') executes the file (tests fail because read_figaro() does not yet exist — that is correct)"
    - "system.file('extdata', 'figaro-sample', package = 'sube') resolves to a non-empty path after devtools::install() / load_all()"
  artifacts:
    - path: "inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv"
      provides: "Synthetic FIGARO supply flat file with 7-col header"
      contains: "icsupRow,icsupCol,refArea,rowPi,counterpartArea,colPi,obsValue"
    - path: "inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv"
      provides: "Synthetic FIGARO use flat file with intermediate-use, FD, primary-input, and FIGW1 rows"
      contains: "icuseRow,icuseCol,refArea,rowPi,counterpartArea,colPi,obsValue"
    - path: "tests/testthat/test-figaro.R"
      provides: "Pending testthat test skeletons for FIG-01..FIG-04"
      exports: ["library(testthat)", "library(sube)"]
  key_links:
    - from: "tests/testthat/test-figaro.R"
      to: "inst/extdata/figaro-sample/*.csv"
      via: "system.file('extdata', 'figaro-sample', package = 'sube')"
      pattern: "system\\.file.*figaro-sample"
---

<objective>
Create the Wave 0 test substrate for Phase 5: two synthetic FIGARO-format CSV
fixtures under `inst/extdata/figaro-sample/` and a new
`tests/testthat/test-figaro.R` file containing test skeletons that map 1:1 to
05-VALIDATION.md § Phase Requirements → Test Map.

Purpose: establish the failing-test substrate BEFORE any production code so
Plan 02 (`read_figaro()` implementation) and Plan 03 (`.coerce_map()` NACE
extension) can be validated against a concrete contract. Tests in this plan
MUST FAIL at end-of-plan (because `read_figaro` does not yet exist) — that is
the TDD RED state and is the acceptance signal for this plan.

Output:
- `inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` (fixture)
- `inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` (fixture)
- `tests/testthat/test-figaro.R` (failing test skeletons)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md
@.planning/phases/05-figaro-sut-ingestion/05-RESEARCH.md
@.planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md
@tests/testthat/test-workflow.R
@inst/extdata/sample/sut_data.csv
@R/import.R

<interfaces>
<!-- The fixture/test skeletons must align with these existing package interfaces.
     The executor MUST use these signatures verbatim — no guessing. -->

From R/import.R (existing; will be extended in Plan 02):
```r
# Existing siblings that read_figaro() will be co-located with:
import_suts(path, sheets = c("SUP", "USE"), recursive = FALSE)
extract_domestic_block(data)
sube_example_data(name = c("sut_data", "cpa_map", "ind_map", "inputs", "model_data"))

# New function that Plan 02 will add (skeletons should call it, not yet exists):
read_figaro(path, year, final_demand_vars = c("P3_S13","P3_S14","P3_S15","P51G","P5M"))
```

From R/matrices.R (existing; read_figaro() output feeds in unchanged):
```r
build_matrices(sut_data, cpa_map, ind_map, final_demand_var = "FU_bas")
```

From R/utils.R (existing; used by canonical column assertions):
```r
.sube_required_columns(data, required)  # hard-errors on missing columns
.standardize_names(data)                 # toupper() column names
```

Canonical sube_suts schema (7 columns):
`REP` (character) | `PAR` (character) | `CPA` (character) | `VAR` (character) |
`VALUE` (numeric) | `YEAR` (integer) | `TYPE` (character — `"SUP"` or `"USE"`)

Expected class tag (from R/import.R:66 `import_suts()` pattern):
`c("sube_suts", "data.table", "data.frame")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create synthetic FIGARO supply fixture CSV</name>
  <files>inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv</files>

  <read_first>
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § D-02, D-14 (header + fixture spec)
    - .planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md § Wave 0 Requirements (fixture spec refinement)
    - .planning/phases/05-figaro-sut-ingestion/05-RESEARCH.md § Wave 0 Gaps (fixture content expectations)
    - inst/extdata/sample/sut_data.csv (pattern for fixture file placement — not content; FIGARO uses the 7-col header, not the sube_suts header)
  </read_first>

  <action>
    Create directory `inst/extdata/figaro-sample/` (use `dir.create(..., recursive = TRUE, showWarnings = FALSE)` via an R one-liner or the Write tool).

    Write `inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` with this EXACT 7-column header on line 1 (note: supply-file prefix is `icsupRow`/`icsupCol`):

    ```
    icsupRow,icsupCol,refArea,rowPi,counterpartArea,colPi,obsValue
    ```

    Then write 36 data rows forming a 2-country × 3-CPA × 3-NACE × 2-counterpart cartesian (2 × 3 × 3 × 2 = 36). Use these literal code sets:
    - Countries (both `refArea` AND `counterpartArea`): `REP1`, `REP2`
    - CPA (goes into `rowPi`, with the literal `CPA_` prefix — Plan 02 will strip it): `CPA_P01`, `CPA_P02`, `CPA_P03`
    - NACE industries (goes into `colPi`): `I01`, `I02`, `I03`
    - `icsupRow` = `<refArea>_<rowPi>` (e.g. `REP1_CPA_P01`)
    - `icsupCol` = `<counterpartArea>_<colPi>` (e.g. `REP1_I01`)
    - `obsValue` = synthetic non-zero numeric. Use a deterministic formula that produces a non-singular supply matrix after aggregation. Suggested: pick values so that the diagonal (rep==par, cpa index == ind index) is strictly larger than off-diagonals. Concrete scheme: base value `10 * cpa_idx + ind_idx`, plus `+100` bonus when `rep == par AND cpa_idx == ind_idx`, plus `+1` when `rep != par` (keeps all values distinct and all off-diagonals non-zero but small). Example first 4 rows after header:
      ```
      REP1_CPA_P01,REP1_I01,REP1,CPA_P01,REP1,I01,111
      REP1_CPA_P01,REP1_I02,REP1,CPA_P01,REP1,I02,12
      REP1_CPA_P01,REP1_I03,REP1,CPA_P01,REP1,I03,13
      REP1_CPA_P01,REP2_I01,REP1,CPA_P01,REP2,I01,12
      ```
      (row 1 has +100 diagonal bonus because REP1==REP1 and P01↔I01; row 2 is same country, off-diagonal; row 4 is cross-country, adds +1.)

    Row ordering: iterate outer loop on `refArea` (REP1, REP2), then `rowPi` (CPA_P01, CPA_P02, CPA_P03), then `counterpartArea` (REP1, REP2), then `colPi` (I01, I02, I03). Total rows: 2 × 3 × 2 × 3 = 36.

    File encoding: UTF-8, Unix line endings (`\n`), trailing newline after last row.

    Do NOT add primary-input rows, final-demand rows, or FIGW1 rows to this supply fixture — those go in the USE fixture (Task 2). The supply fixture is deliberately clean so it exercises ONLY the rename+strip+column-projection path in Plan 02.
  </action>

  <verify>
    <automated>test -f inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv &amp;&amp; head -1 inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv | grep -Fx 'icsupRow,icsupCol,refArea,rowPi,counterpartArea,colPi,obsValue' &amp;&amp; [ "$(tail -n +2 inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv | wc -l)" = "36" ]</automated>
  </verify>

  <acceptance_criteria>
    - `inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` exists
    - First line is EXACTLY: `icsupRow,icsupCol,refArea,rowPi,counterpartArea,colPi,obsValue`
    - File contains exactly 36 data rows (37 total lines including header)
    - `grep -c '^REP1' inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` returns 18 (half the rows)
    - `grep -c 'CPA_P01' inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` returns 12 (one-third of rows × 2 since in rowPi column only)
    - `grep -c ',I01$\|,I02$\|,I03$' inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` returns 36
    - No row in this file has `rowPi = "B2A3G"` or any other non-`CPA_`-prefixed value: `grep -v 'CPA_' inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv | wc -l` returns 1 (only the header)
    - No row contains `FIGW1` (this fixture is clean): `grep -c 'FIGW1' inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv` returns 0
    - `Rscript -e 'dt <- data.table::fread("inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv"); stopifnot(nrow(dt) == 36, ncol(dt) == 7, all(names(dt) == c("icsupRow","icsupCol","refArea","rowPi","counterpartArea","colPi","obsValue")))'` exits 0
  </acceptance_criteria>

  <done>
    Supply fixture file exists with the exact 7-column header, 36 data rows following the deterministic cartesian generator, contains only `CPA_`-prefixed rowPi values, and `data.table::fread()` loads it into a 36×7 data.table with the correct column names.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create synthetic FIGARO use fixture CSV (with FD, primary-input, and FIGW1 rows)</name>
  <files>inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv</files>

  <read_first>
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § D-02, D-14, D-19, D-20, D-21 (the use fixture exercises all three research-driven gaps)
    - .planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md § Wave 0 Requirements (≥5 FD rows, ≥1 primary-input row, ≥1 FIGW1 row)
    - inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv (created in Task 1 — use same codes/ordering where overlap applies)
  </read_first>

  <action>
    Write `inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` with this EXACT 7-column header on line 1 (note: use-file prefix is `icuseRow`/`icuseCol`, NOT `icsupRow`):

    ```
    icuseRow,icuseCol,refArea,rowPi,counterpartArea,colPi,obsValue
    ```

    Then write data rows in four ordered blocks:

    **Block A — Intermediate use (36 rows):** Same 2 × 3 × 2 × 3 cartesian as the supply fixture in Task 1. Codes: `REP1`/`REP2` × `CPA_P01`/`CPA_P02`/`CPA_P03` × `REP1`/`REP2` × `I01`/`I02`/`I03`. Column 1 renamed `icuseRow = <refArea>_<rowPi>`, column 2 renamed `icuseCol = <counterpartArea>_<colPi>`. `obsValue` scheme: `5 * cpa_idx + ind_idx` (always positive, smaller than supply, ensures a valid use matrix after aggregation).

    **Block B — Final-demand rows (30 rows, exercises D-20):** For each (refArea ∈ {REP1,REP2}) × (rowPi ∈ {CPA_P01,CPA_P02,CPA_P03}) × (colPi ∈ {P3_S13, P3_S14, P3_S15, P51G, P5M}), emit ONE row with `counterpartArea = refArea` (final demand is reported by-country, not by counterpart — this is the realistic FIGARO convention). That's 2 × 3 × 5 = 30 rows. `obsValue` for each FD code = `2 + fd_idx` (fd_idx 1..5), so per (REP,CPA) the five FD rows sum to 2+3+4+5+6 = 20. After Plan 02's aggregation these collapse to one `VAR = "FU_bas"` row per (REP, PAR, CPA) with `VALUE = 20`. Example row:
    ```
    REP1_CPA_P01,REP1_P3_S13,REP1,CPA_P01,REP1,P3_S13,3
    ```

    **Block C — Primary-input row (1 row, exercises D-19):** Exactly one row representing a SNA value-added block. Literal values:
    ```
    W2_B2A3G,REP1_I01,W2,B2A3G,REP1,I01,999
    ```
    This row has `refArea = "W2"` and `rowPi = "B2A3G"` (does NOT start with `CPA_`) — Plan 02's `startsWith(rowPi, "CPA_")` filter MUST drop it. Test FIG-02 asserts it is absent from the output.

    **Block D — FIGW1 preservation row (1 row, exercises D-21):** Exactly one row where `refArea = "FIGW1"` and `counterpartArea = "REP1"` to verify D-21 (FIGW1 survives import). Literal:
    ```
    FIGW1_CPA_P01,REP1_I01,FIGW1,CPA_P01,REP1,I01,7
    ```
    This row has a valid `CPA_`-prefixed rowPi so it passes the D-19 filter; the D-21 test asserts `"FIGW1" %in% out$REP` after import.

    **Total row count: 36 + 30 + 1 + 1 = 68 data rows.**

    Row ordering in the file: Block A, then Block B, then Block C, then Block D, in that order. Within each block use deterministic iteration order (outermost = refArea, innermost = colPi).

    File encoding: UTF-8, Unix line endings, trailing newline.
  </action>

  <verify>
    <automated>test -f inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv &amp;&amp; head -1 inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv | grep -Fx 'icuseRow,icuseCol,refArea,rowPi,counterpartArea,colPi,obsValue' &amp;&amp; [ "$(tail -n +2 inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv | wc -l)" = "68" ]</automated>
  </verify>

  <acceptance_criteria>
    - `inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` exists
    - First line is EXACTLY: `icuseRow,icuseCol,refArea,rowPi,counterpartArea,colPi,obsValue`
    - File contains exactly 68 data rows (69 total lines including header)
    - `grep -c 'B2A3G' inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` returns 1
    - `grep -c '^W2,\|,W2,' inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` returns ≥1 (the primary-input row)
    - `grep -c 'FIGW1' inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` returns ≥1
    - Five final-demand codes all present: for each of P3_S13, P3_S14, P3_S15, P51G, P5M, `grep -c ',<code>,' inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv` returns ≥1 (actually 6 each given 2 countries × 3 products × 1 code)
    - `Rscript -e 'dt <- data.table::fread("inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv"); stopifnot(nrow(dt) == 68, ncol(dt) == 7, "B2A3G" %in% dt$rowPi, "FIGW1" %in% dt$refArea, all(c("P3_S13","P3_S14","P3_S15","P51G","P5M") %in% dt$colPi))'` exits 0
  </acceptance_criteria>

  <done>
    Use fixture file exists with the exact 7-column header, 68 data rows across four blocks (36 intermediate use, 30 FD across five codes, 1 primary-input W2/B2A3G row, 1 FIGW1 row). Loads via `data.table::fread()` into a 68×7 data.table with all three research-gap scenarios (D-19, D-20, D-21) represented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/testthat/test-figaro.R with failing skeletons for FIG-01..FIG-04</name>
  <files>tests/testthat/test-figaro.R</files>

  <read_first>
    - .planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md § Phase Requirements → Test Map (every row becomes an expect_*)
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § D-15, D-17 (tests exist in test-figaro.R)
    - tests/testthat/test-workflow.R (style template — library() calls, test_that() blocks, inline helpers)
    - R/import.R (for `import_suts()`, `extract_domestic_block()`, `sube_example_data()` — existing siblings that tests may compose with)
    - inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv (fixture from Task 1)
    - inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv (fixture from Task 2)
  </read_first>

  <action>
    Create `tests/testthat/test-figaro.R` with the EXACT structure below. All tests MUST be "active" (not skipped) — they will fail at end-of-plan because `read_figaro()` does not yet exist. Plan 02 will make them pass.

    File header (first 3 lines, verbatim):
    ```r
    library(testthat)
    library(sube)

    ```

    Inline helper (define before the first test_that block):
    ```r
    figaro_fixture_dir <- function() {
      path <- system.file("extdata", "figaro-sample", package = "sube")
      if (!nzchar(path)) {
        skip("figaro-sample fixture not installed; run devtools::load_all() or install package")
      }
      path
    }

    make_tiny_figaro_maps <- function() {
      # Minimal mapping tables for the FIG-04 integration chain.
      # Matches the fixture's CPA/NACE codes from Task 1/Task 2.
      cpa_map <- data.table::data.table(
        CPA = c("P01", "P02", "P03"),
        CPAagg = c("PX", "PX", "PY")
      )
      ind_map <- data.table::data.table(
        NACE = c("I01", "I02", "I03"),  # deliberately uses "NACE" to also exercise FIG-03
        INDagg = c("IX", "IX", "IY")
      )
      inputs <- data.table::data.table(
        YEAR = 2023L,
        REP = c("REP1", "REP1", "REP2", "REP2"),
        INDUSTRY = c("IX", "IY", "IX", "IY"),
        GO = c(100, 80, 90, 70),
        VA = c(40, 30, 35, 25),
        EMP = c(10, 8, 9, 7),
        CO2 = c(5, 4, 4.5, 3.5)
      )
      list(cpa_map = cpa_map, ind_map = ind_map, inputs = inputs)
    }
    ```

    Then create the following `test_that()` blocks (one per behavior group). Each block below MUST be present with at least the listed assertions. Use `expect_error()` where the map row says "hard error". Use `expect_s3_class()` for class checks.

    **Block 1: FIG-01 — basic import shape and class**
    ```r
    test_that("read_figaro returns a sube_suts object with canonical columns (FIG-01)", {
      dir <- figaro_fixture_dir()
      out <- read_figaro(dir, year = 2023)

      expect_s3_class(out, "sube_suts")
      expect_s3_class(out, "data.table")
      expect_true(all(c("REP", "PAR", "CPA", "VAR", "VALUE", "YEAR", "TYPE") %in% names(out)))
      expect_type(out$REP, "character")
      expect_type(out$PAR, "character")
      expect_type(out$CPA, "character")
      expect_type(out$VAR, "character")
      expect_type(out$VALUE, "double")
      expect_true(is.integer(out$YEAR) || is.numeric(out$YEAR))
      expect_type(out$TYPE, "character")
      expect_setequal(unique(out$TYPE), c("SUP", "USE"))
      expect_true(all(out$YEAR == 2023L))
    })
    ```

    **Block 2: FIG-01 — year argument validation**
    ```r
    test_that("read_figaro hard-errors on missing or invalid year (FIG-01, D-08)", {
      dir <- figaro_fixture_dir()
      expect_error(read_figaro(dir))  # missing year
      expect_error(read_figaro(dir, year = "twenty-three"))
      expect_error(read_figaro(dir, year = 20.5))
      expect_error(read_figaro(dir, year = c(2022, 2023)))
    })
    ```

    **Block 3: FIG-01 — directory pairing errors**
    ```r
    test_that("read_figaro hard-errors on missing path, zero files, or ambiguous files (FIG-01, D-11)", {
      expect_error(read_figaro("/nonexistent/figaro/dir", year = 2023))

      empty_dir <- file.path(tempdir(), "sube-figaro-empty")
      dir.create(empty_dir, showWarnings = FALSE)
      expect_error(read_figaro(empty_dir, year = 2023))

      ambiguous_dir <- file.path(tempdir(), "sube-figaro-ambig")
      dir.create(ambiguous_dir, showWarnings = FALSE)
      file.copy(
        list.files(figaro_fixture_dir(), full.names = TRUE),
        ambiguous_dir,
        overwrite = TRUE
      )
      # Create a second supply file to induce ambiguity
      file.copy(
        file.path(figaro_fixture_dir(), "flatfile_eu-ic-supply_sample.csv"),
        file.path(ambiguous_dir, "flatfile_eu-ic-supply_duplicate.csv")
      )
      expect_error(read_figaro(ambiguous_dir, year = 2023))
    })
    ```

    **Block 4: FIG-02 — CPA_ prefix stripped, inter-country preserved**
    ```r
    test_that("read_figaro strips CPA_ prefix and preserves inter-country rows (FIG-02, D-06, D-10)", {
      dir <- figaro_fixture_dir()
      out <- read_figaro(dir, year = 2023)

      expect_true(all(!startsWith(out$CPA, "CPA_")))
      expect_true(all(out$CPA %in% c("P01", "P02", "P03")))

      # Inter-country rows preserved (REP != PAR)
      expect_true(any(out$REP != out$PAR))
    })
    ```

    **Block 5: FIG-02 — primary-input filter (D-19)**
    ```r
    test_that("read_figaro filters primary-input rows with non-CPA rowPi (FIG-02, D-19)", {
      dir <- figaro_fixture_dir()
      out <- read_figaro(dir, year = 2023)

      expect_false("B2A3G" %in% out$CPA)
      expect_false("W2" %in% out$REP)
      expect_false("W2" %in% out$PAR)
      # Every remaining CPA code must have come from CPA_-prefixed rowPi
      expect_true(all(out$CPA %in% c("P01", "P02", "P03")))
    })
    ```

    **Block 6: FIG-02 — final-demand aggregation to FU_bas (D-20)**
    ```r
    test_that("read_figaro aggregates five FD codes into VAR = 'FU_bas' (FIG-02, D-20)", {
      dir <- figaro_fixture_dir()
      out <- read_figaro(dir, year = 2023)

      use_rows <- out[TYPE == "USE"]
      expect_true("FU_bas" %in% use_rows$VAR)
      # Original FD codes should NOT appear in output
      expect_false(any(c("P3_S13", "P3_S14", "P3_S15", "P51G", "P5M") %in% out$VAR))

      # Fixture: 2 countries × 3 CPA × (2+3+4+5+6)=20 per (REP,CPA)
      # Plan 02 aggregates over counterpart (FD rows have counterpart = refArea)
      # so expect 6 FU_bas rows totaling 6 * 20 = 120
      fu_rows <- use_rows[VAR == "FU_bas"]
      expect_equal(nrow(fu_rows), 6L)
      expect_equal(sum(fu_rows$VALUE), 120)
    })
    ```

    **Block 7: FIG-02 — FIGW1 preservation (D-21)**
    ```r
    test_that("read_figaro preserves FIGW1 rows (FIG-02, D-21)", {
      dir <- figaro_fixture_dir()
      out <- read_figaro(dir, year = 2023)

      expect_true("FIGW1" %in% out$REP)
    })
    ```

    **Block 8: FIG-02 — final_demand_vars override validation**
    ```r
    test_that("final_demand_vars arg validates membership and overrides aggregation set (FIG-02, D-20, D-22)", {
      dir <- figaro_fixture_dir()
      # Unknown code -> hard error
      expect_error(read_figaro(dir, year = 2023, final_demand_vars = "BOGUS"))

      # Subset aggregation yields smaller FU_bas total
      out_full <- read_figaro(dir, year = 2023)
      out_subset <- read_figaro(
        dir, year = 2023,
        final_demand_vars = c("P3_S14")
      )
      expect_lt(
        sum(out_subset[VAR == "FU_bas"]$VALUE),
        sum(out_full[VAR == "FU_bas"]$VALUE)
      )
    })
    ```

    **Block 9: FIG-03 — .coerce_map NACE synonyms routed correctly**
    ```r
    test_that(".coerce_map routes NACE and NACE_R2 column names to VAR (FIG-03, D-16)", {
      sut <- sube_example_data("sut_data")
      cpa_map <- sube_example_data("cpa_map")

      # Use a NACE-named ind_map instead of VARS-named one
      nace_map <- data.table::data.table(
        NACE = c("I1", "I2"),
        INDagg = c("I01", "I02")
      )
      expect_silent(bundle_nace <- build_matrices(sut, cpa_map, nace_map))
      expect_s3_class(bundle_nace, "sube_matrices")
      expect_true(length(bundle_nace$matrices) > 0)

      # Same test with NACE_R2
      nace_r2_map <- data.table::data.table(
        NACE_R2 = c("I1", "I2"),
        INDagg = c("I01", "I02")
      )
      expect_silent(bundle_nace_r2 <- build_matrices(sut, cpa_map, nace_r2_map))
      expect_s3_class(bundle_nace_r2, "sube_matrices")
    })
    ```

    **Block 10: FIG-04 — fixture reachable via system.file**
    ```r
    test_that("figaro-sample fixture directory is reachable via system.file (FIG-04)", {
      dir <- system.file("extdata", "figaro-sample", package = "sube")
      expect_true(nzchar(dir))
      expect_true(file.exists(file.path(dir, "flatfile_eu-ic-supply_sample.csv")))
      expect_true(file.exists(file.path(dir, "flatfile_eu-ic-use_sample.csv")))
    })
    ```

    **Block 11: FIG-04 — end-to-end integration chain**
    ```r
    test_that("read_figaro output flows through extract_domestic_block -> build_matrices -> compute_sube (FIG-04)", {
      dir <- figaro_fixture_dir()
      sut <- read_figaro(dir, year = 2023)

      domestic <- extract_domestic_block(sut)
      expect_s3_class(domestic, "sube_domestic_suts")
      expect_true(all(domestic$REP == domestic$PAR))

      maps <- make_tiny_figaro_maps()
      bundle <- build_matrices(domestic, maps$cpa_map, maps$ind_map)
      expect_s3_class(bundle, "sube_matrices")

      result <- compute_sube(bundle, maps$inputs)
      expect_s3_class(result, "sube_results")
      expect_true(nrow(result$summary) >= 1)
    })
    ```

    Do NOT add `skip()` calls inside any test_that block. The tests must actively fail on `read_figaro()` not existing — that is the correct RED state for this plan. Plan 02 turns them GREEN.
  </action>

  <verify>
    <automated>test -f tests/testthat/test-figaro.R &amp;&amp; grep -c 'test_that(' tests/testthat/test-figaro.R | awk '$1 >= 11 { exit 0 } { exit 1 }' &amp;&amp; Rscript -e 'res &lt;- tryCatch(devtools::test(filter = "figaro"), error = function(e) e); cat("ran\n")' 2&gt;&amp;1 | grep -q 'ran\|FAIL\|read_figaro'</automated>
  </verify>

  <acceptance_criteria>
    - `tests/testthat/test-figaro.R` exists
    - File starts with `library(testthat)` on line 1 and `library(sube)` on line 2
    - File contains at least 11 `test_that(` occurrences (one per block above): `grep -c 'test_that(' tests/testthat/test-figaro.R` returns ≥ 11
    - File references `read_figaro(` at least 10 times: `grep -c 'read_figaro(' tests/testthat/test-figaro.R` returns ≥ 10
    - File references `system.file("extdata", "figaro-sample"`: `grep -c 'figaro-sample' tests/testthat/test-figaro.R` returns ≥ 1
    - File contains the fixture helper: `grep -c 'figaro_fixture_dir' tests/testthat/test-figaro.R` returns ≥ 4
    - File references every research-driven scenario: `grep -c 'B2A3G' tests/testthat/test-figaro.R` ≥ 1, `grep -c 'FIGW1' tests/testthat/test-figaro.R` ≥ 1, `grep -c 'FU_bas' tests/testthat/test-figaro.R` ≥ 2, `grep -c 'P3_S13' tests/testthat/test-figaro.R` ≥ 1, `grep -c 'NACE_R2' tests/testthat/test-figaro.R` ≥ 1
    - `Rscript -e 'devtools::test(filter = "figaro", reporter = "summary")'` runs to completion and reports FAILURES (not an error loading the file). Failure output must include mentions of `read_figaro` because the function does not yet exist. The file ITSELF must parse: `Rscript -e 'parse("tests/testthat/test-figaro.R")'` exits 0
  </acceptance_criteria>

  <done>
    `tests/testthat/test-figaro.R` exists with 11 test_that() blocks covering every row in 05-VALIDATION.md Phase Requirements → Test Map. The file parses cleanly under `Rscript -e 'parse(...)'`. Running `devtools::test(filter = "figaro")` executes all tests and reports failures citing `read_figaro` not existing (NOT a parse/load error). This confirms the test substrate is wired to the fixtures and ready for Plan 02 to make green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → package fixtures | User-supplied fixture data stored in `inst/extdata/figaro-sample/`. Committed to git, reviewed by maintainers. No runtime untrusted input. |
| test file → package code | Tests exercise `read_figaro()` via the testthat harness. No untrusted input — fixtures are deterministic and committed. |

## STRIDE Threat Register

Per 05-RESEARCH.md § Security Domain, `sube` is an R data-analysis package with no network, auth, session, or database surface. Plan 01 creates only static fixture CSVs and an R test file — zero runtime attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `inst/extdata/figaro-sample/*.csv` (committed fixtures) | accept | Fixtures are committed, code-reviewed, and deterministic. Any tamper shows up in `git diff`. |
| T-05-02 | Information Disclosure | fixture contents | accept | Synthetic abstract codes (`REP1`, `REP2`, `CPA_P01`..`P03`). Contains no real country/industry data, no PII, no confidential values. |
| T-05-03 | DoS | fixture size loaded into memory during tests | accept | Fixtures are < 10KB each. Cannot cause memory exhaustion. |

No mitigate-class threats in Plan 01. All threats are "accept" with documented rationale.
</threat_model>

<verification>
Overall plan verification — run after all three tasks:

1. `test -d inst/extdata/figaro-sample && ls inst/extdata/figaro-sample/` shows both CSV files
2. `Rscript -e 'parse("tests/testthat/test-figaro.R")'` exits 0 (file parses)
3. `Rscript -e 'devtools::test(filter = "figaro", reporter = "summary")' 2>&1 | tail -20` shows test failures with `read_figaro` in the failure messages (NOT a file-load error; NOT all skipped)
4. `Rscript -e 'devtools::load_all(); nzchar(system.file("extdata", "figaro-sample", package = "sube"))'` prints `[1] TRUE` (fixture installs via load_all)
5. Regression: `Rscript -e 'devtools::test(filter = "workflow")'` still passes (no unrelated breakage)
</verification>

<success_criteria>
- Both fixture CSVs exist with the exact 7-column FIGARO header (supply uses `icsupRow`/`icsupCol`, use uses `icuseRow`/`icuseCol`)
- Supply fixture has 36 data rows (clean 2×3×2×3 cartesian, all `CPA_`-prefixed)
- Use fixture has 68 data rows (36 intermediate + 30 FD across 5 codes + 1 primary-input `W2`/`B2A3G` row + 1 `FIGW1` row)
- `tests/testthat/test-figaro.R` exists with ≥11 test_that() blocks covering FIG-01..FIG-04 per VALIDATION.md Phase Requirements → Test Map
- Tests actively FAIL (RED state) because `read_figaro()` does not yet exist — this is the expected outcome
- `test-workflow.R` continues to pass (no regression)
- `Rscript -e 'parse("tests/testthat/test-figaro.R")'` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-figaro-sut-ingestion/05-01-SUMMARY.md` documenting:
- Exact fixture layouts (row counts per block, cartesian schema)
- List of test_that block names with their requirement mapping
- Confirmation that tests fail on `read_figaro` not existing (paste last 20 lines of devtools::test output)
- Any deviations from the spec and why
</output>
