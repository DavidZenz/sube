---
phase: 05-figaro-sut-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - R/import.R
  - NAMESPACE
  - man/read_figaro.Rd
autonomous: true
requirements:
  - FIG-01
  - FIG-02
requirements_addressed:
  - FIG-01
  - FIG-02
tags:
  - r-package
  - data.table
  - import
  - figaro
must_haves:
  truths:
    - "User can call read_figaro(path, year) on the figaro-sample fixture directory and receive a sube_suts object with the seven canonical columns"
    - "CPA_ prefix is stripped from rowPi before it becomes the CPA column (D-06)"
    - "Primary-input rows with non-CPA_-prefixed rowPi are filtered out before rename (D-19)"
    - "The five FIGARO final-demand codes are aggregated into a single VAR='FU_bas' row per (REP,PAR,CPA) using sum (D-20)"
    - "FIGW1 country code is preserved — no post-filter on refArea/counterpartArea beyond D-19 (D-21)"
    - "Inter-country rows (REP != PAR) are preserved — no hidden domestic filter (D-10)"
    - "Missing, non-numeric, or non-4-digit year triggers a hard error via stop() (D-08)"
    - "Zero or multiple supply/use matches in the directory trigger a hard error listing matched filenames (D-11)"
    - "final_demand_vars argument validates every element against the USE file's colPi and hard-errors on unknown codes (D-22)"
    - "Output class tag is c('sube_suts', 'data.table', 'data.frame') matching import_suts (D-22)"
    - "All 11 test_that blocks in test-figaro.R that exercise FIG-01 and FIG-02 pass (transition from RED to GREEN)"
  artifacts:
    - path: "R/import.R"
      provides: "read_figaro() function, co-located with import_suts() and extract_domestic_block()"
      contains: "read_figaro <- function(path, year, final_demand_vars"
    - path: "NAMESPACE"
      provides: "export(read_figaro) entry generated by devtools::document()"
      contains: "export(read_figaro)"
    - path: "man/read_figaro.Rd"
      provides: "Auto-generated roxygen2 man page for read_figaro()"
      contains: "read_figaro"
  key_links:
    - from: "R/import.R::read_figaro"
      to: "data.table::fread"
      via: "direct call inside the directory-pairing loop"
      pattern: "fread\\("
    - from: "R/import.R::read_figaro"
      to: ".sube_required_columns"
      via: "final-stage canonical shape assertion before class tagging"
      pattern: "\\.sube_required_columns\\("
    - from: "R/import.R::read_figaro"
      to: "c('sube_suts', 'data.table', 'data.frame')"
      via: "class() assignment at function end"
      pattern: "class\\(out\\).*sube_suts"
---

<objective>
Implement the `read_figaro()` function in `R/import.R` per CONTEXT.md
decisions D-01..D-23 and turn the failing test skeletons from Plan 01 GREEN.

Purpose: deliver FIG-01 and FIG-02 — the user can import FIGARO industry-by-
industry SUT CSVs into the canonical `sube_suts` long table with the full 7-
column schema, correct semantics (CPA_ stripped, primary inputs filtered, FD
codes aggregated to FU_bas, FIGW1 preserved), and clear input validation.

Output:
- `R/import.R` extended with `read_figaro()` (co-located with `import_suts()` per D-05)
- Regenerated `NAMESPACE` containing `export(read_figaro)`
- Auto-generated `man/read_figaro.Rd`
- All 11 test_that() blocks in `tests/testthat/test-figaro.R` that exercise
  FIG-01 and FIG-02 transition from failing to passing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md
@.planning/phases/05-figaro-sut-ingestion/05-RESEARCH.md
@.planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md
@.planning/phases/05-figaro-sut-ingestion/05-01-SUMMARY.md
@R/import.R
@R/utils.R
@R/matrices.R
@tests/testthat/test-figaro.R
@inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv
@inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv
@NAMESPACE
@DESCRIPTION

<interfaces>
<!-- Existing signatures the executor MUST reuse verbatim (no re-derivation). -->

From R/utils.R (unchanged in this plan):
```r
.sube_required_columns(data, required)   # stop() on missing columns
.standardize_names(data)                  # toupper() column names + as.data.table
# NB: read_figaro does NOT call .standardize_names on input because FIGARO
# columns are lowercase/mixedCase and need lookup by exact name (refArea, rowPi, ...)
```

From R/import.R (existing sibling — pattern to mirror for class tagging):
```r
import_suts <- function(path, sheets = c("SUP", "USE"), recursive = FALSE) {
  # ...
  out <- rbindlist(tables, fill = TRUE)
  class(out) <- c("sube_suts", class(out))    # ← mirror this exact pattern
  out[]
}
```

From data.table (already imported in NAMESPACE lines 16-25):
```r
fread(file)                  # fast CSV reader, handles ~500 MB flat files
setnames(dt, old, new)       # in-place rename
rbindlist(list, fill = TRUE) # bind rows, unify columns
```

New signature (per D-22, SUPERSEDES D-13):
```r
read_figaro(
  path,
  year,
  final_demand_vars = c("P3_S13", "P3_S14", "P3_S15", "P51G", "P5M")
)
```

Canonical output columns (fixed order per .sube_required_columns contract):
`c("REP", "PAR", "CPA", "VAR", "VALUE", "YEAR", "TYPE")`

Output class tag (per D-22, mirrors import_suts:66):
`c("sube_suts", "data.table", "data.frame")`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement read_figaro() in R/import.R with full D-01..D-22 pipeline</name>
  <files>R/import.R</files>

  <read_first>
    - R/import.R (the whole file — read to understand the existing `import_suts()` structure and class-tagging pattern at line 66)
    - R/utils.R (lines 1-20 for `.sube_required_columns`; lines 17-21 for `.standardize_names` — NOT used on input since FIGARO column names are mixedCase; lines 38-60 for `.coerce_map` signature context, though not called here)
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § D-01 through D-23 — this is the SOURCE OF TRUTH for every behavior
    - .planning/phases/05-figaro-sut-ingestion/05-RESEARCH.md § Validation Architecture (Phase Requirements → Test Map) — every test in test-figaro.R maps back to a decision the implementation must honor
    - tests/testthat/test-figaro.R (created by Plan 01) — every assertion is a contract
    - inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv (for manual sanity checks)
    - inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv
  </read_first>

  <behavior>
    Tests to pass (from tests/testthat/test-figaro.R — read it verbatim; do not paraphrase):
    - Block 1 (FIG-01): `read_figaro("figaro-sample", year = 2023)` returns `sube_suts` with 7 canonical columns of correct types; TYPE is setequal `c("SUP", "USE")`; YEAR is all 2023
    - Block 2 (FIG-01, D-08): `expect_error` on missing `year`, non-numeric `year`, non-integer `year`, length > 1 `year`
    - Block 3 (FIG-01, D-11): `expect_error` on nonexistent path, empty directory, directory with two supply files
    - Block 4 (FIG-02, D-06, D-10): `all(!startsWith(out$CPA, "CPA_"))`; `all(out$CPA %in% c("P01","P02","P03"))`; `any(out$REP != out$PAR)` is TRUE
    - Block 5 (FIG-02, D-19): `!"B2A3G" %in% out$CPA`; `!"W2" %in% out$REP`; `!"W2" %in% out$PAR`
    - Block 6 (FIG-02, D-20): `"FU_bas" %in% out[TYPE=="USE"]$VAR`; no FD codes remain; exactly 6 FU_bas rows in the fixture output; sum of FU_bas VALUE == 120
    - Block 7 (FIG-02, D-21): `"FIGW1" %in% out$REP`
    - Block 8 (FIG-02, D-20, D-22): `final_demand_vars = "BOGUS"` errors; subset aggregation produces smaller FU_bas total than default
  </behavior>

  <action>
    In `R/import.R`, add a new exported function `read_figaro()` AFTER `extract_domestic_block()` (around line 79) and BEFORE `sube_example_data()`. Do NOT create a new file — D-05 is locked.

    Roxygen header (use markdown, consistent with `import_suts()` at lines 1-24):

    ```r
    #' Import FIGARO Supply-Use Tables
    #'
    #' `read_figaro()` reads a pair of Eurostat FIGARO industry-by-industry
    #' flat-format CSV files (one supply, one use) from a single directory and
    #' returns the canonical `sube_suts` long table with columns `REP`, `PAR`,
    #' `CPA`, `VAR`, `VALUE`, `YEAR`, and `TYPE`. The output feeds directly into
    #' [extract_domestic_block()], [build_matrices()], and [compute_sube()]
    #' without modification.
    #'
    #' The function expects a directory containing exactly one supply file and
    #' exactly one use file. Files are identified by filename: supply files
    #' match `-supply-` or `_supply_` (case-insensitive), use files match
    #' `-use-` or `_use_`. Zero or multiple matches on either side is a hard
    #' error. Single-file input is not supported in this release.
    #'
    #' Primary-input rows (`rowPi` values not starting with `CPA_`, e.g.
    #' `B2A3G`, `D1`, `D21X31`, `D29X39`, `OP_RES`, `OP_NRES`) are dropped
    #' during import because they are SNA/ESA value-added blocks, not products.
    #' The `CPA_` prefix on the remaining `rowPi` values is stripped so product
    #' codes match NACE-style industry codes lexically.
    #'
    #' FIGARO publishes five final-demand codes (`P3_S13`, `P3_S14`, `P3_S15`,
    #' `P51G`, `P5M`). On the use file these are summed per `(REP, PAR, CPA)`
    #' into a single synthetic row with `VAR = "FU_bas"` so the result consumes
    #' cleanly with [build_matrices()] `final_demand_var = "FU_bas"`. Pass a
    #' custom character vector to `final_demand_vars` to override the
    #' aggregation set (e.g. only household consumption).
    #'
    #' `FIGW1` (FIGARO rest-of-world 1) is a real country code in FIGARO
    #' releases and is preserved in the output alongside real country codes.
    #'
    #' @param path Directory containing one FIGARO supply file and one FIGARO
    #'   use file. Neither a single-file path nor recursive directory scanning
    #'   is supported.
    #' @param year Four-digit integer reference year for the data (the year
    #'   encoded in the FIGARO filename, e.g. `2023` in
    #'   `flatfile_eu-ic-supply_25ed_2023.csv`). Required; there is no
    #'   auto-inference from the filename.
    #' @param final_demand_vars Character vector of FIGARO `colPi` codes to sum
    #'   into the synthetic `VAR = "FU_bas"` row on the use file. Defaults to
    #'   the full FIGARO final-demand set. Every supplied code must exist in
    #'   the use file's `colPi` column; unknown codes are a hard error.
    #'
    #' @return An object of class `c("sube_suts", "data.table", "data.frame")`
    #'   with columns `REP`, `PAR`, `CPA`, `VAR`, `VALUE`, `YEAR`, `TYPE`.
    #'
    #' @details
    #' Full FIGARO flat files (e.g. the 25th-edition 2023 release) are
    #' approximately 400--500 MB each and expand to roughly 3--5 GB of peak
    #' resident memory during `data.table::fread()`. Ensure sufficient RAM
    #' before calling `read_figaro()` on a production release. The synthetic
    #' fixture under `inst/extdata/figaro-sample/` is a few kilobytes and is
    #' intended for tests and examples only.
    #'
    #' @export
    ```

    Function body (place immediately after the roxygen block). Use this exact 9-step data.table pipeline — every step maps to a decision:

    ```r
    read_figaro <- function(path, year,
                             final_demand_vars = c("P3_S13", "P3_S14", "P3_S15", "P51G", "P5M")) {
      # --- Step 1: validate `path` (D-11) ------------------------------------
      if (missing(path) || length(path) != 1L || !is.character(path) || !nzchar(path)) {
        stop("`path` must be a single directory path.", call. = FALSE)
      }
      if (!dir.exists(path)) {
        stop(sprintf("`path` does not exist or is not a directory: %s", path),
             call. = FALSE)
      }

      # --- Step 2: validate `year` (D-08) ------------------------------------
      if (missing(year) || length(year) != 1L || !is.numeric(year) ||
          is.na(year) || year != as.integer(year) ||
          year < 1900L || year > 2100L) {
        stop("`year` must be a single four-digit integer (e.g. 2023).",
             call. = FALSE)
      }
      year <- as.integer(year)

      # --- Step 3: validate `final_demand_vars` (D-22) -----------------------
      if (!is.character(final_demand_vars) || length(final_demand_vars) < 1L ||
          any(!nzchar(final_demand_vars)) || anyNA(final_demand_vars)) {
        stop("`final_demand_vars` must be a non-empty character vector.",
             call. = FALSE)
      }

      # --- Step 4: pair supply + use files in the directory (D-11, D-12) -----
      csv_files <- list.files(path, pattern = "\\.csv$", full.names = TRUE,
                              ignore.case = TRUE, recursive = FALSE)
      supply_files <- csv_files[grepl("[-_]supply[-_]", basename(csv_files),
                                      ignore.case = TRUE)]
      use_files    <- csv_files[grepl("[-_]use[-_]",    basename(csv_files),
                                      ignore.case = TRUE)]

      if (length(supply_files) != 1L) {
        stop(sprintf(
          "Expected exactly one FIGARO supply file in %s (found %d: %s).",
          path, length(supply_files),
          paste(basename(supply_files), collapse = ", ")
        ), call. = FALSE)
      }
      if (length(use_files) != 1L) {
        stop(sprintf(
          "Expected exactly one FIGARO use file in %s (found %d: %s).",
          path, length(use_files),
          paste(basename(use_files), collapse = ", ")
        ), call. = FALSE)
      }

      # --- Step 5: read each file and transform into canonical shape ---------
      process_one <- function(file_path, type_tag) {
        dt <- fread(file_path)

        required_in <- c("refArea", "rowPi", "counterpartArea", "colPi", "obsValue")
        missing_in <- setdiff(required_in, names(dt))
        if (length(missing_in) > 0L) {
          stop(sprintf(
            "FIGARO file %s is missing required columns: %s",
            basename(file_path), paste(missing_in, collapse = ", ")
          ), call. = FALSE)
        }

        # Step 5a: drop primary-input rows (D-19) — rowPi must start with CPA_
        dt <- dt[startsWith(rowPi, "CPA_")]

        # Step 5b: rename + project to canonical columns (D-03)
        out <- data.table::data.table(
          REP   = as.character(dt$refArea),
          PAR   = as.character(dt$counterpartArea),
          CPA   = substring(dt$rowPi, 5L),   # strip "CPA_" prefix (D-06)
          VAR   = as.character(dt$colPi),    # D-07 pass-through
          VALUE = as.numeric(dt$obsValue),
          YEAR  = year,
          TYPE  = type_tag
        )

        # Step 5c: on the USE file, validate final_demand_vars then aggregate (D-20)
        if (type_tag == "USE") {
          missing_fd <- setdiff(final_demand_vars, unique(out$VAR))
          if (length(missing_fd) > 0L) {
            stop(sprintf(
              "`final_demand_vars` references codes not present in the USE file's colPi: %s",
              paste(missing_fd, collapse = ", ")
            ), call. = FALSE)
          }

          fd <- out[VAR %in% final_demand_vars,
                    .(VALUE = sum(VALUE, na.rm = TRUE),
                      VAR   = "FU_bas",
                      YEAR  = year,
                      TYPE  = "USE"),
                    by = .(REP, PAR, CPA)]
          non_fd <- out[!(VAR %in% final_demand_vars)]

          out <- rbindlist(
            list(
              non_fd,
              fd[, .(REP, PAR, CPA, VAR, VALUE, YEAR, TYPE)]
            ),
            use.names = TRUE, fill = FALSE
          )
        }

        out[, .(REP, PAR, CPA, VAR, VALUE, YEAR, TYPE)]
      }

      sup <- process_one(supply_files, "SUP")
      use <- process_one(use_files,    "USE")

      # --- Step 6: bind + canonical shape assertion + class tag --------------
      out <- rbindlist(list(sup, use), use.names = TRUE, fill = FALSE)
      .sube_required_columns(out, c("REP", "PAR", "CPA", "VAR", "VALUE", "YEAR", "TYPE"))
      class(out) <- c("sube_suts", class(out))
      out[]
    }
    ```

    Do NOT:
    - Filter on `refArea == "W2"` — use the `startsWith(rowPi, "CPA_")` filter instead (D-19 is defensive against future FIGARO releases)
    - Filter or alter `FIGW1` — it is a real country code (D-21)
    - Call `.standardize_names()` on the raw FIGARO input — column names are mixedCase and need to be looked up by exact name
    - Add a `message()` on successful load — keep the function silent like `import_suts()` (claude's discretion per CONTEXT.md, preference: silent)
    - Modify DESCRIPTION, `_pkgdown.yml`, or `NEWS.md` — those are Plan 04
    - Modify `R/utils.R::.coerce_map()` — that is Plan 03
    - Modify `R/matrices.R`, `R/compute.R`, or any other R file — locked per CONTEXT.md § Non-Integration Points
    - Bump DESCRIPTION Version — D-23 is locked

    NAMESPACE imports: `fread`, `rbindlist`, `data.table`, `setnames` are ALREADY imported (see NAMESPACE lines 16-25). `substring` and `startsWith` are base R — no import needed. The `:=` and `.SD` symbols are already imported. No new `#' @importFrom` directives are needed in this plan.
  </action>

  <verify>
    <automated>Rscript -e 'parse("R/import.R"); devtools::load_all(); stopifnot(exists("read_figaro"), is.function(read_figaro))'</automated>
  </verify>

  <acceptance_criteria>
    - `R/import.R` parses: `Rscript -e 'parse("R/import.R")'` exits 0
    - File contains the exact signature: `grep -c 'read_figaro <- function(path, year,' R/import.R` returns 1
    - File contains the default `final_demand_vars`: `grep -c 'P3_S13.*P3_S14.*P3_S15.*P51G.*P5M' R/import.R` returns ≥ 1
    - File contains the D-19 filter: `grep -c 'startsWith(rowPi, "CPA_")' R/import.R` returns 1
    - File contains the D-06 strip: `grep -c 'substring(dt\$rowPi, 5L)' R/import.R` returns 1
    - File contains the D-20 aggregation to `FU_bas`: `grep -c '"FU_bas"' R/import.R` returns ≥ 1
    - File contains the class tag pattern: `grep -c 'class(out) <- c("sube_suts"' R/import.R` returns 1 (may need escape — acceptable if pattern matches `sube_suts` near `class(out) <-`)
    - File contains year validation: `grep -c 'year < 1900L\|year > 2100L' R/import.R` returns ≥ 1
    - File contains `@export` above the new function: `grep -B1 'read_figaro <- function' R/import.R | grep -c '#.*@export'` returns 1
    - `R/import.R` still exports the existing 3 functions (no accidental removal): `grep -c 'import_suts <- function\|extract_domestic_block <- function\|sube_example_data <- function' R/import.R` returns 3
    - `Rscript -e 'devtools::load_all(); stopifnot(exists("read_figaro"), is.function(read_figaro), length(formals(read_figaro)) == 3L)'` exits 0
    - `Rscript -e 'devtools::load_all(); out <- read_figaro(system.file("extdata","figaro-sample",package="sube"), year = 2023); stopifnot(inherits(out, "sube_suts"), all(c("REP","PAR","CPA","VAR","VALUE","YEAR","TYPE") %in% names(out)), all(!startsWith(out$CPA, "CPA_")), "FU_bas" %in% out$VAR, "FIGW1" %in% out$REP, !("B2A3G" %in% out$CPA), !("W2" %in% out$REP))'` exits 0
  </acceptance_criteria>

  <done>
    `R/import.R` contains the new `read_figaro()` function with the full 9-step pipeline. Loading the package via `devtools::load_all()` and calling `read_figaro()` on the Plan 01 fixture returns a `sube_suts` object where every locked decision (D-06, D-08, D-10, D-11, D-12, D-19, D-20, D-21, D-22) holds. Existing siblings `import_suts()`, `extract_domestic_block()`, `sube_example_data()` are unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate NAMESPACE and man/read_figaro.Rd via devtools::document()</name>
  <files>NAMESPACE, man/read_figaro.Rd</files>

  <read_first>
    - R/import.R (updated in Task 1 — the `#' @export` roxygen tag is the input to this step)
    - NAMESPACE (before state — should contain 14 export() lines; after this task must contain 15 including `export(read_figaro)`)
    - DESCRIPTION (confirm RoxygenNote: 7.3.2 is already set)
  </read_first>

  <behavior>
    Tests to pass (from test-figaro.R Block 11 — integration chain):
    - `library(sube); read_figaro(...)` works without `devtools::load_all()` after install
    - `?read_figaro` resolves to a populated man page
    - Block 10 (fixture reachability) and Block 11 (end-to-end integration) no longer fail with "function not found"
  </behavior>

  <action>
    Run roxygen via devtools (not the raw `roxygen2::roxygenize()` — devtools handles the correct working directory and package loading):

    ```bash
    Rscript -e 'devtools::document()'
    ```

    This will:
    1. Parse the `#' @export` tag on `read_figaro()` and append `export(read_figaro)` to `NAMESPACE`
    2. Generate `man/read_figaro.Rd` from the roxygen header
    3. Sort `NAMESPACE` entries alphabetically within each directive block (roxygen default behavior)

    Do NOT hand-edit `NAMESPACE` or `man/read_figaro.Rd`. Both files are roxygen-managed and must regenerate cleanly. If `devtools::document()` reports warnings about unrelated files, document the warnings in the plan summary but do not attempt to fix them in this plan (they belong to Plan 04's final check).

    After running `devtools::document()`, verify:
    - `NAMESPACE` has grown by exactly one `export(...)` line (`export(read_figaro)`)
    - `man/read_figaro.Rd` exists and contains the canonical sections (`\title`, `\usage`, `\arguments`, `\value`, `\details`)
    - No other `man/*.Rd` file was unexpectedly rewritten (diff check)
    - `NAMESPACE` still contains all 14 prior `export()` lines plus the new one (total 15)

    If `devtools::document()` exits non-zero or complains about missing dependencies, STOP and report. Do not work around by hand-editing — investigate and fix the underlying cause.
  </action>

  <verify>
    <automated>Rscript -e 'devtools::document()' &amp;&amp; test -f man/read_figaro.Rd &amp;&amp; grep -c 'export(read_figaro)' NAMESPACE | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript -e 'devtools::document()'` exits 0 (or exits 0 with benign warnings — no errors)
    - `NAMESPACE` contains exactly one `export(read_figaro)` line: `grep -c 'export(read_figaro)' NAMESPACE` returns 1
    - `NAMESPACE` total export lines ≥ 15: `grep -c '^export(' NAMESPACE` returns ≥ 15
    - All pre-existing exports still present: `grep -c 'export(import_suts)\|export(extract_domestic_block)\|export(sube_example_data)\|export(build_matrices)\|export(compute_sube)' NAMESPACE` returns 5
    - `man/read_figaro.Rd` exists
    - `man/read_figaro.Rd` contains required sections: `grep -c '\\\\title\|\\\\usage\|\\\\arguments\|\\\\value\|\\\\description' man/read_figaro.Rd` returns ≥ 5
    - `man/read_figaro.Rd` references the canonical signature: `grep -c 'read_figaro(path, year, final_demand_vars' man/read_figaro.Rd` returns ≥ 1
    - `man/read_figaro.Rd` mentions the memory expectation (D-18): `grep -c '3--5 GB\|peak' man/read_figaro.Rd` returns ≥ 1
    - `Rscript -e 'library(sube); ?read_figaro' 2>&1 | grep -q 'read_figaro\|No documentation'` — the first pattern is preferred (help resolves)
  </acceptance_criteria>

  <done>
    `devtools::document()` has regenerated `NAMESPACE` with `export(read_figaro)` appended and created `man/read_figaro.Rd` from the Task 1 roxygen header. The package loads cleanly with `library(sube)` after install. All pre-existing exports are preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run test-figaro.R and confirm FIG-01 + FIG-02 tests are GREEN</name>
  <files></files>

  <read_first>
    - tests/testthat/test-figaro.R (all 11 blocks — this task verifies 10 of the 11 pass; Block 9 exercises FIG-03 and belongs to Plan 03)
    - R/import.R (updated in Task 1 — the `read_figaro()` implementation under test)
  </read_first>

  <behavior>
    Tests that MUST PASS after this task:
    - Block 1: "read_figaro returns a sube_suts object with canonical columns (FIG-01)" → PASS
    - Block 2: "read_figaro hard-errors on missing or invalid year (FIG-01, D-08)" → PASS
    - Block 3: "read_figaro hard-errors on missing path, zero files, or ambiguous files (FIG-01, D-11)" → PASS
    - Block 4: "read_figaro strips CPA_ prefix and preserves inter-country rows (FIG-02, D-06, D-10)" → PASS
    - Block 5: "read_figaro filters primary-input rows with non-CPA rowPi (FIG-02, D-19)" → PASS
    - Block 6: "read_figaro aggregates five FD codes into VAR = 'FU_bas' (FIG-02, D-20)" → PASS
    - Block 7: "read_figaro preserves FIGW1 rows (FIG-02, D-21)" → PASS
    - Block 8: "final_demand_vars arg validates membership and overrides aggregation set (FIG-02, D-20, D-22)" → PASS
    - Block 10: "figaro-sample fixture directory is reachable via system.file (FIG-04)" → PASS (this is FIG-04 but already passes once the function loads)
    - Block 11: "read_figaro output flows through extract_domestic_block -> build_matrices -> compute_sube (FIG-04)" → PASS (integration — depends on Task 1 + Plan 03 Task 1, but since Plan 03 Task 1 only extends `.coerce_map`, the Block 11 test uses the `NACE`-named ind_map from the helper which requires Plan 03. IF Plan 03 has not run yet, Block 11 may fail on the NACE synonym — this is ACCEPTABLE for Plan 02 Task 3; Plan 03 Task 2 is responsible for closing it.)

    Tests that MAY remain RED after this task (belong to other plans):
    - Block 9: "coerce_map routes NACE and NACE_R2 column names to VAR (FIG-03, D-16)" — Plan 03
    - Block 11: end-to-end if the `make_tiny_figaro_maps()` helper uses `NACE` before Plan 03 runs — Plan 03

    Regression check: `test-workflow.R` must continue to pass unchanged.
  </behavior>

  <action>
    Run the focused FIGARO test suite and capture output:

    ```bash
    Rscript -e 'devtools::test(filter = "figaro", reporter = "summary")'
    ```

    Then run the full suite to catch regressions:

    ```bash
    Rscript -e 'devtools::test(reporter = "summary")'
    ```

    Expected results AFTER running both commands:

    1. `test-figaro.R` shows AT LEAST 8 passing test_that blocks (Blocks 1, 2, 3, 4, 5, 6, 7, 8, 10 — that's 9 blocks). Blocks 9 and 11 may still fail if they depend on Plan 03's `.coerce_map()` NACE extension. Record the pass/fail breakdown in the plan summary.
    2. `test-workflow.R` shows ALL blocks passing (no regression).
    3. Total test suite: 0 unrelated regressions. New failures are limited to Blocks 9 (FIG-03) and possibly Block 11 (integration using NACE) — document these as expected-red deferred to Plan 03.

    If any FIG-01 or FIG-02 block (Blocks 1-8) fails, STOP and fix Task 1 before proceeding. Do NOT paper over failures with `skip()` or `expect_error(..., "whatever")` broadening. Root-cause failures against the CONTEXT.md decision ID.

    If a pass/fail mismatch exists with the acceptance criteria below, re-read the failing block, identify which decision ID (D-XX) is not being honored, and amend `R/import.R` accordingly.
  </action>

  <verify>
    <automated>Rscript -e 'res &lt;- testthat::test_file("tests/testthat/test-figaro.R", reporter = testthat::SummaryReporter$new()); df &lt;- as.data.frame(res); fig1_fig2_blocks &lt;- grepl("FIG-01|FIG-02", df$test); stopifnot(all(df$failed[fig1_fig2_blocks] == 0))'</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript -e 'devtools::test(filter = "figaro")'` runs to completion without an R error
    - All test_that blocks with names matching `FIG-01` or `FIG-02` pass (zero failed expectations)
    - Concretely, these block names pass: "read_figaro returns a sube_suts object with canonical columns (FIG-01)", "read_figaro hard-errors on missing or invalid year (FIG-01, D-08)", "read_figaro hard-errors on missing path, zero files, or ambiguous files (FIG-01, D-11)", "read_figaro strips CPA_ prefix and preserves inter-country rows (FIG-02, D-06, D-10)", "read_figaro filters primary-input rows with non-CPA rowPi (FIG-02, D-19)", "read_figaro aggregates five FD codes into VAR = 'FU_bas' (FIG-02, D-20)", "read_figaro preserves FIGW1 rows (FIG-02, D-21)", "final_demand_vars arg validates membership and overrides aggregation set (FIG-02, D-20, D-22)"
    - `Rscript -e 'devtools::test(filter = "workflow")'` passes with zero failures (no regression)
    - Summary of all 11 test-figaro.R blocks recorded in the plan summary, each marked PASS/FAIL with the expected-failure reason if FAIL (only Blocks 9 and possibly 11 may be FAIL, and only because of Plan 03 not yet run)
  </acceptance_criteria>

  <done>
    Running `devtools::test(filter = "figaro")` shows ALL FIG-01 and FIG-02 test_that blocks (≥8 blocks) passing. `devtools::test(filter = "workflow")` still passes with zero regressions. The 1–2 remaining red blocks are documented as owned by Plan 03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → `read_figaro(path, year)` | User supplies a directory path and a numeric year. `path` is an opaque string; `year` is type-checked. |
| filesystem → `data.table::fread` | User-supplied CSV files read via `fread`. Files are opaque bytes; `fread` handles malformed CSV gracefully. |

## STRIDE Threat Register

Per 05-RESEARCH.md § Security Domain, `sube` has no network/auth/session/database surface. Threat model for `read_figaro()` is limited to malicious or corrupt input CSVs and user-supplied directory paths. Mitigations mirror the `import_suts()` pattern already accepted for the package.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | `year` argument (user input) | mitigate | Hard type/range check at function entry: `stop()` if missing, non-numeric, non-integer, or outside `[1900, 2100]`. Prevents downstream `NA_integer_` silent propagation (Pitfall #11) and any non-numeric injection into the YEAR column. |
| T-05-05 | Tampering | `path` argument (user input) | mitigate | Hard check: must be single character scalar, non-empty, `dir.exists()`. Prevents confusion with single-file API (D-12 deferred) and bubbles clear error for non-existent paths. |
| T-05-06 | Tampering | `final_demand_vars` argument | mitigate | Validate non-empty character vector; then validate membership in the USE file's `colPi` column. Prevents silent zero-sum aggregation when user typos a FD code. |
| T-05-07 | DoS | large FIGARO file loaded via `fread` | accept | Per D-18, no chunked-read guardrails. Memory expectation documented in `@details`. User controls their input file size; this is a research package, not a multi-tenant service. Same risk posture as `import_suts()`. |
| T-05-08 | Info Disclosure | error messages containing `path` | accept | Error messages include the user-supplied `path` verbatim (e.g., "Expected exactly one FIGARO supply file in /some/path"). The user IS the caller and owns the process — no cross-tenant leakage possible. |
| T-05-09 | Tampering (CSV injection) | cell contents with `=cmd` formulas | accept | `sube` never writes output with formulas; values pass through as numeric/character data.table columns. Never `eval()`ed. Not exploitable in this threat model. |
| T-05-10 | Info Disclosure | symlink traversal via `list.files` | accept | `list.files(path, recursive = FALSE)` enumerates only the requested directory level. Even if symlinks are followed, the caller supplied `path` and owns the trust boundary — same posture as `import_suts()`. |

All mitigate-class threats are closed by the explicit validation steps 1-3 in the Task 1 pipeline. No high-severity residual risk.
</threat_model>

<verification>
Overall plan verification — run after all three tasks:

1. `Rscript -e 'parse("R/import.R")'` exits 0 (file parses)
2. `Rscript -e 'devtools::document(); devtools::load_all()'` exits 0 (package loads with new function)
3. `Rscript -e 'library(sube); length(formals(read_figaro))'` prints `[1] 3`
4. `Rscript -e 'library(sube); out <- read_figaro(system.file("extdata","figaro-sample",package="sube"), year=2023); print(dim(out)); print(table(out$TYPE)); print(sort(unique(out$CPA))); print(sort(unique(out$REP)))'` — expected output includes `SUP` and `USE` type counts, CPA codes `P01`, `P02`, `P03`, REP codes including `REP1`, `REP2`, `FIGW1` — crucially, NO `W2` and NO `B2A3G`
5. `Rscript -e 'devtools::test(filter = "figaro")' 2>&1 | tail -20` shows ≥8 blocks passing (all FIG-01 and FIG-02 blocks)
6. `Rscript -e 'devtools::test(filter = "workflow")' 2>&1 | tail -10` shows zero failures (regression check)
7. NAMESPACE contains `export(read_figaro)`: `grep 'export(read_figaro)' NAMESPACE`
8. `man/read_figaro.Rd` exists and contains `read_figaro(path, year, final_demand_vars`
</verification>

<success_criteria>
- `R/import.R` contains `read_figaro()` with the exact signature from D-22: `read_figaro(path, year, final_demand_vars = c("P3_S13","P3_S14","P3_S15","P51G","P5M"))`
- The 9-step pipeline honors every locked decision: D-03 rename, D-06 CPA_ strip, D-08 year validation, D-10 inter-country preserved, D-11 directory pairing, D-19 primary-input filter via `startsWith(rowPi, "CPA_")`, D-20 FD aggregation to `FU_bas`, D-21 FIGW1 preserved, D-22 final_demand_vars validation
- Class tag is `c("sube_suts", "data.table", "data.frame")` (D-22)
- `devtools::document()` has been run and `NAMESPACE` contains `export(read_figaro)`
- `man/read_figaro.Rd` exists and contains the D-09 reference-year note and D-18 memory expectation in `@details`
- All FIG-01 and FIG-02 test_that blocks in `test-figaro.R` pass (Blocks 1, 2, 3, 4, 5, 6, 7, 8)
- Block 10 (fixture reachability, FIG-04) also passes as a free side-effect
- `test-workflow.R` passes with zero regressions
- `R/matrices.R`, `R/compute.R`, `R/models.R`, `R/filter_plot_export.R`, `R/paper_tools.R`, `DESCRIPTION` are UNCHANGED
- No new entries in DESCRIPTION Imports (data.table is already imported)
</success_criteria>

<output>
After completion, create `.planning/phases/05-figaro-sut-ingestion/05-02-SUMMARY.md` documenting:
- The final 9-step pipeline (paste the function body)
- Which decisions (D-XX) each step honors
- Full `devtools::test(filter = "figaro")` output (summary table with PASS/FAIL per block)
- Full `devtools::test(filter = "workflow")` output (regression confirmation)
- Confirmation that `R/matrices.R`, `R/compute.R`, `R/models.R`, `R/filter_plot_export.R`, `R/paper_tools.R`, and `DESCRIPTION` are unchanged (`git diff --stat` output)
- Any blocks still RED and the plan that owns closing them (expected: Block 9 → Plan 03; possibly Block 11 → Plan 03)
</output>
