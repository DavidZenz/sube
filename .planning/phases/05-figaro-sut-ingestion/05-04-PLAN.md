---
phase: 05-figaro-sut-ingestion
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - _pkgdown.yml
  - NEWS.md
autonomous: true
requirements:
  - FIG-04
requirements_addressed:
  - FIG-04
tags:
  - r-package
  - docs
  - release
  - figaro
must_haves:
  truths:
    - "`read_figaro` appears in `_pkgdown.yml` under the same reference group as `import_suts` (Pitfall #12 closed)"
    - "`NEWS.md` has a v1.1 section documenting the new `read_figaro()` importer and the `.coerce_map()` NACE synonym extension"
    - "`DESCRIPTION` Version is unchanged at `0.1.2` (D-23)"
    - "`Rscript -e 'devtools::check()'` exits 0 with 0 errors, 0 warnings, and 0 new notes beyond any pre-existing acceptable notes"
    - "All 11 test_that blocks in test-figaro.R pass (FIG-01, FIG-02, FIG-03, FIG-04 fully covered)"
    - "Existing test-workflow.R blocks all pass (zero regression)"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "pkgdown reference group entry for read_figaro"
      contains: "read_figaro"
    - path: "NEWS.md"
      provides: "v1.1 changelog entry for read_figaro() + NACE synonym"
      contains: "read_figaro"
  key_links:
    - from: "_pkgdown.yml reference group 'Data import and preparation'"
      to: "man/read_figaro.Rd"
      via: "contents list item"
      pattern: "read_figaro"
---

<objective>
Close the Phase 5 work by completing the release-plumbing chores that do not
belong in Plans 01-03: pkgdown reference-group placement, `NEWS.md`
changelog entry, and final `R CMD check` as the phase gate. This plan
delivers FIG-04's "R CMD check passes" requirement and closes Pitfall #12
(pkgdown omission) from the research index.

Purpose: ship Phase 5 at production quality — website documentation lists
the new function, release notes record the change, and `devtools::check()`
is green as the phase exit gate.

Output:
- `_pkgdown.yml` with `read_figaro` added to the "Data import and preparation" reference group
- `NEWS.md` with a new `# sube (development version)` or `# sube 1.1.0 (unreleased)` section documenting `read_figaro()` and the `.coerce_map()` NACE extension
- `devtools::check()` green (0 errors, 0 warnings, 0 new notes)
- `devtools::test()` green (full regression)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md
@.planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md
@.planning/phases/05-figaro-sut-ingestion/05-01-SUMMARY.md
@.planning/phases/05-figaro-sut-ingestion/05-02-SUMMARY.md
@.planning/phases/05-figaro-sut-ingestion/05-03-SUMMARY.md
@_pkgdown.yml
@NEWS.md
@DESCRIPTION
@R/import.R
@NAMESPACE

<interfaces>
<!-- Existing pkgdown reference group (from _pkgdown.yml lines 10-16) -->

```yaml
reference:
  - title: Data import and preparation
    contents:
      - import_suts
      - extract_domestic_block
      - sube_example_data
      - build_matrices          # ← read_figaro goes BEFORE this (after extract_domestic_block)
```

<!-- Existing NEWS.md structure (from NEWS.md lines 1-24) -->

```markdown
# sube 0.1.2

- Added explicit Leontief matrix extraction...

# sube 0.1.1

- Bumped the development version...

# sube 0.1.0

- ...
```

<!-- DESCRIPTION Version is LOCKED per D-23 -->

```
Package: sube
Version: 0.1.2        # ← DO NOT CHANGE IN PHASE 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add read_figaro to _pkgdown.yml reference group + NEWS.md v1.1 entry</name>
  <files>_pkgdown.yml, NEWS.md</files>

  <read_first>
    - _pkgdown.yml (the whole file, ~58 lines — focus on the `reference:` section lines 10-30)
    - NEWS.md (the whole file, ~24 lines — to match the formatting style of existing entries)
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § "Claude's Discretion" — pkgdown group placement and NEWS wording are discretionary
    - .planning/phases/05-figaro-sut-ingestion/05-CONTEXT.md § D-23 — DESCRIPTION Version does NOT change
    - DESCRIPTION (confirm Version: 0.1.2 — DO NOT edit)
  </read_first>

  <action>
    **Sub-step 1a: Update `_pkgdown.yml`**

    Add `read_figaro` to the "Data import and preparation" reference group.
    Insert it between `extract_domestic_block` and `sube_example_data` so the
    two importers sit next to their domestic helper, matching the workflow
    order a reader expects. Use the Edit tool on `_pkgdown.yml`.

    BEFORE (lines 10-16 of _pkgdown.yml):
    ```yaml
    reference:
      - title: Data import and preparation
        contents:
          - import_suts
          - extract_domestic_block
          - sube_example_data
          - build_matrices
    ```

    AFTER:
    ```yaml
    reference:
      - title: Data import and preparation
        contents:
          - import_suts
          - read_figaro
          - extract_domestic_block
          - sube_example_data
          - build_matrices
    ```

    Do NOT add a new title group for FIGARO. Do NOT reorder the other
    reference groups. Do NOT touch the `articles:`, `navbar:`, or `home:`
    sections.

    **Sub-step 1b: Update `NEWS.md`**

    Add a new top-of-file section for the v1.1 milestone (development
    version). Per D-23, Version in DESCRIPTION stays at `0.1.2` for the
    whole of Phase 5+6, so the NEWS header must use the conventional R
    package format for unreleased changes: `# sube (development version)`.

    Prepend to `NEWS.md` (do NOT remove or modify existing entries):

    ```markdown
    # sube (development version)

    - Added `read_figaro()` for importing Eurostat FIGARO industry-by-industry
      supply and use flat-format CSV files into the canonical `sube_suts` long
      table. The importer auto-pairs one supply and one use file from a
      directory, strips the `CPA_` prefix from product codes, filters SNA
      primary-input rows (`B2A3G`, `D1`, `D21X31`, `D29X39`, `OP_RES`,
      `OP_NRES`), and aggregates the five FIGARO final-demand codes
      (`P3_S13`, `P3_S14`, `P3_S15`, `P51G`, `P5M`) into a single
      `VAR = "FU_bas"` row per `(REP, PAR, CPA)` so the output feeds
      `build_matrices()` unchanged. `FIGW1` (FIGARO rest-of-world 1) is
      preserved as a real country code.
    - Extended `.coerce_map()` to recognize `NACE` and `NACE_R2` as synonyms
      for the industry-identifier column, so FIGARO-derived `ind_map` tables
      route correctly through `build_matrices()` without falling through to
      positional matching.
    - Added `inst/extdata/figaro-sample/` with a synthetic 2-country FIGARO
      fixture and `tests/testthat/test-figaro.R` covering the new importer
      end-to-end.

    # sube 0.1.2
    ```

    The existing `# sube 0.1.2` section and everything below it MUST remain
    verbatim. Preserve the single blank line between sections.

    **Sub-step 1c: Do NOT modify DESCRIPTION**

    Confirm `DESCRIPTION` Version is `0.1.2` and is unchanged. Per D-23,
    the version bump happens once at v1.1 milestone archive, not per phase.
    Any edit to DESCRIPTION's Version field in this plan is a HARD FAILURE.
  </action>

  <verify>
    <automated>grep -q '^      - read_figaro$' _pkgdown.yml &amp;&amp; grep -q '^# sube (development version)$' NEWS.md &amp;&amp; grep -q 'read_figaro' NEWS.md &amp;&amp; grep -q '^Version: 0.1.2$' DESCRIPTION</automated>
  </verify>

  <acceptance_criteria>
    - `_pkgdown.yml` contains `read_figaro` as a list item under the "Data import and preparation" reference group: `grep -c '^      - read_figaro$' _pkgdown.yml` returns 1
    - The reference group order is preserved: `grep -A5 'title: Data import and preparation' _pkgdown.yml | grep -c '^      - '` returns 5 (five entries: import_suts, read_figaro, extract_domestic_block, sube_example_data, build_matrices)
    - `_pkgdown.yml` still parses as YAML: `Rscript -e 'yaml::read_yaml("_pkgdown.yml")' 2>&1 | grep -q Error; [ $? -eq 1 ]` (inverted: no Error lines)
    - Other pkgdown sections unchanged: `grep -c 'Compute, model, and compare\|Comparison and export helpers\|Workflow Start Here' _pkgdown.yml` returns 3
    - `NEWS.md` has a new top section: `head -1 NEWS.md` returns `# sube (development version)`
    - `NEWS.md` contains `read_figaro` mention in the new section: `grep -c 'read_figaro' NEWS.md` returns ≥ 1
    - `NEWS.md` contains `.coerce_map` and `NACE` mention: `grep -c 'NACE' NEWS.md` returns ≥ 1, `grep -c 'coerce_map' NEWS.md` returns ≥ 1
    - Old NEWS.md entries preserved verbatim: `grep -c '^# sube 0.1.2$' NEWS.md` returns 1, `grep -c '^# sube 0.1.1$' NEWS.md` returns 1, `grep -c '^# sube 0.1.0$' NEWS.md` returns 1
    - DESCRIPTION Version unchanged: `grep '^Version:' DESCRIPTION` returns `Version: 0.1.2`
    - DESCRIPTION Imports unchanged (no new dependencies): `git diff DESCRIPTION | grep -c '^[+-]' ` returns 0 (file unchanged)
  </acceptance_criteria>

  <done>
    `_pkgdown.yml` has `read_figaro` in the "Data import and preparation"
    reference group. `NEWS.md` has a new `# sube (development version)`
    section documenting both the `read_figaro()` addition and the
    `.coerce_map()` NACE extension. DESCRIPTION is bit-for-bit unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full R CMD check as the phase gate</name>
  <files></files>

  <read_first>
    - .planning/phases/05-figaro-sut-ingestion/05-VALIDATION.md § Phase gate command (`Rscript -e 'devtools::check()'`)
    - .planning/phases/05-figaro-sut-ingestion/05-02-SUMMARY.md (confirm Plan 02 test results baseline)
    - .planning/phases/05-figaro-sut-ingestion/05-03-SUMMARY.md (confirm Plan 03 test results baseline)
    - DESCRIPTION (verify Version still 0.1.2 at gate time)
    - .Rbuildignore (confirm no new entries needed — `inst/extdata/figaro-sample/` is the standard fixture path and ships with the package)
  </read_first>

  <action>
    Run the phase-gate check sequence:

    **Sub-step 2a: Full test suite**
    ```bash
    Rscript -e 'devtools::test(reporter = "summary")'
    ```
    Expected: zero failures across all test files. Every test_that block in
    tests/testthat/test-figaro.R (11 blocks) and tests/testthat/test-workflow.R
    (pre-existing blocks) must pass.

    **Sub-step 2b: Roxygen re-doc (idempotent sanity check)**
    ```bash
    Rscript -e 'devtools::document()'
    ```
    Expected: no changes to NAMESPACE or man/*.Rd beyond what Plan 02 already
    generated. Any unexpected diff here indicates a roxygen header issue and
    must be investigated.

    **Sub-step 2c: Full R CMD check**
    ```bash
    Rscript -e 'devtools::check()'
    ```
    Expected output: `── R CMD check results ── sube 0.1.2 ────` with
    `0 errors ✔ | 0 warnings ✔ | 0 notes ✔` at the end.

    Allowed deviations (document in summary if they appear):
    - Pre-existing notes from v1.0 that are not caused by Phase 5 (e.g., URL
      redirect notes, non-standard-file notes on `.planning/` — which is in
      `.Rbuildignore`). If such notes exist, confirm they were present before
      Phase 5 by comparing against v1.0 archive.
    - Vignette build skips (if `knitr` / `rmarkdown` are missing, `check()`
      skips vignettes — acceptable on minimal CI environments).

    NOT acceptable (hard failure):
    - Any error
    - Any warning
    - NEW notes that did not exist in the v1.0 baseline, caused by Phase 5
      code. Common culprits: "undocumented argument" (fix the roxygen
      `@param`), "Undocumented S3 method" (add `@rdname`), "unused `@importFrom`"
      (remove it), "non-ASCII text in code" (fix encoding).

    **Sub-step 2d: Fixture install reachability**
    ```bash
    Rscript -e 'devtools::install(quick = TRUE); library(sube); print(system.file("extdata","figaro-sample",package="sube"))'
    ```
    Expected: prints a non-empty path to the installed fixture directory.
    This confirms `inst/extdata/figaro-sample/` survives the R CMD build
    → install pipeline and is reachable via `system.file()` in a real
    installed package context (not just `devtools::load_all()`).

    If any sub-step fails, STOP, root-cause the failure, and either:
    - Fix the issue in the relevant previous plan's files and re-run the gate, OR
    - If the fix is non-trivial, return `## CHECKPOINT REACHED` to the
      orchestrator with the failure details — do NOT paper over real errors.

    Do NOT modify source files in this task beyond what `devtools::document()`
    regenerates (NAMESPACE / man/*.Rd) — and those should already be
    idempotent from Plan 02. If `document()` makes unexpected changes, stop
    and investigate.
  </action>

  <verify>
    <automated>Rscript -e 'devtools::test(reporter = "summary")' 2&gt;&amp;1 | tail -5 &amp;&amp; Rscript -e 'chk &lt;- devtools::check(args = "--no-manual", error_on = "warning"); cat("check ok\n")'</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript -e 'devtools::test(reporter = "summary")'` exits 0 with zero failures
    - All 11 blocks in tests/testthat/test-figaro.R pass
    - All pre-existing blocks in tests/testthat/test-workflow.R pass (regression)
    - `Rscript -e 'devtools::document()'` produces no unexpected git diff (NAMESPACE and man/*.Rd are idempotent from Plan 02)
    - `Rscript -e 'devtools::check()'` completes with `0 errors | 0 warnings | 0 notes` (or only pre-existing notes documented in the summary)
    - `Rscript -e 'library(sube); nzchar(system.file("extdata","figaro-sample",package="sube"))'` prints `[1] TRUE` after `devtools::install(quick=TRUE)`
    - DESCRIPTION Version unchanged: `grep '^Version:' DESCRIPTION` returns `Version: 0.1.2`
    - Files modified in the whole phase (all 4 plans): `git diff --name-only master...HEAD` returns exactly these 8 paths — `R/import.R`, `R/utils.R`, `NAMESPACE`, `man/read_figaro.Rd`, `tests/testthat/test-figaro.R`, `inst/extdata/figaro-sample/flatfile_eu-ic-supply_sample.csv`, `inst/extdata/figaro-sample/flatfile_eu-ic-use_sample.csv`, `_pkgdown.yml`, `NEWS.md` (9 paths total) — plus any summary markdown files under `.planning/phases/05-figaro-sut-ingestion/`
    - Locked files are UNTOUCHED: `git diff --name-only master...HEAD | grep -E '^(R/matrices\.R|R/compute\.R|R/models\.R|R/filter_plot_export\.R|R/paper_tools\.R|DESCRIPTION)$' | wc -l` returns 0
  </acceptance_criteria>

  <done>
    Full test suite green, `devtools::check()` reports 0 errors, 0 warnings,
    0 new notes. Fixtures install cleanly and are reachable via
    `system.file()`. DESCRIPTION Version is still `0.1.2`. None of the locked
    files (R/matrices.R, R/compute.R, R/models.R, R/filter_plot_export.R,
    R/paper_tools.R, DESCRIPTION) have been modified anywhere in Phase 5.
    Phase 5 is ready to hand off to `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan only edits two documentation files and runs verification commands. No new user-facing API, no new data handling, no new dependencies. |

## STRIDE Threat Register

Per 05-RESEARCH.md § Security Domain, this plan makes purely documentation
and release-plumbing changes. No new code paths, no new trust boundaries.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Info Disclosure | NEWS.md wording leaks internal details | accept | NEWS.md wording describes the user-facing API (`read_figaro()` signature, aggregation behavior, FIGW1 preservation). All of this is intentionally public and belongs in release notes. |
| T-05-15 | Tampering | `_pkgdown.yml` YAML injection | accept | YAML is parsed by the `yaml` R package at pkgdown build time. No code execution from the YAML contents; reference entries are string function names that must exist as `man/*.Rd` files. A typo becomes a pkgdown build error, not a security issue. |
| T-05-16 | Integrity | R CMD check gates inconsistent artifacts | mitigate | `devtools::check()` is the phase gate. It builds the tarball, installs it, runs all tests against the installed version, and validates roxygen coherence. A failing check blocks the phase exit. This closes Phase 5 at a known-good state. |

Only one mitigate-class threat: R CMD check is the structural integrity gate
and is explicitly sequenced as Task 2.
</threat_model>

<verification>
Overall plan verification — run after both tasks:

1. `grep -A5 'Data import and preparation' _pkgdown.yml` shows `read_figaro` listed after `import_suts`
2. `head -30 NEWS.md` shows the new `# sube (development version)` section with bullets for `read_figaro` and `.coerce_map`
3. `grep '^Version:' DESCRIPTION` returns `Version: 0.1.2`
4. `Rscript -e 'devtools::test(reporter = "summary")'` reports zero failures
5. `Rscript -e 'devtools::check()'` exits 0 with `0 errors | 0 warnings | 0 notes` (or only pre-existing notes)
6. `Rscript -e 'library(sube); ?read_figaro' 2>&1 | head -5` resolves to the man page
7. `git diff --name-only master...HEAD | sort` shows only the expected Phase 5 files plus summary markdown files
8. Locked files untouched: `git diff --name-only master...HEAD | grep -E 'R/(matrices|compute|models|filter_plot_export|paper_tools)\.R|^DESCRIPTION$' | wc -l` returns 0
</verification>

<success_criteria>
- `_pkgdown.yml` lists `read_figaro` in the "Data import and preparation" reference group between `import_suts` and `extract_domestic_block`
- `NEWS.md` has a new `# sube (development version)` section documenting both `read_figaro()` and the `.coerce_map()` NACE extension
- `DESCRIPTION` Version is unchanged at `0.1.2` (D-23)
- `devtools::check()` is green: 0 errors, 0 warnings, 0 new notes
- `devtools::test()` is fully green: all 11 test-figaro.R blocks + all test-workflow.R blocks pass
- `library(sube); system.file("extdata","figaro-sample",package="sube")` returns a valid path after install
- All four Phase 5 plans are complete; Phase 5 is ready for `/gsd-verify-work`
- Every FIG-XX requirement (FIG-01, FIG-02, FIG-03, FIG-04) has at least one passing automated test backing it
</success_criteria>

<output>
After completion, create `.planning/phases/05-figaro-sut-ingestion/05-04-SUMMARY.md` documenting:
- Full `devtools::test()` output (all suite results)
- Full `devtools::check()` output (errors/warnings/notes summary)
- `git diff --stat master...HEAD` showing all Phase 5 changes across 4 plans
- List of all 11 test-figaro.R blocks with PASS status and requirement ID mapping
- Confirmation that locked files (R/matrices.R, R/compute.R, R/models.R, R/filter_plot_export.R, R/paper_tools.R, DESCRIPTION) are unchanged
- Any pre-existing notes or acceptable deviations observed in `R CMD check` output
- Phase 5 gate status: READY FOR VERIFY or CHECKPOINT (with reason)
</output>
