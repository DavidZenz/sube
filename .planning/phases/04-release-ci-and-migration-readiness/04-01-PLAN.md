---
phase: 04-release-ci-and-migration-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/R-CMD-check.yaml
  - README.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DOC-03
  - CI-01
user_setup: []
must_haves:
  truths:
    - Maintainers can follow one documented package check path locally and in GitHub Actions.
    - GitHub Actions reflects current package assumptions and provides useful maintenance signal.
  artifacts:
    - .github/workflows/R-CMD-check.yaml
    - README.md
  key_links:
    - Local release instructions match the CI path closely enough for maintainers to debug one workflow.
    - CI hardening improves signal quality instead of only adding ceremony.
---

<objective>
Harden the package release and CI path so maintainers have a clear, current, and reproducible check workflow both locally and in GitHub Actions.

Purpose: Make the release path trustworthy and maintainable after the workflow and documentation stabilization work in earlier phases.
Output: Updated Actions workflow and local release instructions that reflect the same package-check assumptions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-ci-and-migration-readiness/04-CONTEXT.md
@.planning/phases/04-release-ci-and-migration-readiness/04-RESEARCH.md
@.github/workflows/R-CMD-check.yaml
@README.md
@DESCRIPTION
@tests/testthat/test-workflow.R
</context>

<threat_model>
- T-4-01: Maintainers may rely on local release commands or CI checks that no longer reflect the same package assumptions, producing inconsistent failure modes and harder releases.
- Mitigation: Align local instructions and Actions workflow, and harden the workflow where current assumptions are implicit or under-specified.
- Block on: medium severity or higher mismatches between documented local checks and CI behavior.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Align and harden the CI/release check path</name>
  <files>.github/workflows/R-CMD-check.yaml, README.md</files>
  <read_first>.github/workflows/R-CMD-check.yaml, README.md, DESCRIPTION, NEWS.md</read_first>
  <action>Review the current `R-CMD-check` workflow and the README release section together. Update them so maintainers can see one coherent release path: current matrix/support assumptions, the local commands that mirror CI, and any useful workflow hardening such as clearer job naming, concurrency/cancel behavior, practical platform coverage, or explicit setup/check options. Keep CI edits pragmatic and package-focused; avoid unrelated workflow sprawl.</action>
  <verify>R -q -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - README release instructions and CI workflow describe the same package-check path
    - `.github/workflows/R-CMD-check.yaml` is measurably hardened beyond the current minimal baseline
    - At least one verification command in docs is the same command family maintainers use locally
  </acceptance_criteria>
  <done>The local release path and GitHub Actions check path are aligned and current.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_dir("tests/testthat")'` succeeds
- [ ] `R CMD build .` succeeds if environment permits
- [ ] README and Actions workflow no longer imply different release paths
</verification>

<success_criteria>
- All tasks completed
- Maintainers can trust that local release instructions and GitHub Actions are aligned
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-ci-and-migration-readiness/04-01-SUMMARY.md`
</output>
