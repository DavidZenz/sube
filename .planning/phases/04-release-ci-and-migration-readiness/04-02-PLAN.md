---
phase: 04-release-ci-and-migration-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/scripts/run_legacy_pipeline.R
  - README.md
  - tests/testthat/test-workflow.R
autonomous: true
requirements:
  - MIG-01
user_setup: []
must_haves:
  truths:
    - The legacy wrapper remains a narrow, documented bridge into package functions.
    - Script-era users can see the required inputs and expected outputs without treating the wrapper as a separate architecture.
  artifacts:
    - inst/scripts/run_legacy_pipeline.R
    - README.md
    - tests/testthat/test-workflow.R
  key_links:
    - Wrapper usage is explicit and testable.
    - Migration guidance points users toward package functions while preserving a compatibility path.
---

<objective>
Audit and document the legacy wrapper so it remains a minimal but real migration bridge for users coming from the script-era workflow.

Purpose: Preserve compatibility without reopening the old script-first architecture.
Output: Verified wrapper assumptions, explicit usage guidance, and regression protection where appropriate.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-ci-and-migration-readiness/04-CONTEXT.md
@.planning/phases/04-release-ci-and-migration-readiness/04-RESEARCH.md
@inst/scripts/run_legacy_pipeline.R
@R/import.R
@R/matrices.R
@R/compute.R
@R/filter_plot_export.R
@README.md
@tests/testthat/test-workflow.R
</context>

<threat_model>
- T-4-02: Legacy users may believe a migration bridge exists but encounter undocumented wrapper assumptions or silent output differences.
- Mitigation: Make wrapper inputs, outputs, and purpose explicit; add verification around the wrapper path where feasible.
- Block on: medium severity or higher wrapper ambiguity that prevents script-era users from reaching the package workflow reliably.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Verify and document the legacy wrapper path</name>
  <files>inst/scripts/run_legacy_pipeline.R, README.md, tests/testthat/test-workflow.R</files>
  <read_first>inst/scripts/run_legacy_pipeline.R, README.md, tests/testthat/test-workflow.R, R/filter_plot_export.R</read_first>
  <action>Audit `inst/scripts/run_legacy_pipeline.R` for usage clarity, input assumptions, output naming, and failure behavior. Keep the wrapper intentionally small, but update it or its surrounding docs if the required arguments, produced files, or intended migration role are unclear. Add or refine regression coverage if the wrapper path can be exercised safely from tests or through adjacent contract assertions. Document the wrapper as a compatibility bridge into package functions, not as the preferred interface.</action>
  <verify>R -q -e 'testthat::test_dir("tests/testthat")'</verify>
  <acceptance_criteria>
    - Wrapper usage and outputs are explicitly documented or made self-explanatory
    - Migration guidance points users from the wrapper toward package functions
    - Any wrapper-specific assumptions that matter to users are validated or at least asserted indirectly in tests/docs
  </acceptance_criteria>
  <done>The legacy wrapper path is narrow, explicit, and credible as a migration bridge.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `R -q -e 'testthat::test_dir("tests/testthat")'` succeeds
- [ ] Wrapper usage and expected outputs are discoverable without source spelunking
</verification>

<success_criteria>
- All tasks completed
- Script-era users have a documented bridge into the package workflow without reopening the old architecture
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-ci-and-migration-readiness/04-02-SUMMARY.md`
</output>
