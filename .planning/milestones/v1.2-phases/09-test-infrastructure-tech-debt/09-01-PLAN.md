---
phase: 09-test-infrastructure-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-workflow.R
  - .planning/PROJECT.md
  - NEWS.md
  - DESCRIPTION
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "R CMD check --as-cran on the built tarball produces zero test failures from test-workflow.R"
    - "devtools::test() runs all tests green with no regressions"
    - "The system2() call at test-workflow.R passes R_LIBS to the child Rscript process"
    - "The resolution strategy is documented in PROJECT.md, NEWS.md, DESCRIPTION, and an inline comment"
  artifacts:
    - path: "tests/testthat/test-workflow.R"
      provides: "Fixed subprocess invocation with R_LIBS threading"
      contains: "env.*R_LIBS"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry for INFRA-01 resolution"
      contains: "R_LIBS"
    - path: "NEWS.md"
      provides: "Changelog entry for subprocess fix"
      contains: "R_LIBS"
    - path: "DESCRIPTION"
      provides: "Note about legacy wrapper test .libPaths() threading"
      contains: "legacy wrapper"
  key_links:
    - from: "tests/testthat/test-workflow.R"
      to: "inst/scripts/run_legacy_pipeline.R"
      via: "system2() subprocess invocation with R_LIBS env"
      pattern: "system2.*env.*R_LIBS"
---

<objective>
Fix the legacy-wrapper subprocess test (`test-workflow.R:218`) so it passes under
`R CMD check --as-cran` by threading `.libPaths()` into the child `Rscript` process
via the `R_LIBS` environment variable, then document the fix in all four required
locations.

Purpose: Close the last non-blocking tarball-check failure inherited from v1.1,
completing the INFRA-01 requirement.

Output: Modified `test-workflow.R` with working subprocess, updated PROJECT.md,
NEWS.md, and DESCRIPTION with resolution documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-infrastructure-tech-debt/09-CONTEXT.md
@.planning/phases/09-test-infrastructure-tech-debt/09-RESEARCH.md

<interfaces>
<!-- Key call site the executor must modify. Extracted from test-workflow.R lines 218-251. -->

From tests/testthat/test-workflow.R (lines 240-245 — the ONLY code change target):
```r
status <- system2(
  Sys.which("Rscript"),
  c(script_path, sut_path, cpa_map_path, ind_map_path, inputs_path, output_dir),
  stdout = TRUE,
  stderr = TRUE
)
```

The existing assertion (line 247 — DO NOT MODIFY):
```r
expect_null(attr(status, "status"))
```

From DESCRIPTION (current — no Note field exists yet):
```
Description: Tools for importing supply-use tables, building domestic
    matrices, computing Leontief-style SUBE multipliers, estimating
    panel and cross-sectional elasticity regressions, filtering derived
    results, plotting tidy outputs, and exporting package objects for
    reproducible input-output analysis workflows.
```

From .planning/PROJECT.md Key Decisions table (append new row here):
```
| Decision | Rationale | Outcome |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread .libPaths() into subprocess via R_LIBS env var</name>
  <files>tests/testthat/test-workflow.R</files>

  <read_first>
    - tests/testthat/test-workflow.R (full file — understand current test structure, locate lines 240-245)
    - inst/scripts/run_legacy_pipeline.R (read-only — confirm it calls library(sube) and needs no changes)
    - .planning/phases/09-test-infrastructure-tech-debt/09-RESEARCH.md (Pattern 1 section — exact code example)
  </read_first>

  <action>
Modify the `system2()` call at lines 240-245 of `tests/testthat/test-workflow.R`.
Insert two lines BEFORE the `system2()` call and add the `env` parameter:

1. Add an inline comment block (3-4 lines) explaining why `R_LIBS` threading is needed:
   ```
   # Thread .libPaths() into child Rscript via R_LIBS so R CMD check's
   # temporary library directory is visible to the subprocess. Without this,
   # library(sube) fails in the child because R CMD check isolates the
   # installed package in a temp dir that is not on the default search path.
   ```

2. Add the `r_libs` variable construction:
   ```r
   r_libs <- paste(.libPaths(), collapse = .Platform$path.sep)
   ```

3. Modify the `system2()` call to add the `env` parameter as the last named argument:
   ```r
   status <- system2(
     Sys.which("Rscript"),
     c(script_path, sut_path, cpa_map_path, ind_map_path, inputs_path, output_dir),
     stdout = TRUE,
     stderr = TRUE,
     env    = paste0("R_LIBS=", r_libs)
   )
   ```

DO NOT modify:
- The `expect_null(attr(status, "status"))` assertion on line 247
- The `expect_true(dir.exists(output_dir))` assertion on line 248
- The `expect_true(file.exists(...))` assertions on lines 249-250
- Any other test blocks in the file
- `inst/scripts/run_legacy_pipeline.R` (the script is correct; only the test invocation needs fixing)

Per D-01: Use `R_LIBS` env var threading, NOT `skip_on_cran()` or any skip workaround.
Per D-02: `.Platform$path.sep` handles cross-platform (`:` on Unix, `;` on Windows).
  </action>

  <verify>
    <automated>cd /home/zenz/R/sube && Rscript -e "devtools::test(filter = 'workflow')" 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - test-workflow.R contains the string `r_libs <- paste(.libPaths(), collapse = .Platform$path.sep)`
    - test-workflow.R contains the string `env    = paste0("R_LIBS=", r_libs)`
    - test-workflow.R contains the comment `# Thread .libPaths() into child Rscript via R_LIBS`
    - test-workflow.R still contains `expect_null(attr(status, "status"))` (assertion unchanged)
    - `Rscript -e "devtools::test(filter = 'workflow')"` exits with 0 failures
  </acceptance_criteria>

  <done>
The `system2()` call in test-workflow.R passes `.libPaths()` to the child process via
`R_LIBS` env var. `devtools::test(filter = "workflow")` runs green. The inline comment
explains the fix for future maintainers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document resolution in PROJECT.md, NEWS.md, and DESCRIPTION</name>
  <files>.planning/PROJECT.md, NEWS.md, DESCRIPTION</files>

  <read_first>
    - .planning/PROJECT.md (Key Decisions table — append new row)
    - NEWS.md (top of file — add bullet under existing v1.2 entries)
    - DESCRIPTION (Description field — append note about legacy wrapper test)
  </read_first>

  <action>
Per D-03, document the INFRA-01 resolution in three files (the fourth location — inline
comment — was handled in Task 1).

**1. PROJECT.md — Key Decisions table**

Append this row to the `## Key Decisions` table in `.planning/PROJECT.md`:

```
| Thread `.libPaths()` via `R_LIBS` into legacy-wrapper subprocess test (INFRA-01) | `R CMD check --as-cran` isolates the package in a temp library that `system2()` children don't inherit; passing `R_LIBS` from `.libPaths()` is the zero-dependency cross-platform fix | Good (v1.2) |
```

**2. NEWS.md — Changelog bullet**

Add this bullet AFTER the existing INFRA-02 bullet (the last bullet currently in the file),
maintaining the same indentation style (two-space continuation):

```
- Fixed the legacy-wrapper subprocess test (`test-workflow.R`) to pass under
  `R CMD check --as-cran` by threading `.libPaths()` into the child `Rscript`
  process via the `R_LIBS` environment variable (INFRA-01). Previously, the
  subprocess could not find `sube` because `R CMD check` installs to a
  temporary library not on the child's default search path.
```

**3. DESCRIPTION — Note in Description field**

The RESEARCH.md Open Question 2 flagged that a standalone `Note:` field may cause
`R CMD check` NOTEs. Instead, append one sentence to the end of the existing
`Description:` paragraph text (before the closing period or after the last word):

Change the Description field from:
```
Description: Tools for importing supply-use tables, building domestic
    matrices, computing Leontief-style SUBE multipliers, estimating
    panel and cross-sectional elasticity regressions, filtering derived
    results, plotting tidy outputs, and exporting package objects for
    reproducible input-output analysis workflows.
```

To:
```
Description: Tools for importing supply-use tables, building domestic
    matrices, computing Leontief-style SUBE multipliers, estimating
    panel and cross-sectional elasticity regressions, filtering derived
    results, plotting tidy outputs, and exporting package objects for
    reproducible input-output analysis workflows. The legacy wrapper test
    threads .libPaths() via R_LIBS to support R CMD check environments.
```

DO NOT add a standalone `Note:` field to DESCRIPTION.
  </action>

  <verify>
    <automated>cd /home/zenz/R/sube && grep -c "R_LIBS" .planning/PROJECT.md NEWS.md DESCRIPTION</automated>
  </verify>

  <acceptance_criteria>
    - .planning/PROJECT.md Key Decisions table contains the string `Thread .libPaths() via R_LIBS`
    - NEWS.md contains the string `R_LIBS environment variable (INFRA-01)`
    - DESCRIPTION Description field contains the string `threads .libPaths() via R_LIBS`
    - DESCRIPTION does NOT contain a standalone `Note:` field line
    - `Rscript -e "devtools::test()"` exits with 0 failures (full suite, no regressions per D-04)
  </acceptance_criteria>

  <done>
The INFRA-01 resolution is documented in all four locations per D-03: inline comment (Task 1),
PROJECT.md Key Decisions, NEWS.md changelog bullet, and DESCRIPTION field. Full test suite
passes with no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> child Rscript | Parent passes R_LIBS env var containing file system paths to subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | R_LIBS env var value | accept | R_LIBS is constructed from `.libPaths()` at test time — no user input, no network. Test-only code path, not shipped to end users. The env var contains only local filesystem paths already trusted by the R session. |
| T-09-02 | Information Disclosure | .libPaths() leaked to subprocess | accept | Library paths are not sensitive; they are standard R session metadata visible to any R process on the same machine. Test-only context. |
</threat_model>

<verification>
Per D-04, both verification gates must pass:

1. **Quick gate:** `Rscript -e "devtools::test(filter = 'workflow')"` — subprocess test passes
2. **Full regression:** `Rscript -e "devtools::test()"` — all tests green, no regressions
3. **Phase gate:** Build tarball and run `R CMD check --as-cran` — zero test failures from test-workflow.R

Gate 3 is the definitive verification. If devtools::test() passes but R CMD check fails,
the fix is incomplete.
</verification>

<success_criteria>
- test-workflow.R:218 subprocess test passes under `R CMD check --as-cran` on built tarball
- devtools::test() runs all tests green (no regressions)
- Resolution documented in 4 locations: inline comment, PROJECT.md, NEWS.md, DESCRIPTION
- No new dependencies added to DESCRIPTION
- No skip-based workarounds used (per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-infrastructure-tech-debt/09-01-SUMMARY.md`
</output>
