% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{batch_sube}
\alias{batch_sube}
\title{Batch-Run the SUBE Pipeline Over Country x Year Groups}
\usage{
batch_sube(
  sut_data,
  cpa_map,
  ind_map,
  inputs,
  countries = NULL,
  years = NULL,
  by = c("country_year", "country", "year"),
  estimate = FALSE,
  ...
)
}
\arguments{
\item{sut_data}{A \code{sube_suts} object (from \code{\link[=import_suts]{import_suts()}} or
\code{\link[=read_figaro]{read_figaro()}}).}

\item{cpa_map, ind_map, inputs}{Correspondence tables and industry inputs;
see \code{\link[=build_matrices]{build_matrices()}} and \code{\link[=compute_sube]{compute_sube()}}.}

\item{countries}{Optional character vector of REP codes; defaults to all
countries in \code{sut_data}.}

\item{years}{Optional integer vector of years; defaults to all years in
\code{sut_data}.}

\item{by}{Grouping key; one of \code{"country_year"} (default, per D-8.8),
\code{"country"}, or \code{"year"}.}

\item{estimate}{Forwarded per-group to the compute stage; see
\code{\link[=run_sube_pipeline]{run_sube_pipeline()}} (D-8.4).}

\item{...}{Forwarded per-group to \code{\link[=build_matrices]{build_matrices()}} and \code{\link[=compute_sube]{compute_sube()}}.}
}
\value{
An object of class \code{c("sube_batch_result", "list")} with elements
\verb{$results} (named list of \link[=run_sube_pipeline]{sube_pipeline_result}, one
per group), \verb{$summary} (rbindlist of per-group \verb{$results$summary}),
\verb{$tidy} (rbindlist of per-group \verb{$results$tidy}), \verb{$diagnostics}
(rbindlist of per-group \verb{$diagnostics} with an added \code{group_key}
column), and \verb{$call} (provenance metadata including \code{by}, \code{n_groups},
\code{n_errors}).
}
\description{
Loops \code{\link[=run_sube_pipeline]{run_sube_pipeline()}}-style processing over a pre-imported \code{sube_suts}
table grouped by country, year, or country-year. Each group produces a
\link[=run_sube_pipeline]{sube_pipeline_result}; per-group results are preserved
alongside merged tidy \verb{$summary}, \verb{$tidy}, and \verb{$diagnostics} tables
suitable for downstream analysis.
}
\details{
Per D-8.7, each group's processing is wrapped in \code{tryCatch}; a
failing group appends a diagnostics row with \code{stage = "pipeline"},
\code{status = "error"} and the loop continues. A single summary \code{warning()}
is emitted at the end if any group errored or produced non-\code{"ok"}
diagnostics (per D-8.10).
}
\examples{
sut <- sube_example_data("sut_data")
# Duplicate the sample to a second year so batch_sube has 2 groups:
sut2 <- data.table::copy(sut); sut2[, YEAR := 2021L]
sut_multi <- rbind(sut, sut2)
class(sut_multi) <- c("sube_suts", class(sut_multi))

inp <- sube_example_data("inputs")
inp2 <- data.table::copy(inp); inp2[, YEAR := 2021L]
inp_multi <- rbind(inp, inp2)

result <- batch_sube(
  sut_data = sut_multi,
  cpa_map  = sube_example_data("cpa_map"),
  ind_map  = sube_example_data("ind_map"),
  inputs   = inp_multi
)
result$summary
result$diagnostics

}
\seealso{
\code{\link[=run_sube_pipeline]{run_sube_pipeline()}}
}
