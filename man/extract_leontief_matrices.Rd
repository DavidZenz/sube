% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper_tools.R
\name{extract_leontief_matrices}
\alias{extract_leontief_matrices}
\alias{prepare_sube_comparison}
\alias{plot_paper_comparison}
\alias{plot_paper_regression}
\alias{plot_paper_interval_ranges}
\title{Leontief and Paper-Style Comparison Tools}
\usage{
extract_leontief_matrices(
  results,
  matrix = c("A", "L"),
  format = c("list", "long", "wide")
)

prepare_sube_comparison(
  leontief,
  models,
  measure = c("multiplier", "elasticity"),
  aggregate_years = TRUE,
  variables = c("GO", "VA", "EMP", "CO2"),
  apply_paper_filters = TRUE
)

plot_paper_comparison(
  data,
  kind = c("by_country", "by_product", "density"),
  measure = c("multiplier", "elasticity"),
  variables = c("GO", "VA", "EMP"),
  type = NULL,
  label_outliers = TRUE
)

plot_paper_regression(
  data,
  method = c("between", "pooled", "ols"),
  measure = c("multiplier", "elasticity"),
  variables = c("GO", "VA", "EMP"),
  include_ci = TRUE
)

plot_paper_interval_ranges(
  models,
  by = c("country", "product"),
  variables = c("GO", "VA", "EMP")
)
}
\arguments{
\item{results}{A \code{sube_results} object returned by \code{\link[=compute_sube]{compute_sube()}}.}

\item{matrix}{Which matrix to extract. One of \code{"A"} or \code{"L"}.}

\item{format}{Output format. One of \code{"list"}, \code{"long"}, or \code{"wide"}.}

\item{leontief}{A \code{sube_results} object.}

\item{models}{A \code{sube_models} object returned by \code{\link[=estimate_elasticities]{estimate_elasticities()}}.}

\item{measure}{Which quantity to compare. One of \code{"multiplier"} or
\code{"elasticity"}.}

\item{aggregate_years}{Whether yearly Leontief and OLS results should be
averaged over time as in the paper comparison tables.}

\item{variables}{Variables to include.}

\item{apply_paper_filters}{Whether to apply the 2024 paper exclusion rules.}

\item{data}{A tidy comparison table returned by \code{\link[=prepare_sube_comparison]{prepare_sube_comparison()}}.}

\item{kind}{Plot type: \code{"by_country"}, \code{"by_product"}, or \code{"density"}.}

\item{type}{Comparison types to plot. Defaults to all available types.}

\item{label_outliers}{Whether to annotate Tukey outliers.}

\item{method}{Model type to compare against Leontief in regression plots.}

\item{include_ci}{Whether to include the prediction interval ribbon.}

\item{by}{Orientation for interval range plots.}
}
\value{
\code{extract_leontief_matrices()} returns a list or \code{data.table}.
\code{prepare_sube_comparison()} returns a tidy \code{data.table}.
\code{plot_paper_comparison()}, \code{plot_paper_regression()}, and
\code{plot_paper_interval_ranges()} return named lists of \code{ggplot} objects.
}
\description{
These helpers expose the Leontief matrices already computed by
\code{\link[=compute_sube]{compute_sube()}}, build comparison datasets that align Leontief and SUBE
estimates, and generate plots modeled on the archived paper workflow.
}
