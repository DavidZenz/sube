% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_figaro}
\alias{read_figaro}
\title{Import FIGARO Supply-Use Tables}
\usage{
read_figaro(
  path,
  year,
  final_demand_vars = c("P3_S13", "P3_S14", "P3_S15", "P51G", "P5M")
)
}
\arguments{
\item{path}{Directory containing one FIGARO supply file and one FIGARO
use file. Neither a single-file path nor recursive directory scanning
is supported.}

\item{year}{Four-digit integer reference year for the data (the year
encoded in the FIGARO filename, e.g. \code{2023} in
\code{flatfile_eu-ic-supply_25ed_2023.csv}). Required; there is no
auto-inference from the filename.}

\item{final_demand_vars}{Character vector of FIGARO \code{colPi} codes to sum
into the synthetic \code{VAR = "FU_bas"} row on the use file. Defaults to
the full FIGARO final-demand set. Every supplied code must exist in
the use file's \code{colPi} column; unknown codes are a hard error.}
}
\value{
An object of class \code{c("sube_suts", "data.table", "data.frame")}
with columns \code{REP}, \code{PAR}, \code{CPA}, \code{VAR}, \code{VALUE}, \code{YEAR}, \code{TYPE}.
}
\description{
\code{read_figaro()} reads a pair of Eurostat FIGARO industry-by-industry
flat-format CSV files (one supply, one use) from a single directory and
returns the canonical \code{sube_suts} long table with columns \code{REP}, \code{PAR},
\code{CPA}, \code{VAR}, \code{VALUE}, \code{YEAR}, and \code{TYPE}. The output feeds directly into
\code{\link[=extract_domestic_block]{extract_domestic_block()}}, \code{\link[=build_matrices]{build_matrices()}}, and \code{\link[=compute_sube]{compute_sube()}}
without modification.
}
\details{
The function expects a directory containing exactly one supply file and
exactly one use file. Files are identified by filename: supply files
match \verb{-supply-} or \verb{_supply_} (case-insensitive), use files match
\verb{-use-} or \verb{_use_}. Zero or multiple matches on either side is a hard
error. Single-file input is not supported in this release.

Primary-input rows (\code{rowPi} values not starting with \code{CPA_}, e.g.
\code{B2A3G}, \code{D1}, \code{D21X31}, \code{D29X39}, \code{OP_RES}, \code{OP_NRES}) are dropped
during import because they are SNA/ESA value-added blocks, not products.
The \code{CPA_} prefix on the remaining \code{rowPi} values is stripped so product
codes match NACE-style industry codes lexically.

FIGARO publishes five final-demand codes (\code{P3_S13}, \code{P3_S14}, \code{P3_S15},
\code{P51G}, \code{P5M}). On the use file these are summed per \verb{(REP, PAR, CPA)}
into a single synthetic row with \code{VAR = "FU_bas"} so the result consumes
cleanly with \code{\link[=build_matrices]{build_matrices()}} \code{final_demand_var = "FU_bas"}. Pass a
custom character vector to \code{final_demand_vars} to override the
aggregation set (e.g. only household consumption).

\code{FIGW1} (FIGARO rest-of-world 1) is a real country code in FIGARO
releases and is preserved in the output alongside real country codes.

Full FIGARO flat files (e.g. the 25th-edition 2023 release) are
approximately 400--500 MB each and expand to roughly 3--5 GB of peak
resident memory during \code{data.table::fread()}. Ensure sufficient RAM
before calling \code{read_figaro()} on a production release. The synthetic
fixture under \verb{inst/extdata/figaro-sample/} is a few kilobytes and is
intended for tests and examples only.

The \code{year} argument should match the reference / data year encoded in
the FIGARO filename (e.g. \code{2023} in \code{flatfile_eu-ic-supply_25ed_2023.csv}),
not the edition or release year.
}
