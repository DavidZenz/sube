% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_sube_pipeline}
\alias{run_sube_pipeline}
\title{Run the SUBE Pipeline End-to-End}
\usage{
run_sube_pipeline(
  path,
  cpa_map,
  ind_map,
  inputs,
  source = c("wiod", "figaro"),
  domestic_only = TRUE,
  estimate = FALSE,
  ...
)
}
\arguments{
\item{path}{Single character path to a SUT file or directory (WIOD:
workbook/CSV or dir; FIGARO: directory containing supply+use flatfiles).}

\item{cpa_map, ind_map}{Correspondence tables; see \code{\link[=build_matrices]{build_matrices()}}.}

\item{inputs}{Industry-level inputs with columns \code{YEAR}, \code{REP}, an industry
identifier, and at least \code{GO}.}

\item{source}{One of \code{"wiod"} or \code{"figaro"}. Selects the importer. No
auto-detect (D-8.2).}

\item{domestic_only}{If \code{TRUE} (default), runs \code{\link[=extract_domestic_block]{extract_domestic_block()}} on
the imported SUTs before building matrices.}

\item{estimate}{If \code{TRUE} and \code{build_matrices(..., inputs = inputs)}
produces non-empty \verb{$model_data}, also runs \code{\link[=estimate_elasticities]{estimate_elasticities()}} and
attaches the result to \verb{$models}. Default \code{FALSE} (D-8.4).}

\item{...}{Importer-specific arguments (e.g. \code{sheets}, \code{recursive} for WIOD;
\code{year}, \code{final_demand_vars} for FIGARO) and compute-specific arguments
(e.g. \code{metrics}, \code{diagonal_adjustment}, \code{zero_replacement}) forwarded to
\code{\link[=compute_sube]{compute_sube()}}.}
}
\value{
An object of class \code{c("sube_pipeline_result", "list")} with
elements \verb{$results} (the \link[=compute_sube]{sube_results} object), \verb{$models}
(a \code{sube_models} object from \code{\link[=estimate_elasticities]{estimate_elasticities()}} or \code{NULL}),
\verb{$diagnostics} (a unified diagnostics \code{data.table}; see \emph{Details}), and
\verb{$call} (provenance metadata).
}
\description{
Chains the SUBE import → domestic filter → matrix construction → compute
step into a single call, returning a structured result with unified
diagnostics. The FIGARO source routes through \code{\link[=read_figaro]{read_figaro()}}; WIOD-style
CSVs/workbooks route through \code{\link[=import_suts]{import_suts()}}.
}
\details{
The diagnostics table has columns \code{country} (character; \code{NA} for
pipeline-level aggregates), \code{year} (integer; \code{NA} for pipeline-level
aggregates), \code{stage} (\code{import}, \code{build}, \code{compute}, or \code{pipeline}),
\code{status} (\code{ok}, \code{singular_supply}, \code{singular_go}, \code{singular_leontief},
\code{skipped_alignment}, \code{coerced_na}, \code{inputs_misaligned}, or \code{error}),
\code{message} (human-readable reason), and \code{n_rows} (optional row count,
populated for \code{coerced_na} aggregates). If any row has \code{status != "ok"} a
single \code{warning()} summarising counts is emitted at the end of the call.
}
\examples{
sut_path <- system.file("extdata", "sample", "sut_data.csv", package = "sube")
result <- run_sube_pipeline(
  path    = sut_path,
  cpa_map = sube_example_data("cpa_map"),
  ind_map = sube_example_data("ind_map"),
  inputs  = sube_example_data("inputs"),
  source  = "wiod"
)
result$results$summary
result$diagnostics

\dontrun{
# FIGARO branch needs a directory with real or synthetic FIGARO flatfiles:
figaro_dir <- system.file("extdata", "figaro-sample", package = "sube")
run_sube_pipeline(
  path    = figaro_dir,
  cpa_map = my_cpa_map,     # user-supplied
  ind_map = my_ind_map,
  inputs  = my_inputs,
  source  = "figaro",
  year    = 2023L
)
}

}
\seealso{
\code{\link[=batch_sube]{batch_sube()}}, \code{\link[=compute_sube]{compute_sube()}}, \code{\link[=build_matrices]{build_matrices()}},
\code{\link[=import_suts]{import_suts()}}, \code{\link[=read_figaro]{read_figaro()}}
}
